# AGENTS.md

> 本文件是 Agent 的项目入口，只保存当前状态、文档地图、跨工具协作前提和项目级红线。需求、进度、设计与规范由对应文档维护，本文件只引用，不重复说明。

## 读取顺序

每次会话开始按以下顺序加载上下文：

1. 阅读本文件了解当前状态、范围和文档入口。
2. 阅读 `TODO.md` 确认唯一当前任务。没有唯一当前任务时，只能在用户明确提出新任务后建立任务卡并进入开发。
3. 阅读 `docs/standards/README.md`，再加载本次任务相关规范。
4. 从 `docs/README.md` 进入相关设计、排障、方法论或概念文档。
5. 任务涉及 AI 评审测试数据时阅读 `data/README.md`，再按其中的题目与论文关联规则选择数据。
6. 检查当前代码、配置、Flyway SQL 和测试，以实际目录与运行行为为准。

README 只负责导航，不能代替任务相关文档和当前实现。

## 项目与当前状态

- LeetModel 是数学建模在线实训平台，项目定位与结构参考 `README.md`。
- 项目由一名独立开发者维护，目标是积累 Java 后端工程经验并服务实习面试。
- 当前同时开发 `LeetModel-backend/` 与 `LeetModel-frontend/`，以真实前后端联调完成 MVP 主链。`LeetModel-frontend/` 虽源自旧前端，但已重新作为 MVP 唯一前端维护并与后端契约对齐。
- `legacy/` 是历史与暂缓内容隔离区，默认不读取。当前 RAG 知识源位于 `rag_kb/`；`legacy/knowledge-base/` 仅是历史归档，不是运行时知识源。
- `data/` 是本地测试数据集，当前保存 AI 评审使用的 Markdown 题面和对应论文 PDF。它不是生产预置数据，具体数据清单、导入方式、关联规则和限制以 `data/README.md` 为准。
- `v2.0.0` 是当前已发布稳定版本；`dev` 在其后继续演进，MVP 主链、三级缓存和 RocketMQ 可靠异步链路已完成真实验收。新增或后续改动仍不得只凭局部代码、构建或单元测试宣称完整交付。
- 当前任务、候选任务和暂缓事项属于频繁变化的执行信息，统一以 `TODO.md` 为准，不在本文件重复维护。已完成任务和阶段过程不单独归档；长期仍有效的结论直接合并到所属正式文档，实际分支以 Git 状态为准。

## 开发者与 Agent

- 开发者主要使用 Agent 工具完成分析、设计、编码、测试和文档维护。
- Agent 协作采用文档驱动。项目状态、目标、设计、规范和关键决策必须写入仓库文档，不能只保存在会话上下文中。
- 文档是不同 Agent 工具之间的最低公共能力。所有 Agent 都应通过本文件和文档地图获得一致上下文。
- 一般情况下不使用 Skill，也不把项目流程和知识建立在 Skill 上。部分工具没有 Skill 功能，例如 DeepSeek Harness 极简模式。特定环境强制使用内置 Skill 时，只能将其作为执行辅助，最终状态和规则仍须写入仓库文档。

### 协作要求

1. Agent 以资深架构师和技术导师的方式协作，辅助架构设计、问题排查和知识提炼。
2. 项目以实习面试为导向。提出方案或在会话中说明设计思考、核心知识点、面试考点和常见问题，不能只给出结果。但严禁在源代码注释或 Javadoc 中出现“面试”、“考点”等字样或求职元信息；代码注释必须保持严肃专业的生产工程视角，只阐述职责定义、设计原因、技术约束与边界规则。
3. 同一时间只推进一个任务卡，小步输出并逐步验收，不并行生成多个任务卡的设计或实现。任务卡不等于阶段；用户在同一轮验收中连续提出的同类修改，应作为同一阶段内的连续任务卡复用当前分支，不得每完成一条反馈就合回 `dev`。用户可以明确要求托管模式；此时 Agent 按已确认路线图和依赖自动形成、执行并验收后续任务卡，不要求逐卡重复确认。
4. Agent 应主动读取上下文、调查代码、发现风险和提出问题，但不得擅自扩大任务范围或执行用户未要求的操作。
5. 复杂任务先形成包含业务流程、职责边界、数据归属、接口方向、实施步骤和非目标的任务卡，再修改代码。默认由用户确认任务卡；用户明确启用托管模式后，Agent 可以依据已确认路线图自动领取下一张任务卡，但不得扩大任务范围。
6. 本文件只用于定位上下文。具体任务必须继续阅读相关设计、规范和代码，不能根据入口摘要直接完成设计或实现。
7. 信息不足或文档冲突时，先核对当前代码和运行行为。仍有歧义时向用户确认，不基于猜测推进。
8. 任务执行中出现影响范围、方案或完成标准的新信息时，停止扩展实现并回到任务卡。托管模式下，只有现有文档和代码无法消除的实质歧义才需要用户重新确认。
9. `AGENTS.md` 应保持稳定，只维护新 Agent 长期有效的项目入口信息。当项目定位、长期开发范围、协作原则、项目级红线或文档入口等关键定义发生变化时，Agent 必须主动同步更新。
10. 当前任务、实施进度、当前阻塞和短期方案不写入 `AGENTS.md`。任务信息由 `TODO.md` 维护，实际分支由 Git 状态表达，避免入口文档频繁变动。
11. AI 能力是开发者重点参与的系统设计实践。进入 AI 功能实现前，必须由开发者主导完成充分的分析、提问、资料核验、方案比较和逐项确认；Agent 不得以快速生成完整方案代替共同设计，也不得在关键细节尚未确认时直接开发。
12. AI 系统设计允许多轮反复打磨并穿插必要的学习与探索。设计深度、思考量和停止条件由开发者判断，Agent 应小步推进、主动反问和暴露取舍，不以一次性产出或固定篇幅判定设计完成。
13. 在具体设计点上，Agent 可以主动提出少量、简单、便于判断的建议，用于补充开发者可能尚未考虑的风险、方案或灵感。建议必须说明其用途和主要代价，默认只作为候选项，不得未经确认写成正式设计，也不得借此扩展成大批远期规划。
14. 系统分析与服务梳理规范：对系统、微服务或公共层进行梳理分析时，遵循“先摸清全貌与流程，再直观图解结构，最后讨论问题与调整”的原则，严禁急于修改代码或跳步推进方案。必须先理清职责边界、组件依赖和端到端执行流程，并通过 Mermaid 图（架构拓扑图、依赖图、运行时时序图等）进行直观结构化呈现。在全貌与执行机制彻底清晰并向用户展示后，再进入潜在问题排查与调整方案讨论，整体节奏由用户引导。
15. 代码风格与注释可读性规范：代码首要原则是可读性与可维护性，严禁写过度语法糖、多层匿名函数嵌套或一长串复杂逻辑堆叠的晦涩代码。过于复杂的逻辑必须拆成多个明确步骤与局部变量，严禁出现内嵌多层复杂对象构造的“俄罗斯套娃式 return”。参数较多换行时第一个参数必须换行并独立闭合；尾随行内注释必须垂直对齐，不易直观识别的长常量、配置或复杂格式必须在行末添加对齐说明。

### 决策沉淀

对话中确认的长期有效信息不能只保留在当前会话。Agent 必须按职责写入对应文档：

| 信息 | 归档位置 |
|------|----------|
| 当前任务、候选任务和暂缓事项 | `TODO.md` |
| 全项目通用的 Agent 协作习惯和用户偏好 | `AGENTS.md` |
| 强制开发规则 | `docs/standards/` 下的对应规范 |
| 特定目录的结构、命名和职责 | 对应目录的 `README.md` |
| 具体功能的流程、规则和取舍 | 对应功能设计文档 |
| 跨服务架构、调用链和数据边界 | `docs/project/02-架构设计/` |
| 具体报错、根因和修复 | `docs/troubleshooting/` |
| 可复用方法和技术概念 | `docs/learning/` 或 `docs/concepts/` |

任务执行中和收尾时都必须检查本次对话是否产生需要沉淀的状态、规则、决策、排障或方法，不允许因任务较小而默认跳过。只有会长期影响新 Agent 进入项目时判断的关键定义，才同步更新 `AGENTS.md`。

## 文档地图

| 内容 | 唯一入口 |
|------|----------|
| 项目说明与目录结构 | `README.md` |
| 当前任务、候选任务与暂缓事项 | `TODO.md` |
| 文档分类与逐层导航 | `docs/README.md` |
| 全部强制规范 | `docs/standards/README.md` |
| 单目标、任务卡、分支、验收和完成定义 | `docs/standards/04-development-workflow.md` |
| 文档写作、解释保留、原子化和 README 职责 | `docs/standards/01-document-spec.md` |
| Git 分支、提交与合并流程 | `docs/standards/05-git-workflow-spec.md` |
| 数据库、数据隔离和 Flyway 规则 | `docs/standards/08-database-spec.md` |
| 微服务设计文档、功能目录和原子文档规则 | `docs/standards/12-microservice-design-document-spec.md` |
| 最小闭环与独立开发方法 | `docs/learning/系统分析经验.md` |
| 跨服务架构与数据边界 | `docs/project/02-架构设计/README.md` |
| 微服务职责、数据所有权与功能设计 | `docs/project/03-微服务设计/README.md` |
| 技术栈与版本选择 | `docs/project/02-架构设计/技术栈选型.md` |
| AI 评审测试题目与论文数据 | `data/README.md` |
| RAG 知识内容与维护入口 | `rag_kb/CONTEXT.md` |

具体任务必须沿 README 索引继续阅读相关原子文档，不能只依赖入口摘要作出设计或实现决策。

## 项目级补充

- 复杂功能先讲清参与者、触发条件、前置条件、主流程、状态变化、异常分支和最终结果，再划分职责、数据所有权、接口和实现步骤。详细方法参考 `docs/learning/系统分析经验.md`。
- 功能先完成最小可运行闭环，不追求产品完备。已有半成品优先验收和封口，不因不完美直接推倒重写。
- 设计以面试叙事价值与实现成本权衡。重要取舍必须记录结论和原因，一般不会发生但后果有害的场景仍需兜底。
- AI 相关设计既是实现依据，也是可独立讲解的系统设计实践。除业务闭环外，应按开发者确认的深度说明调度、状态、数据、异常、可观测性、成本、质量、扩展边界和方案取舍；必要时可以细化到拟议数据表与字段，但必须区分设计草案、已确认设计和当前实现。
- 当前阶段优先保证职责、数据所有权和业务边界清晰。性能问题在阻断闭环或有实测证据时再优化，不为未来复用提前公共化。
- 扩充 mock 数据前先判断现有数据能否覆盖测试场景，数据足够时不为数量扩充。
- 使用 `data/` 中的 AI 评审数据前必须核对题目与论文是否对应、文件是否满足当前提交限制；不得仅根据目录位置建立关联。
- TODO 由用户主导，Agent 只在用户要求时更新。进度和待办不得分散到其他文档；任务完成且必要的长期结论已同步到所属文档后，直接从 TODO 删除，不保留完成历史或单独的阶段过程归档。

## 红线

1. 不读取或修改 `legacy/`，除非用户明确要求。`LeetModel-frontend/` 已纳入当前 MVP 开发范围，可以按唯一任务卡读取和修改。
2. Agent 按开发与 Git 规范完成任务时，可以自主创建和切换本地阶段分支，并为阶段内每张已验收任务卡创建原子提交；整个阶段完成后统一非快进合入 `dev`，并在确认完整合并后删除本地阶段分支，不需要逐次获得用户授权。`push`、`force push`、`rebase`、破坏性 `reset`、改写历史、远端分支操作和删除用户文件仍必须由用户明确授权。`git status`、`git log` 和 `git diff` 等只读操作可以直接执行。
3. 不创建、不调用、不维护自定义 Skill，忽略 superpower 相关内容。
4. 不主动扩展任务范围。任务过大时先拆分为带依赖的任务卡；默认由用户确认后开发，托管模式下可以按已确认路线图逐卡自动推进。
5. 对话中形成的长期有效决策必须写入职责对应的文档，归类方式参考 `docs/README.md` 和相关目录 README。只有项目定位、长期范围、协作原则、项目级红线或文档入口等关键定义变化时，才同时更新 `AGENTS.md`。
6. 完成任务时按 `docs/standards/04-development-workflow.md` 验收，并检查相关设计、排障、方法论、概念和索引是否需要同步。
7. 用户当前指令优先于项目文档。本地阶段分支、任务卡原子提交、阶段合入 `dev` 和已合并分支清理按规范自主执行；远端写入、历史改写和破坏性操作遵守本节授权要求。
