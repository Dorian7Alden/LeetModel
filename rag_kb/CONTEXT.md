# RAG 知识库上下文

## 定位

`rag_kb/` 是 LeetModel 当前知识内容源，不是应用代码目录。知识内容的事实源是受 Git 管理的 Markdown；Elasticsearch 索引是可重建的派生数据。

在线 RAG V1 归 `ai-assistant-service`，使用 LangChain4j、统一 Embedding 调用和 Elasticsearch 基础向量召回。`.kb/` 定义原子笔记、目录组织、人工维护和纯 AI 导航规则；纯 AI 目录导航命名为 RAG V2，当前不进入运行时实现。


## 读取顺序

1. 阅读本文件确认运行索引边界。
2. 维护知识内容时阅读 `.kb/README.md`，再按其索引加载相关规范。
3. 只执行人工目录导航时阅读 `.kb/03-检索工作流.md`。
4. 设计或实现在线 RAG 时阅读 `docs/project/02-架构设计/RAG知识库.md` 和 assistant README，不把 `.kb/03-检索工作流.md` 当作 V1 算法。


## RAG V1 索引边界

只纳入 `数学建模/` 下整理后的内容 Markdown，并排除所有层级的 `README.md`。

明确排除：

- `.kb/`、`.claude/` 和本文件。
- `data/` 原始抓取内容。
- `数模评审参考资料/` PDF 原始材料。
- `scripts/`、`.git/` 和未声明的新顶层目录。

新增目录不会自动进入索引。索引任务必须先生成确定性文件清单，再加载正文。


## 维护规则

- 知识内容仍遵守 `.kb/` 中的原子笔记、frontmatter、目录和 README 同步规则。
- 修改内容时同步更新摘要和所在目录 README。
- 不在知识文件中保存密钥、Token、用户对话、论文提交或其他敏感业务数据。
- 不用索引状态覆盖 Markdown 内容；索引失败时修复派生索引或源内容问题。
- 不执行大规模重分类、批量重命名或删除，除非当前任务明确要求并完成影响核验。


## 版本与降级

在线索引使用独立 `ragIndexVersion`，并记录内容、Embedding 模型和切分策略版本。检索失败时 assistant 记录不含正文的错误并降级为无 RAG 回答；知识目录本身不承担在线可用性保证。


## 旧入口

`.claude/CLAUDE.md` 是迁移前的工具专属入口，其有效内容已由本文件和 `.kb/` 规范覆盖。S4-04 负责删除旧文件或将其缩减为无冲突迁移提示；在此之前以本文件为准。
