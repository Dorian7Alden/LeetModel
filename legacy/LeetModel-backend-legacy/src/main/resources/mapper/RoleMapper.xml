<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.senior.leetmodelbackend.mapper.RoleMapper">

    <insert id="insertRole" parameterType="com.senior.leetmodelbackend.pojo.entity.Role">
        insert into role (name, code, description, status, create_time, update_time)
        values (#{name}, #{code}, #{description}, #{status}, now(), now())
    </insert>

    <update id="updateRole">
        update role
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="code != null and code != ''">code = #{code},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            update_time = now(),
        </set>
        where role_id = #{roleId}
    </update>

    <select id="getPermissionsByRoleId" resultType="com.senior.leetmodelbackend.pojo.entity.Permission">
        select p.* from permission p
        inner join role_permission rp on p.permission_id = rp.permission_id
        where rp.role_id = #{roleId}
        order by p.permission_id
    </select>

</mapper>
