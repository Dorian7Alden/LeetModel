<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.senior.leetmodelbackend.mapper.PermissionMapper">

    <insert id="insertPermission" parameterType="com.senior.leetmodelbackend.pojo.entity.Permission">
        insert into permission (name, code, description, status, create_time, update_time)
        values (#{name}, #{code}, #{description}, #{status}, now(), now())
    </insert>

    <update id="updatePermission">
        update permission
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="code != null and code != ''">code = #{code},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            update_time = now(),
        </set>
        where permission_id = #{permissionId}
    </update>

</mapper>
