## 归属与权限

论文提交归属于参赛队伍，同时记录实际上传用户。只有具备当前队伍论文上传权限的成员可以初始化上传。

submission-service 负责提交与上传状态，team-service 负责队伍、成员和角色事实。submission-service 通过服务契约校验，不直接读取 team-service 数据库。

权限在初始化上传时校验。队长或成员关系发生变化后，尚未使用的上传授权必须失效，避免旧权限继续提交论文。
