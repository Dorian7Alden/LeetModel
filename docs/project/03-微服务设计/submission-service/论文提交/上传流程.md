## 上传流程

```mermaid
flowchart LR
    A[初始化上传] --> B[校验题目与队伍权限]
    B --> C[分片上传 PDF]
    C --> D[确认分片完整]
    D --> E[合并并保存文件]
    E --> F[创建提交记录]
    F --> G[触发 AI 评审]
```

上传初始化阶段完成业务权限和队伍互斥校验。文件分片完成后，系统检查完整性并生成提交记录，再向 ai-review-service 发起评审任务。

具体接口、字段、分片状态和持久化方式以当前代码与 Flyway SQL 为准。
