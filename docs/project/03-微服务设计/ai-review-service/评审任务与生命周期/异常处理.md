## 异常处理

- PDF 不存在、不可下载或当前工作流无法读取时，评审失败。
- 版本的中间步骤失败时，按照该版本明确的失败或降级规则处理。
- AI 输出不符合当前版本约定时，评审失败。
- 任意 AI 过程输出不是合法 JSON，或不符合所属版本的过程契约时，当前步骤失败。
- 最终 `score` 缺失、无法转换或不在 `[0,100]` 范围时，不保存为成功结果。
- 每次领取、开始、模型调用、校验、完成和失败都写入追加式任务日志。
- 重试在同一公共任务下创建新的执行轮次。旧轮次日志和失败事实只追加、不覆盖，成功结果仍保持一个任务一个版本结果。
