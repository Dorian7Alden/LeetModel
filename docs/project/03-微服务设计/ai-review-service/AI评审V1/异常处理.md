## 异常处理

- PDF 不存在、无法下载、无法打开、无页面或超过 V1 页数限制时，任务失败。
- 页面渲染失败、单图或整体请求超过 V1 安全限制时，任务失败，不降级为文本抽取。
- AI 网关不可用、模型拒绝请求、响应为空或调用未正常结束时，任务失败。
- 模型回答不是单个合法 JSON 对象，或缺少 V1 必需字段时，任务失败。
- `score` 或维度得分无法转换、超出 `[0,100]` 时，不保存成功结果。
- 失败信息只暴露可理解摘要；队伍成员可在同一任务上重试，旧轮次日志不得覆盖。
