## 输入输出

一个 AI 评审任务只处理某次提交对应的一份 PDF。submission-service 拥有提交记录与原始文件，ai-review-service 根据提交标识获取锁定的文件对象路径。

任务创建时必须确定评审版本。任务开始后，本次使用的 PDF 和评审版本不再变化。

每个成功评审都必须输出 `[0,100]` 范围的十进制评分。评分缺失、无法转换或超出范围时，评审不能成功。

评语、维度分、证据和质量标记是版本可选输出，不要求三个版本保持相同结构。
