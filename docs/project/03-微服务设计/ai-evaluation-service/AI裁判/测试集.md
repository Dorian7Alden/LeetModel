## 测试集

测试集是 AI 评价的固定输入，由 1–10 个已有最终提交组成。创建时由 submission-service 解析并校验该提交的队伍、题目和 PDF 可用性，同一提交不能在数据集内重复。

测试用例可以附带简短说明，用于表达该样本希望覆盖的场景。测试集应覆盖不同题目、论文质量和文档复杂度，避免评价结论只适用于单一样本。

数据集在创建时就锁定输入引用和必要快照，不提供编辑接口。需要改变样本时必须创建新数据集，避免历史版本对比口径漂移。

ai-evaluation-service 只保存稳定引用和必要快照，不成为题目与原始 PDF 的数据所有者。
