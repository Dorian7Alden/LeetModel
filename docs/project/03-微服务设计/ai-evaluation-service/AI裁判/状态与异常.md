## 状态与异常

评价任务依次经过等待执行、生成评审结果、计算指标、生成综合得分和完成。可恢复失败进入等待重试，无法继续时标记失败。

- 测试集为空、题目不可读、PDF 不可用或题目与 PDF 不对应时，不创建评价任务。
- 候选评审版本不存在或不可执行时，不开始评价。
- 单次评审失败会保留原失败事实并计入成功率。重试不覆盖原记录。
- 依赖服务暂时不可用属于评价环境故障，不直接计为版本质量问题。
- 裁判模型失败时不生成虚假质量分数。只有综合得分所需指标齐全后，评价才能完成。
