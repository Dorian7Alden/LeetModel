## 系统设计文档目录

当前目录按开发版本进行划分，版本号从 `v1` 开始，按迭代顺序递增。

每个版本必须先有对应的版本计划，再编写该版本下各功能的实现说明。

开发范围必须严格以版本计划为准：
- 不多做
- 不少做
- 不额外扩展计划外内容

在开发具体功能前，建议先阅读前序版本文档，了解版本演进过程，建立对项目的全局认知。

`草稿` 目录为历史设计内容，仅用于回溯与灵感参考，不作为开发依据。随着版本持续迭代，草稿内容会逐步失效，后续将按需清理。

