
## v0.1.0 TODO List



### 本版完成标准

- [ ] 邮箱注册、邮箱登录、主动退出全链路可用。
- [ ] 受保护接口必须通过 JWT 鉴权后访问。
- [ ] 普通用户仅可访问个人数据，管理员可访问全量数据。
- [ ] 同账号重复登录后旧令牌失效，主动退出后当前令牌失效。



### 开发分支说明

- v0.1.0 的系统设计在 design/v0.1.0 分支完成

- v0.1.0 的开发与提交在 dev/v0.1.0 分支完成




### 待 Commit 的 TODO

- [x] 初始化鉴权基础设施（JWT、Redis、邮件服务配置）。
	
	```
	feat(back): 初始化鉴权基础设施配置
	```
	
- [ ] 定义并落地统一响应结构与鉴权错误码。
	
	```
	feat(back): 新增鉴权统一响应与错误码
	```

- [ ] 完成邮箱验证码发送能力（QQ 邮箱）与 Redis 存储（5 分钟过期）。
	
	```
	feat(auth): 新增邮箱验证码发送与缓存
	```

- [ ] 实现用户注册流程（邮箱唯一校验、验证码校验、密码 MD5 存储）。
	
	```
	feat(user): 新增邮箱注册能力
	```

- [ ] 实现用户登录流程（邮箱 + 密码）并签发 JWT。
	
	```
	feat(auth): 新增邮箱登录与令牌签发
	```

- [ ] 实现 JWT 认证过滤与受保护接口放行控制（鉴权接口免 token 校验）。
	
	```
	feat(auth): 新增JWT认证过滤与接口放行规则
	```

- [ ] 实现 RBAC 权限校验（游客、普通用户、管理员）与管理入口授权。
	
	```
	feat(auth): 新增RBAC角色权限控制
	```

- [ ] 实现令牌唯一性与失效机制（重复登录踢出旧令牌、退出登录失效当前令牌）。
	
	```
	feat(auth): 新增令牌唯一性与失效控制
	```

- [ ] 记录关键鉴权审计日志（注册、登录、退出、鉴权失败、权限拒绝）。
	
	```
	feat(auth): 新增鉴权关键审计日志
	```

- [ ] 补充鉴权相关接口与流程文档，并更新版本进度说明。
	
	```
	docs(auth): 补充鉴权接口与流程文档
	```

- [ ] 完成 v0.1.0 版本发布提交。
	
	```
	release(version): 发布 v0.1.0
	```

