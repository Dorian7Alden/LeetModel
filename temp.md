## 临时备忘录 - 记录一些未经整理的灵感/idea



### 临时待整理的内容


Flyway 框架





项目期望描述：

我希望的最终样子，大概是这样的：
- 用户完成登录，3个人组建一个队伍，可以为小组成员分配角色。这里可以简化操作，主要是涉及到权限管理RBAC的使用，一个队伍人数不超过3个人，1个人也可以。队伍可以解散，有记录留存。
- 题目管理，用户可以自己选择一个题目来进行完成，查看相关的题目内容，做题目的筛选，匹配合适的题目（可以附加一个AI筛选、AI推荐题目的功能）
- 用户可以提交题目内容，选择好一个题目，然后提交pdf，只支持提交pdf论文，暂时只评审pdf论文，其他的都不管了。AI 后台完成评审任务，并给出最终的分数，调整建议。这个题目提交评审完后，更新这个题目的排行榜，提交记录 ，要注意数据的一致性，因为有多次提交，而且队伍可能会解散，是以前的提交记录，但是也算数。
- 各种记录查看，日志
- 后台管理端，做数据统计，数据分析用




### 数据库迁移（Flyway）

每个服务模块自行管理 DDL，使用 Flyway 版本化迁移：

```
LeetModel-user/src/main/resources/db/migration/
├── V1__create_user_table.sql
├── V2__create_role_permission_table.sql
└── ...

LeetModel-team/src/main/resources/db/migration/
├── V1__create_team_table.sql
└── ...
```

- 每个服务连接独立的数据库（user_db、team_db、problem_db …）
- Flyway 随服务启动自动执行迁移，保持数据库与代码版本一致
- 不再需要集中管理的 `sqls` 目录


如何理解日志系统。日志系统主要是在干嘛？

沉淀做系统分析的过程说明。架构拆分。

感受：我感觉对于系统架构的设计，微服务其实跟普通的单体架构是类似的，微服务其实就是每个模块，单体架构下，每个模块被内聚到了同一个项目中，而微服务就是把每个模块单独拆分出去成一个独立的项目管理。微服务拆分之后项目架构更加清晰了的感觉，单体架构下，各个模块之间的边界感没那么强，职责边界比较模糊，我感觉以后对于系统设计就得直接按照微服务的架构来拆分，即使是单体应用，设计思想微服务架构更加清晰。


对于原子笔记的定义，后续快速让 AI 积累增量个人知识库。

减少上下文干扰：不明确的内容就不要说，防止后续明确表述的时候与之冲突、干扰。实际上就是减少括号的使用，括号常常用来备注辅助说明，但是一个非常明确的文档，就不应该出现括号这种辅助说明，最多也就通过一个角标对其特别的辅助说明，但这一定是补充没有直接说明的相关的内容，一般说明都直接写在正文中了，否则就是不必要的内容。干脆直接禁止使用括号进行辅助说明，能好好用正文描述就用正文描述。

评审要根据不同的赛事要求来进行评审。美赛、国赛。并在题目中明确说明是什么赛事的评审要求，给用户看的评审要求，以及给AI看的评审要求不一样。

对于专业的软件工程开发人员来说，根本不需要通过 AI 生成 TODO 文档，自己就知道该做什么，TODO 反而限制了。

我有一个缺点，有一个错觉：希望把一个东西整理之后，对其进行标注，防止下次重复整理，对原始内容留一个存档，而且原始内容可能更改，所以得知道哪些没有被整理过，这十分麻烦，导致我迟迟不开始整理内容，生怕内容更新了。直接不允许更改原始内容，只能增量文档这种原则？搞一个版本号，增量管理，不动原始内容，这样还更加有成就感，更多的文档。（git管理版本不好用，这里的不好用体现在对于内容版本的不直观，不能快速的对比多个版本，倒不如直接把每个版本罗列出来，最原始的版本管理。git管理代码好，但是管理文档就这样原始的列版本号更便于文档的快速调整，文档的变更太多了，有些参考内容就是得追溯历史版本，用git来做历史版本反倒不直观了。每次发布都令起一个版本，可以在文档命名上添加一个特殊标记，比如：完全舍弃，这样快速的节省文档的阅读成本，有些内容被完全丢弃了，但是可以体现出写文档的探索过程，试错，有些文档过期了，但是也有一些存在的参考价值）


制定一个动态对文件管理的规则。文件分文件夹、编号的规则场景说明。动态整理、管理文件。




文档规范：
我不希望通过括号来进行辅助说明，应该把所有括号拆了，该放大正文描述就放到正文描述，标题保持简洁




"一句话描述项目"太死板 — 方法论应该讲原则，不是定规矩

文档高内聚低耦合，防止过度依赖。写文档跟写代码是一样的。


连接池为什么用这个不用那个？

能用 mermaid 画架构图的，绝对不用纯文本，纯文本太消耗 token 了，尤其是用文本来排版。
