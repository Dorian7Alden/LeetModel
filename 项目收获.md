## 做该项目的过程中的收获



- gpt 开发时，没说让做的，基本就不做。写代码不写注释。写了注释不写参数类型
- 测试代码每个模块得分开，不能集合在一起测
- 分析代码架构是否合理的时候，先分析当前的做法有没有它的道理，如果有道理就不改，没道理才改。
- 异常处理的代码全都高内聚，放在一个文件夹，而不是把文件分类存储。不是业务代码。
- exception 目录内聚，但是 result 文件响应体不内聚
- 开发的时候，忘记从 design 分支切到dev分支了。
- 测试代码的文件夹怎么生成。整个创建文件夹的规则
- 自顶而下的思想。不喜欢把所有内容都堆在一个文件里面。一个文件不能有过多的内容
- 遇到的问题：ide缓存不一致，导致没有找到包
- ide在application的配置中推荐键名，但是不能改！改了会出错，原本是小写的键，推荐改成大写，改了就错了
- 保持全局属性干净，内聚属性
- 写代码的时候，同时检查异常兜底
- controller 的代码内容范式：校验参数，调用服务，返回结果
- 开始一个后端项目，从头到尾需要做的事
- 减少硬编码的内容。例如：发送邮箱的模板
- ai ide 每个 ssesion 不要随意切换模式，agent模式，plan模式，ask模式，防止上下文丢失，或者记录丢失。一般一个session只一个任务
- plan 模式比直接 agent 好用，plan 模式下有提问对话，确定一些位置的问题
- copilot bug：几乎每次从plan模式且回到agent模式，都会显示还是plan模式，无法执行
- 有很多的配置参数，比如redis的命名空间，不要散落的使用，最好写进properties，聚合起来
- 提取出开发的全局观，例如：写代码注释
- 写 TODO 的时候顺带写上验收标准
- 邮箱发送时嵌入图片无法加载：原理是为什么？



![image-20260425204912582](https://gitee.com/kualk/pic-go/raw/master/imgs/image-20260425204912582.png)



### 遇到的问题



### 经验



### 良好的实践



