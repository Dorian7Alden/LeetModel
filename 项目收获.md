## 做该项目的过程中的收获



### 踩坑



项目依赖包找不到：不知道为什么找不到，自定义了仓库的位置，中间让 AI 直接把 pom 文件修复了一下，然后重新下载，就行了。

环境变量读取：重新修改了环境变量，需要重启 idea ，重新加载环境变量缓存






Git 终端字体粗细不统一：右键点击窗口的 options 进行更改字体设置

![image-20260502170259789](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260502170259789.png)

使用 git config --global core.quotePath false 支持非 ASCII 码显示，关闭自动编码



windows 字体粗细不统一：（重装系统，选择英文地区）

![image-20260502200619658](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260502200619658.png)

使用 noMeiryoUI3.4.2 开源软件直接一键修改，把系统字体设置为微软雅黑






### 良好的实践

**代码组织**
- 异常处理的代码全部高内聚，放在一个文件夹，而不是把文件分类存储。不是业务代码。
- 自顶而下的思想。不喜欢把所有内容都堆在一个文件里面。一个文件不能有过多的内容
- 保持全局属性干净，内聚属性
- 有很多的配置参数，比如 Redis 的命名空间，不要散落的使用，最好写进 properties，聚合起来
- 减少硬编码的内容。例如：发送邮箱的模板

**Controller 规范**
- controller 的代码内容范式：校验参数，调用服务，返回结果
- 写代码的时候，同时检查异常兜底

**测试**
- 测试代码每个模块得分开，不能集合在一起测

**AI 协作**
- codex 开发时，没说让做的，基本就不做。写代码不写注释。写了注释不写参数类型
- 提取出开发的全局观，例如：写代码注释与 TODO
- 写 TODO 的时候顺带写上验收标准



### 经验

**ClaudeCode 音效类型**

```
{
    "start": ["start1.mp3", "start2.mp3"],
    "submit": ["submit.mp3"],
    "complete": ["complete.mp3"],
    "error": ["error.mp3"],
    "notification": ["notify.mp3"],
    "compact": ["compact.mp3"],
    "stop": ["stop.mp3"]
}
```

**id 类型**

- 实体类的 id 直接用 Integer 类型，防止 int 跟 long 类型之间的转化，或者 Integer 转 long  

**AI 指令执行**

- AI 执行指令询问用户的时候，需要给出一个简单的指令说明，这个指令是在干嘛

**AI 开发项目**

- 第一步不是直接写代码。而是调研技术。

**AI 生成数据库测试数据**

- 可是使用豆包进行生成。豆包可以选择本地文件夹作为仓库，方便读取项目的相关信息，直接上传项目。弊端就是：代码公开。

**AI IDE 使用**

- plan 模式比直接 agent 好用。plan 模式下有提问对话，可以确定一些位置的问题

**架构决策**

- 分析代码架构是否合理的时候，先分析当前的做法有没有它的道理。如果有道理就不改，没道理才改。
- exception 目录内聚，但是 result 文件响应体不内聚

**开发流程**
- 开始一个后端项目，从头到尾需要做的事（待整理）



**RBAC 的资源管控**

并不是所有的资源都需要添加权限码来进行管控，那样只会增加复杂度。RBAC 管控的是需要保护的资源，公共的、静态的资源不用保护。公共的资源做访问频率限制。

- **绝对不要试图把所有资源都纳入 RBAC 管控。**
- 确立一个公共资源白名单，明确哪些路径是“免鉴权”的，由拦截器放行。
- 只对真正有访问控制需求的功能定义权限，采用“菜单级为主、操作级按需”的粒度。
- 保持“认证”和“授权”分离，很多需要登录但不需要区分角色的功能使用 `permitAll` 即可。
- 对于“我要管但又不能阻”的需求（如限流、日志），用中间件独立处理，不要变成权限码。

**一句口诀：按需管控，公共放行，粗粒度别心疼，细粒度看场景。** 这样的 RBAC 才能清晰、高效且不臃肿，真正“管住该管的，放掉该放的”。






### 遇到的问题

- 开发的时候，忘记从 design 分支切到 dev 分支了，导致了合并冲突
- IDE 缓存不一致，导致没有找到包
- IDE 在 application 的配置中推荐键名，但是不能改。改了会出错 — 原本是小写的键，推荐改成大写，改了就错了
- copilot bug：几乎每次从 plan 模式切回 agent 模式，都会显示还是 plan 模式，无法执行



- claude code 在使用 plan 模式的时候，一个任务的 plan 可能很长，每次都得网上翻到最上面从上往下 review ，翻上去很麻烦，尤其是当 plan 需要调整时。



![image-20260503003500871](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260503003500871.png)

这是由于远端服务器的 redis 服务挂了，但是我记得这些接口没有走 redis 啊，是用的 mysql 。不知为何。



Deepseek 把不分包理解为单一文件了。

![image-20260503015650814](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260503015650814.png)



**并发问题**

![image-20260503082918979](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260503082918979.png)

​	

**AI 把注释删了**

![image-20260503084420423](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260503084420423.png)



- 网络请求

![image-20260504004346327](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260504004346327.png)

````
这段日志记录了一次用户登录的完整流程。下面从**业务流程、潜在问题、安全与优化建议**三个角度进行分析。

---

### 1. 业务流程解析
- **用户查询**  
  `UserService.getUserByEmail` 用邮箱 `admin@email.com` 查询用户，执行 SQL：
  ```sql
  select * from user where email = 'admin@email.com'
  ```
  返回用户 `admin`（ID=1002），状态正常（status=1），密码为密文存储。

- **角色查询**  
  根据 userId=1002 联表查询角色，得到角色 `SUPER_ADMIN`（系统管理员）。

- **登录成功**  
  `AuthController` 记录登录成功，并生成 JWT Token（有效期约 3 天），随后将完整 Token 打印到日志中。

---

### 2. 潜在问题

#### 2.1 事务缺失
日志出现两次：
```
Creating a new SqlSession
SqlSession [...] was not registered for synchronization because synchronization is not active
```
说明 **两次数据库操作是在独立的 SqlSession 中执行的，没有纳入 Spring 事务管理**。  
- 一个请求内，本该是“查询用户→匹配密码→获取角色”的原子操作，现被拆成两个独立会话，无法保证一致性。  
- 原因通常是 `UserService` 或上层方法未添加 `@Transactional` 注解，或者数据源没有配置事务管理器。

#### 2.2 敏感信息泄露到日志
- **密码密文**  
  返回结果列中虽然没有显式打印，但 MyBatis 的 `<== Row` 直接将数据库整行打印出来，包含密码哈希 `bbe6a8cb...`。虽然经过哈希，但密码哈希仍属敏感信息，**不应出现在日志**（尤其在开发/测试环境可能与生产共用日志平台）。

- **JWT Token 完整打印**  
  ```
  生成用户 admin@email.com 的登录 token: eyJhbGciOiJIUzI1NiJ9...
  ```
  **将完整的 Token（含签名）打到日志是严重安全隐患**。一旦日志被泄露或未授权访问，任何人都能直接使用该 Token 冒充该用户进行操作，直到 Token 过期。

- **Payload 信息过细**  
  Token 中包含了 `userId`、`email`、`username`、`role` 等信息，虽然不是直接的漏洞，但配合日志泄露会暴露更多用户数据。

#### 2.3 日志级别不合理
- `UserService` 和 `AuthController` 的日志都是 `INFO` 级别，却输出了请求参数、完整 Token 等。  
- 生产环境应该控制日志粒度，敏感业务流程应使用 `DEBUG` 级别，且必须过滤敏感字段。

---

### 3. 优化建议

| 问题 | 建议措施 |
|------|----------|
| **事务管理** | 在 Service 层方法（如 `loadUserByUsername` 或自定义登录逻辑）添加 `@Transactional`，确保用户查询和角色查询在同一事务中。 |
| **日志脱敏** | 1. 配置 MyBatis 日志只打印 SQL 语句和参数，不打印结果集（关闭 `TRACE` 级别的结果集输出或配置 `log-impl` 限制）；<br>2. 对 JWT Token、密码等字段在日志中脱敏，例如只打印前缀或完全隐藏。 |
| **Token 不记录** | **绝对禁止** 在日志中输出完整 Token。如果需要审计登录行为，可记录用户 ID、时间、IP 等，不携带认证凭证。 |
| **Token 内容精简** | Token 中建议只保留必要的声明（如 `userId`、`role`），避免放入过多个人信息，降低泄露危害。 |
| **日志级别调整** | 将敏感操作的日志级别降为 `DEBUG`，生产环境默认使用 `INFO` 并屏蔽请求/响应体。 |

---

**总结**：  
这是一次正常的超级管理员登录，但日志暴露了**无事务控制**、**密码哈希和完整 Token 打印**等多个安全和规范性风险。需要尽快从代码层面修复日志输出，并加上事务注解。
````



- 服务器内存快满了，数据库查询变慢，导致原本可以正常运行的接口，查询失败。刚才还能执行的接口，现在就无法正确返回数据

![image-20260504004944956](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260504004944956.png)

- 通过服务器分析内存占用情况可以知道，redis 数据库占用了大量的内存

![image-20260504005307825](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260504005307825.png)

- 解决过程

![image-20260504010603736](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260504010603736.png)

- 最终，在 AI 的排查下，不是数据占用的问题，也基本不可能是数据占用的问题。根本没什么数据。
- 结果：不是 Redis 内存异常，是 Redis 被“磁盘问题锁死了”。Redis 持久化（RDB）出问题，内存无法释放

![image-20260504012428181](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260504012428181.png)

- 解决过程：

![image-20260504012704604](https://gitee.com/Seniorsy/pic-go/raw/master/typora/image-20260504012704604.png)




### 待探索

- 测试代码的文件夹怎么生成？整个创建文件夹的规则是什么？
- 邮箱发送时嵌入图片无法加载：原理是为什么？
- 给 Claude 添加一个弹窗机制，有消息时，通过弹窗进行提醒



































