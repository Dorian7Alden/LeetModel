server:
  port: 8091

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lm_ai_evaluation?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root
  flyway:
    enabled: true
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}

evaluation:
  recovery:
    delay-ms: 60000

jwt:
  secret-key: leetmodel-dev-secret-key-2026
  timeout: 604800

springdoc:
  api-docs:
    path: /api/evaluations/v3/api-docs
