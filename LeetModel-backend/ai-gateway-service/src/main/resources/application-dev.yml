server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lm_ai_gateway?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root
  flyway:
    enabled: true
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDR:localhost:8848}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /internal/ai/v3/api-docs
