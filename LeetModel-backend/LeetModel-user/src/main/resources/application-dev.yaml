# ==================== LeetModel-user 开发环境配置 ====================

server:
  port: 8081

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848   # Nacos 2.3.2 默认端口

# 暴露 Actuator 端点，便于健康检查
management:
  endpoints:
    web:
      exposure:
        include: health,info
