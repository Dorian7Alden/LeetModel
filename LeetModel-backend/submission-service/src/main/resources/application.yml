spring:
  application:
    name: submission-service
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 21MB
minio:
  max-file-size: 20971520
server:
  shutdown: graceful
