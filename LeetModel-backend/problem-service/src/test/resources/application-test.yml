spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lm_problem_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        enabled: false

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

leetmodel:
  messaging:
    enabled: true
    relay:
      enabled: false
  cache:
    enabled: false
