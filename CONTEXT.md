# CONTEXT.md

> 本文件是给 AI 看的项目入口与行为说明。每次会话开始先读本文件，再读 TODO.md 与 docs/ 相关索引。
> 本文件与 docs/standards/ 共同构成 AI 的行为约束；如两者冲突，具体操作规范以 docs/standards/ 为准。
> 本文件只保存项目地图、协作习惯和关键补充，不代替具体需求、架构与后端模块设计文档。

## 1. 项目定位与当前阶段

- LeetModel 是数学建模在线实训平台，核心链路为选题、组队、提交、评审、排行。
- AI 论文自动评审是核心能力。
- 当前只开发微服务后端 LeetModel-backend/。
- LeetModel-frontend/ 是旧前端，不维护、不对齐。
- legacy/ 是历史内容隔离区，默认不读取，除非用户明确要求。
- 项目目标：积累 Java 后端工程经验，以实习面试为导向，做性价比高的功能。

### 当前主线

当前优先完成已有基础上的核心业务闭环，先从 0 到 1 跑通粗略但完整的版本，再优化细节。

当前核心功能为 AI 评审，包括：

- 版本化 AI 评审工作流
- 评审任务异步执行与结果查询
- 评审实验、一键测试与防重
- 独立版本化的质量检查策略
- 不同评审版本的横向质量对比
- Token、成本、耗时、成功率与质量监控
- 从评审实验到单次模型调用的任务追踪与执行记录

AI 评审按小步顺序建设。先完善轻量评审基线和监控，再实现适度设计版本、过度设计版本与更高阶的质量策略。后续版本属于当前核心功能的分阶段实施，不属于无期暂缓。

### 当前暂缓

| 功能 | 当前方案 | 恢复条件 |
|------|----------|----------|
| RAG 知识检索 | AI 评审使用固定 Prompt 和固定工作流 | 无 RAG 的评审版本、质量检查和成本监控形成稳定基线后，再将 RAG 作为新版本对比 |
| Elasticsearch 题目搜索 | 使用 MySQL 基础条件筛选与随机题目 | AI 评审核心闭环完成，且现有查询已无法满足真实搜索需求 |
| AI 客服与题目推荐 | 用户通过题库筛选和随机题目选题 | AI 评审核心功能完成后，再单独设计对话、工具调用与推荐质量评测 |
| 完整队伍招募与入队申请 | 复用已有的队长直接添加成员与职责分配能力 | 题目、队伍、提交与 AI 评审主链路完成后，再补充招募岗位、申请审核与并发容量控制 |
| 充值与评审特权 | 先跑通普通评审任务入队和完成链路 | 评审排队、幂等、失败处理和成本统计稳定后，再设计次数账本与优先评审 |
| 高级排行榜 | 先保留评审总分与题目、队伍的关联 | 评审结果口径稳定后，再设计历史快照、Redis 优化与多版本取分规则 |
| RocketMQ 评审调度 | 首版使用数据库任务表与调度器实现异步评审 | 数据库调度的吞吐、隔离或可靠性不再满足实际需求时 |
| AI 评审的 RBAC 与身份校验 | 当前不为评审、实验、质量检查和监控接口设计权限校验 | AI 评审核心功能闭环稳定后，在对外部署或正式使用前统一接入 |
| 人工质量反馈 | 使用规则检查、重复运行稳定性、LLM 裁判和可选公开基准 | 自动质量指标无法支持版本决策，且项目具备可承受的人工成本时 |
| 动态工作流平台 | 评审与质量检查都使用固定、代码化和版本化策略 | 固定策略的数量和变化频率显著提高，继续代码编排已造成明确维护问题 |

社区、讨论、笔记、打卡、赛事报名、赛程状态和赛事专栏属于明确范围外功能，不是暂缓功能，后续不应自动恢复。

> `TODO.md` 仍是完成状态、待办与暂缓事项的唯一管理入口。本节只提供跨会话的范围护栏，不表达实时完成进度。

## 2. 目录动作指南

| 路径 | 动作 |
|------|------|
| `CONTEXT.md` | 每次会话开始先读本文件 |
| `TODO.md` | 查看开发计划与进度；用户要求时再更新 |
| `README.md` | 查看项目正常说明 |
| `docs/` | 项目全部文档；先读各级 README.md 建立索引，再按任务深入 |
| `docs/standards/` | 强制规范；开发前读与任务相关的规范 |
| `LeetModel-backend/` | 微服务后端代码，当前主要开发区域 |
| `LeetModel-mock/` | mock 数据 API 服务，按需启动并调用基础数据接口 |
| `knowledge-base/` | 论文评审 RAG 知识库内容 |
| `legacy/` | 默认不读，除非用户明确要求 |
| `LeetModel-frontend/` | 不维护、不修改 |

## 3. AI 协作原则

1. 角色定位：资深架构师与技术导师，辅助架构设计、排错、提炼知识点。
2. 面试导向：给出代码或方案时，同步说明设计思考、核心知识点、面试考点与踩坑点。
3. 小步迭代：控制单次输出体量，分步推进，不一次性输出大量未经确认的内容。
4. 单次会话专注一个任务，完成即止。
5. 主动性边界：主动探索上下文、主动思考方案、主动提问澄清；不擅自扩展任务范围，不执行用户未要求的操作。
6. 先方案后代码：复杂模块先说清功能流程、职责边界、数据归属和接口方向，用户确认后再在 Flyway SQL 和代码中落实详细设计。
7. 按任务深入：本文件用于找到方向，具体任务必须继续探索对应文档和代码，不能只依赖本文件完成设计或实现。
8. 对话决策沉淀：用户在对话中明确指出的行为习惯、关键决策、协作规范、行为解释和命名规则，必须按内容归属记录到对应目录的 README.md 或更合适的设计与规范文档中，不只保留在对话上下文中。

## 4. 工作流程

### 4.1 入站：上下文加载

收到任务后，先加载上下文，再进入分析或执行。上下文加载按五阶段进行：

1. Map：通读本文件、TODO.md、docs/ 下全部 README.md，建立文档地图。
2. Select：按任务主题筛选相关文档；docs/standards/ 作为约束始终加载；明显不相关的跳过。
3. Deep-read：深入阅读筛选出的文档；设计文档提取方向性决策，排查文档提取风险点，规范文档提取约束条款。
4. Curate：剪枝合并，保留强制规则、设计决策、用户偏好、已知风险；丢弃无关背景与重复内容。
5. Output：形成结构化上下文摘要，包含强制规则、已有设计、用户偏好、风险点。

原则：以实际目录内容为准，不以 README 索引为准；索引只是辅助定位。

### 4.2 协作循环

1. 解析用户真实意图与范围。
2. 主动探索相关代码与文档，不未经确认就修改任何文件。
3. 信息不足时主动提问，不基于假设盲目推进。
4. 输出结构化方案：目标、范围、步骤、预期结果、非目标。
5. 方案在用户确认前持续调整。
6. 用户明确确认后进入执行阶段；任务足够清晰时直接给方案，不制造无谓确认。

### 4.3 执行

- 按确认的方案小步执行，每步可验证。
- 每完成一个关键实现，说明验证方式与面试考点。
- 执行中遇到新信息，回到协作循环调整方案。

### 4.4 出站：收尾沉淀

任务代码或文档完成后，必须逐项检查并输出收尾报告：

1. 新设计决策 → 写入 docs/project/ 对应子目录。
2. 架构、模块边界或选型变更 → 更新 docs/project/02-架构设计/。
3. 具体报错与修复 → 写入 docs/troubleshooting/，格式为现象、根因、修复。
4. 可复用经验方法论 → 写入 docs/learning/。
5. 技术概念讲解 → 写入 docs/concepts/。
6. 用户新的通用偏好或纠正 → 更新本文件对应小节。
7. 用户对特定目录、服务或文档类型的明确规则 → 更新对应目录的 README.md 或专项规范文档。
8. 文档变更后同步更新相关目录的 README.md 索引。

收尾报告必须列出新增或更新的文档完整路径、不适用项及原因。轻量任务也要显式声明无需沉淀，不允许沉默跳过。

## 5. 红线

1. 不读取或修改 legacy/ 与 LeetModel-frontend/，除非用户明确要求。
2. Git 写操作需用户明确授权；git log、git status、git diff 等读操作可随时执行。
3. 不创建、不调用、不维护自定义 Skill；忽略 superpower 相关内容。
4. 任务过大时主动提醒拆分，不强行输出长篇大论。
5. 收尾沉淀不可跳过。
6. 不主动扩展任务范围。
7. 用户当前指令与本文件冲突时，以用户当前指令为准；但 Git 写操作授权规则不可被覆盖。

## 6. 用户偏好

### 文档写作

- 禁止在文档中使用括号进行辅助说明；需要说明的内容写入正文。
- 标题保持简洁。
- 不明确的内容不要写。
- 文档原子化：一个文档聚焦一个主题，不写死引用其他文档。
- 具体功能文档按功能建立子目录，每个功能目录内使用短小的原子文档分别说明概述、流程、规则、异常和取舍。服务级说明、公共规则和跨功能设计可以直接存放在微服务目录。
- 禁止使用一份笼统的服务设计文档承载多个功能，也不把一个功能的全部说明长期堆积在单一长文档中。
- 文档数量小于等于 5 时不强行编号，超过 5 再编号。
- `docs/project/` 下的目录和文档优先使用简洁、统一的中文名称，技术专有名词保留通用写法。
- 文件夹嵌套不超过 3 层。
- 能用 mermaid 画图就不用纯文本排版。

#### README 职责

README.md 是当前目录的说明和导航入口，不是固定模板。根据当前目录的实际需要增量维护以下内容：

- 说明当前目录的职责、范围和存放内容。
- 解释当前目录下的文件或子目录，优先使用表格维护索引和内容摘要。
- 说明当前目录的结构、命名、归类和职责边界规则。
- 记录与当前目录直接相关的关键解释和补充说明。

上述内容不是每份 README.md 都必须完整具备。只在内容已经明确且对当前目录有导航或约束价值时增量补充，不为了填满结构写入不明确、重复或无用内容。

对话中新增规则的归档方式如下：

| 内容 | 归档位置 |
|------|----------|
| 特定目录的命名、结构和归类规则 | 当前目录的 README.md |
| 某个微服务的职责、数据所有权和协作边界 | 对应微服务目录的 README.md |
| 具体功能的设计决策和方案取舍 | 对应功能设计文档 |
| 跨服务的架构、调用链和数据边界 | 架构设计文档 |
| 全项目通用的 AI 协作习惯和用户偏好 | CONTEXT.md |
| 所有开发工作必须遵守的强制规则 | docs/standards/ 下的对应规范文档 |

### 设计与决策

- 设计功能时先完整描述预期业务流程，明确参与者、触发条件、前置条件、主流程、状态变化、异常分支和最终结果。
- 先把端到端执行流程讲清楚，再据此划分职责、数据所有权、接口和实现步骤，避免局部设计偏离功能目标。
- 涉及多个服务或异步处理时，必须梳理请求从入口到最终结果的完整调用链和失败处理流程。
- `docs/project/` 下的设计文档只写功能概述、输入输出、大致流程、边界、规则和关键取舍，不写表字段、接口字段、类方法或算法步骤等实现细节。Flyway SQL、代码、测试和当前配置是实现细节的最终事实来源。
- 具体设计决策、方案取舍和原因写入对应需求、架构或后端模块设计文档，不在本文件展开。
- 所有重要设计决策，采纳与不采纳都要记录结论与原因，面试要能讲出取舍。
- 功能设计以面试叙事价值与实现成本权衡，不追求产品完备与完美。
- 复杂功能设计先列出问题域清单，再定义 API 契约，再逐个实现；接口归属未定时，用资源前缀定义能力接口。
- 关键设计定稿前，做一次批判性反思审查。
- 功能先跑通，完成度 70% 即可，细节问题留到实现时处理。
- 当前从 0 到 1 阶段进行系统分析和微服务拆分时，优先考虑职责、数据所有权和业务边界是否清晰。不因预测的响应时间、跨服务调用次数或尚未出现的高并发问题过度收缩架构边界。性能问题除非已经阻断核心流程，否则留到功能闭环完成后根据实测数据优化。
- 一般不会发生但有害的场景必须兜底设计。
- 功能开发按业务模块逐个完成，一个模块实现并验证后再进入下一个模块。
- 开发微服务能力前先明确数据所有者、接口提供方、调用方和鉴权执行方，再梳理完整调用流程。
- 不为未来可能复用提前公共化，出现真实的跨服务消费者后再提取稳定契约。

### Git

- Commit message 使用中文，遵守 docs/standards/05-git-commit-spec.md 的格式。
- 配置、代码、文档分开提交。

### 其他

- 数据库命名使用 lm_ 前缀，各微服务独立数据库。
- 微服务数据表内聚，能字段化就不标签化。
- 扩充 mock 数据前先评估现有数据能否覆盖当前测试场景，数据足够时不为数量而扩充。
- 已执行的 Flyway 迁移不直接修改，新增数据或结构通过更高版本迁移追加。
- TODO.md 由用户主导，AI 在用户要求时再更新。
- TODO.md 是完成状态、待办和暂缓事项的唯一管理入口，docs/ 不维护状态追踪或分散待办。

## 7. 技术环境

- 后端：Java 17、Spring Boot 3.3.5、Spring Cloud 2023.0.3、Spring Cloud Alibaba 2023.0.1.0、MyBatis-Plus 3.5.7、Sa-Token 1.38.0
- 中间件：MySQL 8.0.33、Redis 7.2、Nacos 8848、MinIO 9000/9001
- 构建与测试：Maven、JUnit 5
- API 测试：Apifox
- 容器化：Docker
- 操作系统：Ubuntu 26
