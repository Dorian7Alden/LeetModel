# LeetModel TODO

> 本文件只保存当前及后续可执行任务、依赖与简短的已完成阶段摘要。已经结束阶段的关键决策、边界和验收依据统一归档到 [AI 已完成阶段归档](docs/project/02-架构设计/AI已完成阶段归档.md) 及其链接的原子设计文档，避免 TODO 随历史任务持续膨胀。
>
> 当前已启用托管模式。Agent 按依赖一次只领取一个 S 编号任务卡；同一阶段的多个任务卡在一条阶段分支内串行完成并形成原子提交，整个阶段验收后才非快进合回 `dev` 并清理已合并的本地阶段分支。

## 使用约定

- [ ] 待开始；[~] 进行中；[x] 已完成并验收；[!] 被外部条件阻塞。
- 每次开始前阅读根 `CONTEXT.md`、`README.md`、本文件、相关规范、目标模块 README 和任务卡指向的设计文档。
- 修改公共契约前检查全部生产者和消费者；数据库变更只新增 Flyway 迁移。
- 任务完成至少同步受影响的正式文档并运行目标模块测试；公共模块变化时验证直接消费者。
- 不在配置、日志、测试夹具或文档中保存真实密钥、Relay Token、Prompt、回答正文、知识片段或论文内容。
- 本地 new-api 测试 Token 位于仓库外 `~/Desktop/new-api-test-key.txt`；具体注入、脱敏和模型复核规则见 [AI 网关测试与验收](docs/project/03-微服务设计/ai-gateway-service/22-测试与验收.md)。
- 可以自主创建本地阶段分支、执行任务卡原子 commit 和阶段 merge；未经用户明确授权，不执行 push、force push、rebase、破坏性 reset、改写历史、远端分支操作或删除用户文件。

## 已完成阶段摘要

| 阶段 | 完成摘要 | 长期记录 |
|------|----------|----------|
| D | 正式设计校准完成，统一 new-api/AI 网关职责、RAG V1/V2、评价术语和五种版本标识。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#正式设计校准) |
| S0 | 保留 LangChain4j 0.34.0，确认项目内 Embedding 适配与 Elasticsearch 技术基线。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s0-langchain4j-兼容性) |
| S1 | Chat 唯一生产链切换至 new-api，删除供应商官方直连和隐式运行时回退。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s1-new-api-chat-主链) |
| S2 | 统一业务调用上下文、Token、费用、耗时、失败与调用关联事实。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s2-调用计量与成本) |
| S3 | Embedding 统一经过 AI 网关，RAG_V1 锁定 qwen3.7-text-embedding 与 1024 维。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s3-embedding-统一调用) |
| S4 | 客服向量 RAG V1 完成受控知识选择、稳定索引、召回、审计、降级和回滚。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s4-客服-rag-v1) |
| S5 | AI 网关完成单实例持久化公平调度、P0 保留、幂等、租约、恢复、UNKNOWN 与运维入口。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s5-ai-网关优先级调度) |
| S6 | REVIEW/ASSISTANT 完成功能目录、不可变模型配置快照与无正式业务副作用的通用隔离实验。 | [归档](docs/project/02-架构设计/AI已完成阶段归档.md#s6-ai-功能与版本契约) |

## 当前执行路线

`S7 通用评价平台 → S8 权重与管理端闭环 → S9 RAG V2 设计 → S10 生产版本切换与 REST API 版本化`。

S7 依赖的网关调度和业务 owner 隔离实验已经完成。SUGGESTION 在提供版本目录与通用隔离入口前仍不进入可评价集合。远期任务只有满足各自任务卡的真实触发条件后才可实现。

## 当前及后续细粒度任务卡

### S7：通用 ai-evaluation-service 任务

#### [x] S7-01 设计通用领域模型与旧数据迁移

- 依赖：D-04、S6-04。
- 工作：把现有 dataset、sample、task、run_attempt 映射为 feature + version + dataset，选择兼容扩列或新表。
- 验收：历史论文评审任务不丢失且仍可查询，字段所有权和状态机明确。

S7-01 验收记录（2026-08-29）：确认沿用 `evaluation_dataset`、`evaluation_sample`、`evaluation_task` 与 `evaluation_run_attempt` 四张主表做兼容扩列，不建立第二套领域表和状态机。通用模型按 feature、不可变 datasetVersion、版本化样本 Payload、候选工作流与执行配置快照、逻辑槽位和 attempt 组织；文档明确 REVIEW、ASSISTANT 与暂不准入的 SUGGESTION 映射、字段所有权，以及暂停、取消、恢复和 UNKNOWN 保护的目标状态机。迁移固定为先新增可空字段、确定性回填、兼容读取、验证后收紧，保留全部历史 ID、旧分数字段、状态、callId 和查询入口，不修改 V1、不删除数据，也不把旧 `overallScore` 冒充新版本选择指数。

#### [x] S7-02 定义版本化数据集与样本 Payload

- 依赖：S7-01。
- 工作：REVIEW 引用 submissionId；ASSISTANT 保存问题、标签和可选标准要点；SUGGESTION 在真实实验入口完成后再接入。
- 验收：schema 版本化，不把本地绝对路径或大 PDF 写入评价库。

S7-02 验收记录（2026-08-29）：common-api 新增版本化样本载荷 DTO，评价服务按 feature、sampleType 与 payloadSchemaVersion 组合做白名单校验。REVIEW V1 只接受正整数 submissionId 引用；ASSISTANT V1 接受问题、标签和可选标准要点，并限制问题、数组数量和单项长度。未知字段直接拒绝，因此本地绝对路径、PDF 内容或其他无约束数据不能混入评价库；SUGGESTION 在 owner 隔离契约落地前明确拒绝。确定性测试覆盖两个已准入 schema、路径字段拒绝和未准入功能拒绝。

#### [x] S7-03 建立指标注册表

- 依赖：D-04、S7-02。
- 工作：为网关资源、业务确定性、人工/真值质量指标定义单位、方向、来源、适用功能、missingPolicy 和 metricVersion。
- 验收：Token、费用、延迟和稳定性不能冒充正确性；未知指标不可参与计算。

S7-03 验收记录（2026-08-29）：评价服务建立 `METRIC_SET_V1` 不可变指标注册表，每个指标固定 metricVersion、类别、单位、方向、事实来源、适用功能、missingPolicy 和证据门槛。Token 与费用归 RESOURCE，排队/执行/总耗时归 RUNNING，评审方差/标准差/极差归 STABILITY，结构、检索、来源和格式规则归确定性质量，人工质量必须来自版本化量表与标注。注册表按功能校验适用性，未知编码明确失败；测试证明资源事实不能冒充质量、方向与缺失策略可追溯、跨功能误用和未知指标均被拒绝。

#### [x] S7-04 新增通用评价 Flyway

- 依赖：S7-01、S7-03。
- 工作：保存 feature、候选版本、执行配置、指标口径和快照，为旧任务回填兼容值，建立 task/slot/attempt/callId 索引。
- 验收：空库和已有库都可升级，无破坏性 SQL，历史读取回归通过。

S7-04 验收记录（2026-08-29）：新增 ai-evaluation-service V2 Flyway，在 V1 四表上依次执行可空扩列、REVIEW 确定性回填和通用必填字段收紧。数据集回填稳定版本和样本 schema；样本从 submissionId 生成最小引用 Payload；任务保留 workflowVersion 与全部旧分数字段，补充模型配置、legacy 指标集合及版本快照；运行尝试补充稳定 slotKey、experimentRunId 和执行版本。迁移不删除表或列、不截断数据、不覆盖 overallScore，并建立 feature/dataset、task/slot/attempt、experimentRunId 和 callId 索引。实体已映射新增字段；模块及公共依赖 26 项测试通过。另在本地 MySQL 8.0 临时库实际执行 V1 建表、插入历史四表数据、V2 升级和回填查询，原 ID、状态、workflowVersion、submissionId 与 callId 均保留，验证后已删除临时库。

#### [x] S7-05 实现通用实验 Runner SPI

- 依赖：S6-04、S7-04。
- 工作：定义版本发现、样本校验、执行、结果解析和指标提取接口，避免评价核心堆叠 feature if/else。
- 验收：可注册假 Runner；未知功能明确失败；核心不依赖 review 专有 DTO。

S7-05 验收记录（2026-08-29）：ai-evaluation-service 新增通用 Runner SPI，以 featureCode 为注册键，分别定义版本发现、版本化样本校验、隔离实验执行、通用结果解析和指标提取。执行命令固定 experimentRunId、样本、workflowVersion、modelExecutionConfigVersion、ragIndexVersion 与 P3 优先级；解析结果只依赖通用 AiExperimentResultDTO 和评价侧 outcome，不引用 Review 专用 DTO。注册表在启动时拒绝重复功能，运行时对未知或未启用功能明确失败。假 Runner 测试完整走通五个 SPI 阶段，并覆盖未知功能和重复注册拒绝。

#### [x] S7-06 迁移论文评审 Runner

- 依赖：S6-05、S7-05。
- 工作：适配 submission、review 结果、score 与结构有效性，保留恢复和环境失败重试。
- 验收：旧数据集和比较可用；成功、业务失败、环境失败测试通过。

S7-06 验收记录（2026-08-29）：新增 REVIEW Runner，版本发现与实验执行只调用 AiFeatureDefinitionDTO、AiExperimentRequestDTO 和 AiExperimentResultDTO 通用契约，样本校验固定为 REVIEW_SUBMISSION_V1，结果解析把有效 score 注册为 REVIEW_SCORE，并将身份漂移、结构缺失归为业务 OUTPUT 失败，将依赖不可用归为可恢复 ENVIRONMENT 失败。评价调度核心已改为从 Runner 注册表选择功能，不再引用 ReviewVersionDTO 或评审专用实验 DTO；新数据集写入 feature、不可变版本和最小 Payload，新任务与运行槽位同步写入配置/指标快照、slotKey 和稳定 experimentRunId。历史 V1 实体的空通用字段仍按 REVIEW 兼容读取，原比较、重试和中断恢复路径保留。模块及公共依赖 33 项测试通过，覆盖成功、业务失败、环境失败、旧数据集行为和通用 Runner 契约。

#### [x] S7-07 实现客服 Runner

- 依赖：S6-06、S7-05。
- 工作：调用隔离实验接口，保存必要结果摘要、callId 和索引版本，不创建正式会话。
- 验收：能比较无 RAG 与 RAG V1；不保存不必要的完整敏感回答。

S7-07 验收记录（2026-08-29）：新增 ASSISTANT Runner，通过 owner 的通用版本目录与单轮隔离入口执行 `ASSISTANT_NO_RAG_V1` 和 `ASSISTANT_RAG_V1`。通用数据集创建契约支持 QUESTION / ASSISTANT_QUESTION_V1 Payload，不引用 submission、conversation 或 message；RAG 任务必须锁定非空物理 ragIndexVersion，无 RAG 任务必须保持为空，全部调用固定 P3 和 `MODEL_CFG_ASSISTANT_TEXT_0001`。成功结果保存 callId、模型配置、索引版本、结构指标以及回答 SHA-256/长度摘要，明确 `answerStored=false`，不持久化完整敏感回答。身份或索引漂移归 OUTPUT 失败，依赖失败保留恢复路径。模块及公共依赖 40 项测试通过，覆盖两个客服版本、索引锁定、摘要脱敏、客服数据集无正式业务引用和核心持久化参数。

#### [x] S7-08 扩展重复次数与规模预估

- 依赖：S5-12、S7-05。
- 工作：替换固定 1 至 3 限制；增加最大样本、版本、总槽位、预计调用量/费用和权限校验。
- 验收：创建前可预估；超限拒绝；所有批次固定为 P3。

S7-08 验收记录（2026-08-29）：新增管理员与内部创建前预估接口，统一校验数据集、启用版本、固定模型执行配置、RAG 索引组合和候选去重；按样本数 × 候选版本数 × 重复次数计算总槽位，并按 REVIEW/无 RAG 每槽位一次、RAG 每槽位两次估算调用量。默认限制为 100 个样本、8 个候选、20 次重复、2000 个槽位和 3000 次调用，全部可通过环境配置覆盖；预估返回全部超限原因，创建任务再次执行相同服务端校验。管理端由 `admin` 角色保护，前端在创建前展示规模、P3 和费用完整性并二次确认。由于创建前缺少真实 Token 与可追溯价格快照，费用明确标记 `UNAVAILABLE` 而不虚构金额。后端 61 项相关测试与前端生产构建通过；详细口径见 [批次规模预估](docs/project/03-微服务设计/ai-evaluation-service/通用评价/批次规模预估.md)。

#### [x] S7-09 接入低优先级、幂等和调用关联

- 依赖：S7-08。
- 工作：为 task/slot/attempt 生成稳定上下文和 idempotencyKey，保存 callId，区分业务失败与环境失败。
- 验收：服务重启不重复成功槽位；客服可优先执行；未知上游结果不盲目重试。

S7-09 验收记录（2026-08-29）：评价 taskId、稳定 slotKey、attemptNo、槽位级 experimentRunId 和 attempt 级 idempotencyKey 已贯穿通用契约、REVIEW/ASSISTANT owner 与 AI 网关；V3 Flyway 回填并唯一约束幂等键，成功及可获取的失败均保存 callId。评价和 owner 双重固定 P3，网关策略保持正式客服 P0 优先且为后台队列保留公平份额。同步等待中的调用以原幂等键继续查询，不重复创建上游任务；网关 51213、owner UNKNOWN、评价 UNKNOWN 全链路保留，重启遗留 RUNNING 也转 UNKNOWN，普通重试只接受明确的 ENVIRONMENT/CONFIGURATION，不会盲目重试不确定结果。真实 MySQL 8 已验证 V1→V2→V3 迁移与非空唯一索引；评价/网关 116 项测试及 owner 88 项测试通过。详细规则见 [调用身份与未知结果](docs/project/03-微服务设计/ai-evaluation-service/通用评价/调用身份与未知结果.md)。

#### [x] S7-10 实现暂停、取消和恢复

- 依赖：S7-09。
- 工作：暂停新派发、取消可取消的排队调用、保留运行中结果、恢复剩余槽位，并记录操作者。
- 验收：暂停后无新调用；取消不删历史；重启后可恢复。

S7-10 验收记录（2026-08-29）：任务状态新增 PAUSED/CANCELLED 控制路径，调度领取 SQL 关联任务表并只领取 WAITING/RUNNING 任务的待执行槽位，暂停完成后不再产生新调用；运行中结果继续写入原 attempt，刷新逻辑不会覆盖 PAUSED/CANCELLED。恢复只继续剩余 WAITING 槽位，不重建成功历史；取消以条件更新锁定终态，将未派发槽位置 CANCELLED，按 evaluationTaskId 请求 AI 网关取消仍活动的队列调用，不删除任务、槽位或尝试。admin-service 从登录上下文取得操作者，V4 迁移保存最近动作、操作者和时间；管理页面提供暂停、恢复、取消与审计展示。真实 MySQL 8 已验证评价 V1→V4 与网关 V1→V8 连续迁移；后端 127 项测试和前端生产构建通过。状态与边界见 [调用身份与未知结果](docs/project/03-微服务设计/ai-evaluation-service/通用评价/调用身份与未知结果.md)。

#### [x] S7-11 实现可信原始指标

- 依赖：S7-03、S7-09。
- 工作：聚合成功率、失败分类、Token、费用、排队/执行/总耗时；review 计算均值、方差、标准差、极差和结构有效率。
- 验收：公式有确定性测试；样本不足和缺失 usage 显式处理。

S7-11 摘要（2026-08-29）：已落地版本化原始指标快照、网关调用资源聚合、REVIEW 按样本描述统计和管理端完整性展示；缺失 Token、费用、耗时及样本不足均不按零处理。真实 MySQL 8.0.33 已验证 V1→V5，相关后端 146 项测试与前端构建通过。关键公式、服务边界和验收基线见 [可信原始指标](docs/project/03-微服务设计/ai-evaluation-service/通用评价/可信原始指标.md)。

#### [x] S7-12 实现客服/RAG 可验证指标

- 依赖：S7-07、S7-11。
- 工作：先支持检索命中、来源覆盖、格式规则和人工标注占位；语义正确性只在有标准要点或人工评分时计算。
- 验收：无真值时显示未评价，不从成本或延迟推导质量。

S7-12 摘要（2026-08-29）：隔离客服实验新增不含知识正文的检索条数与来源证据，版本化样本可选标准来源、固定格式规则和标准要点；评价快照聚合检索命中、来源覆盖、规则通过和标准要点文本覆盖，并以证据覆盖数区分已评价、部分评价、未评价和不适用。人工质量评分保持未评价占位，历史缺失检索轨迹不会误算为 0%，自由模型裁判规则会被拒绝。客服及公共依赖 63 项测试（8 个外部 smoke 显式跳过）、评价及公共依赖 56 项测试与前端构建通过；详细口径见 [客服与 RAG 可验证指标](docs/project/03-微服务设计/ai-evaluation-service/通用评价/客服与RAG可验证指标.md)。

#### [x] S7-13 实现口径快照和同口径比较

- 依赖：S7-11、S7-12。
- 工作：保存 metricVersion、参数和数据集版本；比较时校验 feature、dataset、metricVersion 和执行配置。
- 验收：不同口径默认拒绝排名，只允许带不可比标记并排展示。

S7-13 摘要（2026-08-29）：新任务固化 feature、datasetVersion、metricSetVersion、完整指标定义/计算参数和执行配置；新增标准要点指标后口径从 `METRIC_SET_V1` 提升为 `METRIC_SET_V2`，历史 V1 保持可读。比较接口逐项校验功能、数据集版本、指标集、参数快照和模型执行配置，只有同口径且至少两个候选时才排名；否则保留最近顺序并返回不可比原因，页面明确“仅并排展示”。真实 MySQL 8.0.33 已验证 V1→V6，评价及公共依赖 58 项测试和前端构建通过；详见 [口径快照与可比性](docs/project/03-微服务设计/ai-evaluation-service/通用评价/口径快照与可比性.md)。

#### [x] S7-14 完成通用评价迁移回归

- 依赖：S7-06 至 S7-13。
- 工作：覆盖历史读取、新任务、恢复、暂停、取消、比较、限流和费用缺失。
- 验收：全量测试通过，旧接口兼容策略有明确结束条件。

S7-14 摘要（2026-08-29）：回归覆盖历史 legacy 读取、新任务快照、重启恢复、暂停/恢复/取消、同口径比较、批次与网关限流以及费用缺失；页面把 `overallScore` 明确标成“旧口径分”。后端 17 模块共 373 项测试零失败（9 个外部 smoke 显式跳过），前端生产构建和真实 MySQL 8.0.33 V1→V6 连续迁移通过。旧字段/DTO 仅在数据审计、S8/S9 替代、两个发布周期无消费者、历史导出恢复和独立主版本迁移五项门槛全部满足后才能删除；详见 [迁移回归与兼容退出](docs/project/03-微服务设计/ai-evaluation-service/通用评价/迁移回归与兼容退出.md)。

### S8：权重与管理端闭环任务

#### [ ] S8-01 定义归一化配置

- 依赖：S7-03、S7-13。
- 工作：支持越高越好、越低越好、目标区间和不可归一化，参数版本化，缺失值不默认为零。
- 验收：边界、越界、常量样本和缺失值公式测试通过；不使用当前批次 min/max 随意制造排名。

#### [ ] S8-02 实现版本化权重方案

- 依赖：S8-01。
- 工作：由 evaluation 保存 feature、metricVersion、归一化参数、权重、状态、创建人和版本，权重和校验为 100%。
- 验收：停用不影响历史；不兼容指标不能加入；admin 不建权重表。

#### [ ] S8-03 计算并快照版本选择指数

- 依赖：S8-02。
- 工作：任务保存方案快照；保留每项原值、归一化值、权重和贡献值。
- 验收：结果可复算且不覆盖原始指标，名称不再使用准确率或客观质量分。

#### [ ] S8-04 实现不覆盖历史的重新计算

- 依赖：S8-03。
- 工作：另一权重方案产生新的 scoreResultVersion，引用同一原始指标并记录操作者。
- 验收：旧结果不可变，不可比数据拒绝重算。

#### [ ] S8-05 扩展 admin-service 评价代理

- 依赖：S7-14、S8-04。
- 工作：增加功能/版本查询、规模预估、权重、暂停、取消、重试、重算和调用追踪，只做鉴权、校验、转发。
- 验收：所有写操作要求管理员；admin 不直接读取评价或网关数据库。

#### [ ] S8-06 渐进改造现有 EvaluationPage

- 依赖：S8-05。
- 工作：增加功能选择、版本下拉、配置摘要、重复次数、预估调用/费用、权重、进度和控制；移除任意版本文本输入。
- 验收：旧 review 任务仍可查看；高成本任务二次确认；刷新后状态正确。

#### [ ] S8-07 增加原始指标与版本比较视图

- 依赖：S8-06。
- 工作：展示 Token、费用、排队/执行耗时、稳定性、质量依据、权重贡献、口径版本和 callId。
- 验收：缺失/不可比明显；可追踪单次失败；不把选择指数写成准确率。

#### [ ] S8-08 完成管理端端到端验收

- 依赖：S8-07。
- 工作：分别运行小型 REVIEW 和 ASSISTANT 实验，验证暂停、恢复、重试、权重重算和调用追踪。
- 验收：权限、状态、数据一致性通过，批量实验运行时客服仍获得高优先级。

### S9：RAG V2 设计任务，仅设计

#### [ ] S9-01 定义轻量知识目录 manifest

- 依赖：S4-04。
- 工作：只包含目录、文档名、tags、summary、版本和路径白名单，实测完整 manifest Token。
- 验收：不含全文，缺少 summary 有明确处理，不允许模型构造任意路径。

#### [ ] S9-02 设计两阶段 AI 选文流程

- 依赖：S9-01。
- 工作：定义选择 Prompt、最大候选数、受限文档加载、二次裁剪、超时、无候选降级和与向量召回组合点。
- 验收：流程有清晰输入输出和安全边界，不产生运行时代码。

#### [ ] S9-03 设计 V1/V2 固定对比实验

- 依赖：S9-02。
- 工作：定义问题集、召回相关性、答案支持率、延迟、Token、费用和人工标注需求。
- 验收：无人工/真值不宣称准确率，实验能锁定索引版本与目录版本。

#### [ ] S9-04 记录 V2 实施门槛

- 依赖：S9-03。
- 工作：只有 V1 基线完成且对比实验证明增益后才评估实现；在线文件管理和独立 RAG 服务仍排除。
- 验收：正式 RAG 文档有独立 V2 章节，当前 TODO 不含未授权的 V2 代码实现。

### S10：生产工作流动态切换与 REST API 版本任务，远期

#### [ ] S10-01 设计生产版本配置所有权

- 依赖：S6-06、S8-08。
- 工作：业务服务拥有版本和生效配置，admin 只下发命令；定义默认版本、配置版本、操作者、审批和审计。
- 验收：评价服务不直接改生产业务库；工作流切换与 new-api 渠道/模型切换边界清楚。

#### [ ] S10-02 设计安全激活、在途快照和回滚

- 依赖：S10-01。
- 工作：定义预检查、原子激活、缓存失效、旧版本保留、健康观察和一键回滚。
- 验收：在途业务任务使用启动时快照，不会在一次任务中混用两个版本；失败无半生效状态。

#### [ ] S10-03 实现业务服务版本配置与审计

- 依赖：S10-02。
- 工作：先选择一个真实 AI 功能实现版本列表、当前生效项、条件更新和历史审计，再抽取稳定共性。
- 验收：并发切换只有一个成功；旧版本可回滚；没有为了未来场景提前建立中央版本数据库。

#### [ ] S10-04 实现管理端生产版本切换

- 依赖：S10-03。
- 工作：增加强鉴权、版本差异摘要、影响范围、二次确认、切换原因、结果和回滚入口。
- 验收：普通用户不可操作；全部变更可追溯；页面明确区分实验候选版本与生产生效版本。

#### [ ] S10-05 独立评估 REST /v1、/v2 版本化

- 依赖：实际出现外部 API 不兼容变更。
- 工作：盘点 Gateway 路由、Controller、Feign、OpenAPI 和前端迁移，定义并行支持、弃用和下线周期。
- 验收：不使用 REST 版本代替 workflowVersion；没有真实不兼容需求时保持未开始。

## 远期触发条件

RAG V2、生产版本切换和 REST API 版本化已经分别拆为 S9、S10，但只有满足对应依赖和真实需求后才可领取。以下能力暂不拆实现任务：

- 出现第二个真实知识库消费者、在线知识管理、独立扩缩容或权限隔离需求后，评估独立 RAG 服务。
- 单调度器成为可测瓶颈或必须部署多个调度实例后，评估分布式 AI 调度。
- new-api 无法满足渠道治理且出现明确额外业务需求后，评估 LeetModel 多账号资源池。
- 指标具备可信真值且版本选择指数稳定运行后，才讨论自动推荐生产版本；不得自动激活。

## 执行规则

1. 每次只选择一个 D 或 S 编号的任务卡，不直接领取整个阶段。
2. 默认由用户确认任务范围；托管模式下 Agent 按依赖自动领取并在完成后报告代码、测试、文档和未解决风险。
3. 任务依赖未满足时不得用临时硬编码绕过，应明确标记阻塞。
4. 新发现的问题若不阻断当前闭环，只记录到后续任务，不扩大当前任务。
5. 已完成阶段只在本文件保留摘要；关键决策与验收依据归档到对应 docs 文档。当前按 S7 及后续未完成任务的依赖顺序推进。
