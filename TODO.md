# LeetModel TODO

> 本文件只管理当前一轮任务。这里不是完整功能清单、长期路线图或 idea 池。

## 管理原则

- 同一时间只维护一轮任务，只允许一个明确目标。
- 当前任务必须在独立分支完成，不直接在 `dev` 分支工作。
- 本轮是分析与设计任务，不进入功能编码；设计中的候选结论在用户确认前不得写成最终事实。
- 设计按问题逐点推进，允许反复提问、学习、查阅资料、比较方案和修订文档，不要求一次性产出完整方案。
- 由用户判断设计深度、思考量和完成时机。Agent 不得因清单已经覆盖常见模块就自行宣布本轮完成。
- Agent 可以围绕当前设计点提出少量简单建议，帮助补充遗漏或提供灵感；建议默认是候选项，是否采纳和是否继续深入由用户决定。
- 与 AI 系统设计无关的功能、重构和长期路线不进入本轮。

## 当前任务

> 状态：已主动终止，任务未完成。用户决定不再继续本轮 AI 系统分析与设计，保留已经确认并沉淀的阶段性成果，未完成项不再推进。

### 当前进展

- 2026-08-26：用户决定直接退出本轮任务。以下进展是终止前已经确认并沉淀的成果，不代表本任务整体完成。

- 已建立 AI 业务能力层、AI 质量评价层和 AI 调用治理层的分层骨架，并明确 common-ai 与 admin-service 的辅助位置。
- 已确认保留 common-ai：业务服务拥有 Prompt、工作流和业务结果，common-ai 统一客户端契约，ai-gateway-service 统一模型调度和运行时治理。
- 已确认工作流版本与模型执行配置分离：同一工作流可以绑定不同模型组合进行实验，但每次正式任务或实验运行创建后必须锁定每个步骤的精确模型和参数，执行中不得跨模型切换。
- 已确认复杂版本的 AI 网关需要支持同一模型的多账号资源池，并分别处理账号级并发、请求速率、Token 速率和额度；账号全部不可用时排队、重试或失败，不改用另一模型。该能力不进入当前 0-1 首版。
- 已确认账号与 API Key 最好由本项目独占；账号配额可能被其他项目或其他密钥共享，本地限流只能控制本项目流量，必须预留安全余量并以供应商真实限流响应为最终事实。
- 已收缩 ai-gateway-service 的 0-1 范围：首版只完成同步调用、固定模型、单账号单端点、本地并发保护、文本与图片、最小调用记录和静态计量。任务队列、多账号资源池、分布式治理、流式、资源余量和管理能力暂缓，原有设计文档全部保留。
- 已完成 ai-gateway-service 的首版范围取舍和复杂版本恢复边界确认；统一调用契约、最小调用记录、静态计量等首版细节仍需继续逐点设计，不能视为网关详细设计已经全部完成。
- 已将五个 AI 服务的初始职责边界保留在各自 README 中；除 ai-gateway-service 已完成首版范围确认外，其余服务仍待用户选择并逐个梳理、修订和确认。

### 终止说明

- 本轮没有达到全部完成标准，未勾选条目保持未完成。
- 已确认的 AI 系统分层、服务边界和 ai-gateway-service 首版范围继续作为后续实现或重新设计时的当前文档依据。
- 本轮分支按用户要求以 `--no-ff` 合并到 `dev`，但合并不表示本任务已经完成。
- 下一轮任务目标由用户另行确认，不在本任务卡中预设。

### 目标

由用户亲自主导，在已经收缩的 0-1 范围内对 LeetModel 的 AI 功能与调度体系进行系统、深入、可反复修订的分析与设计，形成能够指导首版实现、支撑实习面试讲解，并让用户对首版关键细节有充分把握的设计文档体系。复杂版本只保留边界、已有思路和恢复条件，不要求在本轮继续设计完成。

本轮重视设计过程本身。开发实现只是后续独立任务，不以尽快产出代码为目标，也不预设一次讨论就能完成全部设计。

### 分支

`docs/ai-system-analysis-design`

### 入口

从现有 AI 用户能力、当前代码与已有设计文档出发，先建立系统全景和事实基线，再由用户选择当前要深入的具体问题。每次只讨论一个足够明确的设计点，确认后沉淀，再进入下一点。

### 主流程

1. 核对现有 AI 评审、AI 网关、质量评价及相关服务的代码、数据库、接口、运行事实和已有文档，区分已实现能力、已有设想与未确认内容。
2. 与用户共同梳理 AI 功能的参与者、使用场景、完整业务流程、调度链路、状态变化、服务职责、数据所有权和外部依赖，建立可持续细化的系统全景。
3. 由用户决定当前设计点和所需深度。Agent 针对该点主动提出关键问题、解释陌生概念、整理资料、比较可选方案，并可以补充少量简单建议作为防漏提醒或灵感，说明建议的用途与主要代价。
4. 对已经讨论清楚的内容逐项确认。未确认内容保留为问题、草案或候选方案，不伪装成结论。
5. 将确认结果写入职责对应的架构、服务、功能或概念文档；必要时细化到拟议数据表、字段、类型、约束、索引和关系，并明确其设计状态以及与当前实现的差异。
6. 反复审视 0-1 范围内的跨服务调用、单实例并发、模型调用、成本、必要日志、安全和最小数据生命周期。已经明确暂缓的任务调度、多账号、分布式治理、流式和管理能力只维护边界与恢复条件，不继续扩写。
7. 用户最终确认本轮设计达到预期后，再整理设计索引、已确认决策、未决问题和后续实现边界。本轮不直接进入编码。

### 完成标准

以下完成标准只约束 0-1 首版。已经明确暂缓的能力不要求完成详细流程、数据表和实现方案，只需保留现有文档并明确暂缓原因与恢复条件。

- [ ] 已建立当前 AI 系统的代码、数据库、接口、配置和运行事实基线，并与历史设想明确区分
- [x] 已形成覆盖用户入口、业务能力、服务协作、AI 调用和后台调度的系统全景
- [ ] 关键流程已说明触发条件、前置条件、状态变化、正常结果、异常分支和恢复路径
- [ ] 服务职责、数据所有权、接口方向和跨服务一致性边界已逐项确认
- [ ] 单实例并发、有界等待和超时边界已达到用户认可的深度；任务队列、复杂幂等、自动重试、取消和故障接管已明确暂缓
- [ ] 模型与供应商选择、Prompt 与版本、输入输出契约、Token 与成本、限额和安全边界已达到用户认可的深度
- [ ] 必要调用日志和最小追溯信息已达到用户认可的深度；专项指标、完整追踪、告警和管理审计已明确暂缓
- [ ] 0-1 需要持久化的最小调用事实已细化到用户认可的数据模型；暂缓能力的数据表不在本轮设计
- [ ] 关键方案均保留选择原因、替代方案、代价、例外和恢复条件，能够形成完整的面试讲解材料
- [ ] 所有文档明确区分当前实现、已确认目标设计、候选方案和未决问题
- [ ] 用户明确确认本轮分析与设计工作完成

以上条目是检查维度，不是一次性设计顺序，也不是 Agent 自动扩写的清单。用户可以随时调整问题顺序、增加必要探索、降低或提高某一部分的设计深度。

### 修改范围

- `TODO.md` 中本轮任务卡、进度和未决问题
- `CONTEXT.md` 中长期有效的 AI 设计协作原则
- `docs/project/01-需求分析/` 中 AI 用户场景、需求和用例分析
- `docs/project/02-架构设计/` 中 AI 系统全景、跨服务流程、边界和关键架构取舍
- `docs/project/03-微服务设计/` 中 AI 相关服务、功能、调度、状态和数据设计
- `docs/concepts/` 与 `docs/learning/` 中本轮确有必要沉淀的通用知识和分析方法
- 与详细设计颗粒度直接相关的文档规范和索引

修改范围只表示允许承载本轮设计的文档位置，不代表必须一次性修改所有目录。

### 非目标

- 不直接开发或修改 AI 功能代码、数据库迁移、接口、前端页面和运行配置
- 不因为已有早期设计而默认沿用全部结论，现有内容需要结合当前实现重新核对
- 不一次性生成所有 AI 功能的完整设计并要求用户被动验收
- 不由 Agent 替用户决定设计已经足够深入或思考已经过度
- 不把未经讨论的行业惯例、未来能力或复杂基础设施自动加入最终方案
- 不把整理面试材料变成脱离真实项目的概念堆砌
- 不并行推进排行榜、管理端完善或其他非 AI 业务功能
