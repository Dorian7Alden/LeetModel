# LeetModel TODO

> 本文件保存 RAG、AI 调用治理与 AI 评价平台的阶段路线图。路线图用于防止跨会话丢失已经确认的依赖和边界，不表示多个任务可以并行开发。
>
> 当前已启用托管模式。Agent 按依赖一次只领取一个 D 或 S 编号的细粒度任务卡，完成验收后自动进入下一张任务卡，不并行开发多个任务卡，也不要求用户逐卡重复确认。Git 按阶段管理分支：同一阶段的多个任务卡在同一阶段分支内顺序完成，整个阶段验收后才合回 `dev`。

## 使用约定

- [ ] 待开始；[~] 进行中；[x] 已完成并验收；[!] 被外部条件阻塞。
- 同一时间只有一个任务卡可以改为进行中。默认由用户选择；托管模式下由 Agent 按依赖自动领取下一张任务卡。
- 每个 Agent 开始前必须阅读根 CONTEXT.md、README.md、TODO.md、任务卡指定的设计文档和目标模块 README，并用实际代码、Flyway、配置和测试复核文档。
- 修改公共契约前必须检查全部生产者和消费者；数据库变更只新增 Flyway 迁移。
- 任务完成至少要同步受影响的正式文档并运行目标模块测试。公共模块变化时验证所有直接消费者可以编译。
- 不在配置、日志、测试夹具或文档中保存真实密钥、Relay Token、Prompt、回答正文、知识片段或论文内容。
- 按开发与 Git 规范自主创建本地阶段分支；每张任务卡验收后形成独立原子 commit，但继续在当前阶段分支领取下一张任务卡。阶段全部任务完成后才非快进 merge 到 `dev` 并清理已合并的本地阶段分支，避免频繁切换和合并。未经用户明确授权，不执行 push、force push、rebase、破坏性 reset、改写历史、远端分支操作或删除用户文件。

## 已核验的当前基线

以下结论来自 docs、模块 README、配置、数据库迁移和实际代码的交叉核验，后续开发不能假设项目处于空白状态。

### AI 网关与 new-api

- LeetModel-backend/docker-compose.yml 已部署 new-api，镜像为 calciumion/new-api:v1.0.0-rc.26。
- new-api 当前只是独立基础设施；ai-gateway-service 仍通过 DeepSeekAdapter、KimiAdapter 直连供应商。
- 本地开发 new-api 已创建专用测试 Relay Token，保存在仓库外的 `~/Desktop/new-api-test-key.txt`。该 Token 仅用于本机开发与 S1、S2、S3 的真实接口冒烟，不得把文件内容复制到仓库文档、配置、日志、测试夹具或提交记录。测试时由命令临时读取并注入进程环境，输出必须脱敏。
- 本地 new-api 已配置可用于测试的 DeepSeek 和 Kimi 相关模型。S1-01、S1-02 开始时仍需通过 `/v1/models` 和最小请求核验实际模型名、渠道状态与 Token 权限，不能只根据本说明假设全部模型可用。
- ai-gateway-service 尚无 new-api Base URL、Relay Token 和模型映射配置。
- common-ai 当前只有同步 Chat 客户端和 /internal/ai/chat，没有 Embedding 契约。
- AiScene 只有 GENERAL_TEXT 和 MULTIMODAL，表示输入模态而非真实业务来源，不能支持优先级和评价归因。
- AI 调用审计已有列表、统计及 admin 代理，不应重复从零建设；但字段只有基础 Token、模型、总耗时和错误。
- 网关设计文档曾描述本地并发保护，但当前 AiChatService 未发现对应实现，后续调度不能建立在该假设上。

### LangChain4j、Elasticsearch 与知识库

- 父 POM 锁定 LangChain4j 0.34.0，当前只有 ai-review-service 使用 PromptTemplate。
- 项目尚未引入 LangChain4j RAG、Embedding Store 或 Elasticsearch 集成依赖，本地 Docker 也未部署 Elasticsearch。
- rag_kb/数学建模 目前有 88 个 Markdown，其中包含各级 README；第一版只索引整理后的内容文档。
- rag_kb/data 是 337 个原始抓取 Markdown，rag_kb/数模评审参考资料 是约 88 MiB 的 PDF 原始材料，均不能默认进入 RAG V1 索引。
- rag_kb/.kb 和 CLAUDE.md 描述纯 AI 导航；旧 RAG 架构又把该方案称为 V1 并归 ai-review-service，已与新决策冲突。

### 客服、调度、评价与管理端

- ai-assistant-service 已有会话、消息、历史上下文、题目工具和现有 Chat 失败回复，RAG 应增量接入现有工作流。
- AI 网关当前收到请求后立即调用上游，没有持久化队列、优先级或实际本地容量保护。
- ai-evaluation-service 已有论文评审专用的数据集、任务、重复运行、恢复和比较，不是空白模块。
- 当前评价只支持一个 review workflowVersion，重复次数限制为 1 至 3；代码仍计算 overallScore，而部分设计文档又明确不做综合评分。
- admin-service 已有管理员鉴权的评价代理和 AI 调用查询；前端已有 EvaluationPage，后续应渐进扩展而不是重建。
- ai-suggestion-service 已是父 POM 中的真实运行模块，个别旧文档“尚无 Maven 模块”的说法已过期。

## 阶段 -1：正式设计校准

在任何功能编码前先处理会改变接口和服务边界的文档冲突。此阶段只校准正式设计，不顺手实现业务代码。

### [x] D-01 建立文档与代码冲突清单

- 目标：逐项记录 RAG 归属、RAG V1/V2 定义、new-api 接入状态、网关并发现状、评价综合分冲突和 suggestion 模块状态。
- 产物：每项包含文档位置、代码证据、采用结论和后续任务编号。
- 验收：冲突有唯一结论；不能只在 TODO 留结论而不标出需更新的正式文档。

### [x] D-02 重划 ai-gateway-service 与 new-api 职责

- 依赖：D-01。
- 必读：AI系统分层.md、new-api第三方网关集成.md、ai-gateway-service/设计/01 至 19。
- 工作：明确渠道、密钥、模型映射、重试、限流、计费、审计、错误和健康检查由哪一层负责，标出旧网关设计中被 new-api 取代的能力。
- 验收：正式架构文档包含请求链、数据链、错误链和重试边界，LeetModel 不复制 new-api 渠道治理。

### [x] D-03 重写 RAG 正式架构定义

- 依赖：D-01。
- 必读：RAG知识库.md、ai-assistant-service/README.md、rag_kb/.kb 全部文档。
- 工作：定义向量 RAG 为 V1、AI 目录导航为 V2；定义服务归属、知识源、索引边界、降级和独立服务触发条件。
- 验收：架构文档、根 CONTEXT.md、assistant README 和 rag_kb 行为说明最终一致。

### [x] D-04 校准 AI 评价需求与“版本选择指数”术语

- 依赖：D-01。
- 必读：ai-evaluation-service 全部设计、admin-service/AI测试控制、AI监控。
- 工作：区分稳定性、质量、资源、归一化和加权指数；明确哪些质量指标必须依赖标准答案或人工标注。
- 验收：正式需求不再同时出现“禁止综合评分”和“实现 overallScore”两个冲突目标。

### [ ] D-05 固化五种版本标识

- 依赖：D-02、D-03、D-04。
- 工作：定义 apiVersion、workflowVersion、promptVersion、modelExecutionConfigVersion、ragIndexVersion 的所有者、用途、不可变范围和展示名称。
- 验收：后续 DTO、表字段和页面不再用 /v1、/v2 代替业务工作流版本。

## 已确认的总体决策

- LangChain4j 需要先独立完成版本与组件兼容性核验，再决定是否从当前 `0.34.0` 升级。
- AI 业务服务不直接连接模型供应商或 new-api。Chat 与 Embedding 统一经过 `common-ai → ai-gateway-service → new-api`。
- new-api 负责供应商渠道、密钥、模型映射、渠道重试和额度；LeetModel 的 `ai-gateway-service` 保留内部契约、业务调用标识、统一错误、调度和调用审计。
- Elasticsearch 作为正式 RAG 向量库，内存向量库只用于自动化测试。
- 第一版 RAG 归属 `ai-assistant-service`。出现第二个真实消费者或在线知识管理需求后，再评估独立 RAG 服务。
- RAG 检索失败时记录日志并降级为现有客服回答；Chat 调用失败仍沿用现有失败回复。
- AI 网关调度一次模型调用，不接管论文评审、客服、建议或评价服务拥有的完整业务任务。
- 批量 AI 评价必须在优先级调度能力落地后开放，避免低优先级后台实验占满模型容量。
- 指标、归一化规则、权重方案和版本选择指数归 `ai-evaluation-service`；`admin-service` 只负责管理员入口、转发和跨服务展示。
- 加权结果定义为特定评价目标下的版本选择指数，不称为准确率或客观质量分。所有原始指标和计算口径必须保留。
- REST API 版本、AI 工作流版本、Prompt 版本、模型绑定版本和知识库索引版本分别管理，不使用 `/v1`、`/v2` 代替业务工作流版本。

## 阶段依赖

```text
正式设计校准
  ├→ LangChain4j 兼容性核验 ─────────────┐
  └→ new-api Chat 接入 → 调用上下文/审计 ├→ Embedding 统一调用 → 客服 RAG V1
                         ↓                ┘
                 调用计量与成本追踪 → AI 网关优先级调度
                                             ↓
                                      通用版本契约
                                             ↓
                                      通用 AI 评价平台
                                             ↓
                                      权重与管理端闭环
```

当前主线优先完成设计校准、LangChain4j 核验、new-api、Embedding 和客服 RAG V1。Embedding 至少依赖统一调用上下文和审计基础；完整费用补全可在 RAG 主线之外继续推进。调度必须早于批量评价开放。即使依赖允许交换先后，同一轮仍不得并行开发多个任务卡。

## 阶段 0：LangChain4j 兼容性核验

### 目标

确认 LangChain4j 与 Elasticsearch RAG 组件的可行组合，独立决定是否升级 LangChain4j。

### 调查范围

- 核验当前 LangChain4j `0.34.0` 对 `EmbeddingModel`、`EmbeddingStoreIngestor`、`EmbeddingStoreContentRetriever` 和 Elasticsearch Embedding Store 的支持情况。
- 核验与 Spring Boot 3.3.5、Elasticsearch 8.x 的版本兼容性。
- 判断是否升级 LangChain4j，并明确升级范围、收益和兼容风险。
- 用确定性假 Embedding 完成最小切分、写入和召回实验，避免把框架兼容性与外部模型可用性混为一项。
- 确认项目自定义 EmbeddingModel 适配方式和 Elasticsearch 客户端选择。

### 完成产物

- LangChain4j 版本决策。
- Elasticsearch 客户端与索引技术基线。
- 最小本地召回实验结果。
- Embedding 模型、向量维度和中文真实调用留到阶段 3 决定。

## 阶段 1：new-api 接入 ai-gateway-service

### 目标

将现有 Chat 调用从供应商官方接口直连切换为 `业务服务 → common-ai → ai-gateway-service → new-api → 模型供应商`。

### 主要范围

- 增加 new-api Base URL、Relay Token 和超时配置。
- 实现 new-api OpenAI 兼容调用适配。
- 建立 LeetModel 逻辑模型到 new-api 模型名的映射。
- 映射限流、额度不足、模型不存在和服务不可用等错误。
- 关联 LeetModel `callId` 与 new-api 请求 ID。
- 明确两层网关中模型选择、渠道选择和重试的唯一责任方。
- 设计并验证迁移期间的回退方式。

### 完成标准方向

- AI 客服和论文评审至少各完成一条真实 new-api 调用链。
- 业务服务不持有 new-api Relay Token 或供应商密钥。
- new-api 负责渠道级重试，LeetModel 不产生重复重试和重复费用。

## 阶段 2：AI 调用计量与成本追踪

### 目标

让 `ai-gateway-service` 拥有可关联业务任务的单次调用事实，为监控、调度和评价提供可信数据。

### 指标范围

- 输入、输出、Reasoning、Cache 和总 Token。
- 实际费用或按价格快照核算的费用，以及费用来源。
- 模型、渠道、业务服务、业务场景、业务任务和工作流版本。
- 排队耗时、模型执行耗时和总响应时间。
- 成功状态、失败分类、LeetModel `callId` 和 new-api 请求 ID。

### 规则

- 优先采用 new-api 的实际扣费信息；无法取得时按 Token 和价格快照核算。
- Token 或费用未知时显式标记未知，不使用零值伪装。
- LeetModel 保存必要调用事实，管理页面不依赖临时读取 new-api 日志作为唯一数据源。

## 阶段 3：Embedding 统一调用能力

### 目标

建立 `LangChain4j EmbeddingModel → common-ai → ai-gateway-service → new-api /v1/embeddings` 的统一调用链。

### 主要范围

- 在 `common-ai` 增加 Embedding 请求、响应和客户端契约。
- 在 `ai-gateway-service` 增加内部 Embedding 接口、模型配置和能力校验。
- 为 LangChain4j 实现调用 `common-ai` 的项目内 `EmbeddingModel` 适配器。
- 支持批量输入、输入上限、超时、错误映射和向量维度校验。
- 将 Embedding 的 Token、费用、耗时和调用标识纳入统一审计。

### 非目标

- 本阶段不加载知识库，不实现 Elasticsearch 索引和客服检索。

## 阶段 4：AI 客服 RAG V1

### 目标

在 `ai-assistant-service` 中实现基于 LangChain4j 和 Elasticsearch 的常规向量 RAG，让客服回答可以引用 `rag_kb/` 中的相关知识。

### 索引主流程

```text
rag_kb Markdown
→ 文档过滤与元数据提取
→ 内容清洗与切分
→ Embedding
→ Elasticsearch 索引
```

### 问答主流程

```text
用户问题
→ Query Embedding
→ Elasticsearch Top K 召回
→ 最低相关度过滤
→ Token 预算裁剪
→ 来源化知识上下文
→ 现有 AssistantWorkflow
→ Chat 回答
```

### 主要范围

- 第一版使用 LangChain4j 的基础向量 RAG，不提前实现查询改写、多路召回和重排。
- 为知识片段保存来源路径、标题、内容版本和索引版本等必要元数据。
- 设计知识库初始化、增量更新、删除同步和幂等索引策略。
- 将 `rag_kb/.claude/CLAUDE.md` 的 AI 行为说明迁移为 `rag_kb/CONTEXT.md`。
- 同步修正项目根 `CONTEXT.md` 与 RAG 设计文档中已经过期的归档状态。
- 检索失败时记录日志并降级为无 RAG 客服回答。

### 非目标

- AI 根据完整目录树和文档摘要自主选择参考文档的 RAG V2 只完成设计，不进入本阶段实现。
- 不建设在线知识上传、发布和回滚管理。
- 不拆分独立 RAG 微服务。

## 阶段 5：AI 网关优先级调度详细设计

### 目标

在编码前补全原子调用任务、队列、公平性、容量和恢复设计，使实时请求与后台任务共享有限模型容量时行为可解释。

### 建议优先级

| 等级 | 场景 |
|------|------|
| P0 | AI 客服等用户实时交互 |
| P1 | 正式论文评审和正式建议生成 |
| P2 | 管理员手动触发的单次测试 |
| P3 | ai-evaluation-service 批量重复实验 |
| P4 | RAG 批量索引和离线探测 |

### 必须确认的设计点

- 不采用会让后台任务永久饥饿的简单严格优先级队列。
- 设计高优先级保留容量、加权公平调度和低优先级老化机制。
- 明确同步客服如何等待队列结果，后台任务如何异步取得结果。
- 明确队列持久化、租约、心跳、取消、超时、重新入队、死信和崩溃恢复。
- 明确队列满时的背压与拒绝策略。
- 明确 new-api 限流反馈如何影响派发速度。
- 明确 Chat 与 Embedding 是否共享容量和调度队列。

## 阶段 6：AI 网关任务队列实现

### 目标

按照阶段 5 确认的方案实现模型调用优先级调度，保证后台实验不会阻塞客服和正式业务。

### 第一版建议边界

- 持久化原子调用任务和状态。
- 先实现单调度器实例和有限并发，不直接进入复杂分布式调度。
- 实现固定优先级、加权公平、等待老化和高优先级容量保护。
- 实现任务租约、超时恢复、取消和必要的幂等保护。
- 客服同步等待有界结果，评价服务异步等待业务实验结果。
- 暂不实现多账号资源池和跨模型动态切换。

### 关键验收方向

- 大量评价任务运行时，客服仍能及时获得执行机会。
- 客服持续请求时，后台任务不会永久饥饿。
- AI 网关重启后未完成任务可以恢复或明确失败。
- 调用方超时不会产生不可解释的重复调用和重复费用。
- new-api 限流时不会形成持续请求风暴。

## 阶段 7：统一 AI 功能版本契约

### 目标

让评价平台可以明确选择并执行不同 AI 功能及其工作流版本，而不只支持论文评审版本。

### 版本维度

- `featureCode`：`REVIEW`、`ASSISTANT`、`SUGGESTION` 等业务功能。
- `workflowVersion`：业务工作流版本。
- `promptVersion`：Prompt 版本。
- `modelBinding`：本次执行锁定的模型配置。
- `ragVersion`：适用时锁定知识内容或索引版本。
- REST API 版本继续单独管理。

### 业务服务能力

每个被评价的 AI 业务服务按统一边界提供：

- 可评价版本列表和版本配置摘要。
- 隔离实验执行接口。
- 标准化运行状态和结果摘要。
- 实验结果不写入或覆盖正式用户业务结果。

## 阶段 8：ai-evaluation-service 通用实验平台

### 目标

将当前只覆盖论文评审稳定性的实现扩展为可以批量执行指定次数、指定 AI 功能版本的通用实验平台。

### 实验定义

- AI 功能类型。
- 一个或多个候选工作流版本。
- 固定测试集。
- 每个版本和样本的重复次数。
- 指标口径版本。
- 权重方案快照。
- 运行优先级固定为后台低优先级。

### 指标分层

#### AI 网关运行指标

- 成功率、Token、费用、排队耗时、执行耗时、总耗时和失败分类。

#### 业务确定性指标

- 论文评审的方差、标准差、极差和结构化输出有效率。
- 客服、RAG 和建议功能的指标必须结合各自固定测试集与可验证规则单独设计。

#### 人工或真值指标

- 正确性、相关性和满意度必须依赖人工标注、标准答案或明确评分规则，不能从费用、Token、耗时或稳定性推导。

### 状态与恢复

- 实验任务、样本运行槽位和每次尝试独立留痕。
- 支持暂停、取消、环境失败重试和中断恢复。
- 成功结果不因重试被覆盖。
- 同口径结果才允许直接比较。

## 阶段 9：权重方案与管理端闭环

### 目标

允许管理员配置指标权重，批量启动指定 AI 功能版本的实验，并查看原始指标、版本选择指数、进度、失败和调用成本。

### 权重方案规则

- 权重方案归 `ai-evaluation-service` 所有并版本化保存。
- 方案包含适用功能、指标口径版本、归一化参数、各指标权重和状态。
- 权重之和必须为百分之百。
- 成本、延迟和方差等越低越好的指标采用明确的反向归一化规则。
- 缺失指标显式标记不可计算，不默认按零分处理。
- 实验启动时保存权重与归一化规则快照，后续修改不改变历史结果。
- 使用另一套权重重新计算时生成新的评分结果版本，不覆盖原结果。
- 综合结果命名为版本选择指数，不能解释为准确率或客观质量分。

### admin-service 范围

- 提供管理员鉴权后的测试集、版本、重复次数和权重方案操作入口。
- 转发启动、暂停、取消、重试和重新计算命令。
- 聚合评价进度、业务指标和 AI 网关资源消耗。
- 不保存评价事实，不执行指标计算，不直接读取其他服务数据库。

### 管理端页面范围

- 测试集管理。
- AI 功能和候选版本选择。
- 重复次数、预计调用量和预计成本确认。
- 权重方案配置。
- 实验进度、暂停、取消和失败重试。
- 原始指标、版本选择指数和同口径版本对比。
- 从实验运行追踪到单次 AI 网关调用。

## 细粒度执行任务卡

> 上面的“阶段”负责说明目标、边界和 Git 分支范围，下面的任务卡是后续 Agent 实际领取与验收的最小工作单元。阶段内按编号串行推进，每张任务卡独立提交；任务范围不得擅自合并，但同一阶段共用一条阶段分支，全部任务完成后统一合回 `dev`。

### S0：LangChain4j 兼容性核验任务

#### [ ] S0-01 建立候选版本兼容矩阵

- 依赖：D-03。
- 工作：只依据官方文档、发布说明和 Maven 元数据，对比当前 0.34.0 与候选稳定版的 Java 17、Spring Boot 3.3.5、EmbeddingModel、Ingestor、Retriever、Elasticsearch Store 和 OpenAI 自定义 Base URL 支持。
- 验收：列出 API 差异、依赖冲突、迁移成本和已知风险，不能只以“版本更高”为升级理由。

#### [ ] S0-02 在 0.34.0 上完成最小 RAG 编译实验

- 依赖：S0-01。
- 工作：在隔离测试代码中完成 Markdown → 切分 → 确定性假 Embedding → Store → Top K 召回，并验证项目自定义 EmbeddingModel 适配是否可行。
- 验收：记录可用 API、缺失组件和编译结果；实验代码不进入正式客服工作流。

#### [ ] S0-03 仅在必要时完成升级实验

- 依赖：S0-02 明确判定当前版本不满足必要能力。
- 工作：以最小升级范围验证新版本，同时回归 ai-review-service 的 PromptTemplate 使用。
- 验收：父 POM、review 测试和 RAG 实验通过；若 0.34.0 可满足需求，本任务记录为“不适用”。

#### [ ] S0-04 固化 LangChain4j 技术基线

- 依赖：S0-02，必要时 S0-03。
- 工作：在技术栈文档固定版本、所需模块、Elasticsearch 客户端、项目 EmbeddingModel 适配方式和升级结论。
- 验收：POM 与文档一致；后续阶段不得自行更换到 Spring AI 或另一套向量框架。

### S1：new-api Chat 接入任务

#### [ ] S1-01 核验 new-api 初始化与测试渠道

- 依赖：D-02。
- 工作：只读确认容器版本、持久化、管理员初始化、模型列表、渠道状态和 Relay Token 创建方式；标出需要用户手工完成的控制台操作。
- 验收：形成无密钥运行记录；若缺渠道或 Token，状态改为阻塞并给出最小人工步骤。

#### [ ] S1-02 黑盒验证文本 Chat 协议

- 依赖：S1-01。
- 工作：用固定小请求验证认证头、模型名、文本消息、JSON 输出、thinking、超时、响应体和错误体。
- 验收：输出脱敏字段表；不把真实 Token 写入仓库、日志文件或测试代码。

#### [ ] S1-03 黑盒验证多模态协议

- 依赖：S1-02。
- 工作：使用最小合成图片验证 image_url 或实际兼容格式、图片限制、模型名和失败返回。
- 验收：得到论文评审可实现的映射结论；不上传 data 中的真实论文。

#### [ ] S1-04 黑盒验证请求 ID、usage、quota 和日志

- 依赖：S1-02。
- 工作：核验响应头/体中的请求 ID、输入/输出/缓存/推理 Token、quota、日志查询字段和数据出现延迟。
- 验收：明确同步可得、异步可得和不可得字段，为费用任务提供真实依据。

#### [ ] S1-05 增加 new-api 安全配置

- 依赖：S1-02。
- 工作：新增 Base URL、Relay Token 引用、连接/读取超时和启动校验；本地默认地址与共享环境显式配置分开。
- 验收：缺 Token 时错误明确；配置文件没有真实密钥；属性绑定测试通过。

#### [ ] S1-06 实现 new-api Chat 适配器

- 依赖：S1-03、S1-05。
- 工作：映射文本、多模态、JSON 格式、thinking、temperature、maxTokens 和超时；复用现有协议对象。
- 验收：Mock Server 覆盖两种输入、超时、非 2xx 和畸形响应；适配器不实现渠道级自动重试。

#### [ ] S1-07 映射响应、错误和关联 ID

- 依赖：S1-04、S1-06。
- 工作：处理认证失败、模型不存在、额度不足、限流、上游不可用、超时和未知错误；关联 LeetModel callId 与 new-api 请求 ID。
- 验收：每类错误有测试；对业务服务不泄露上游敏感信息。

#### [ ] S1-08 调整逻辑模型路由

- 依赖：D-05、S1-06。
- 工作：将逻辑能力映射到 new-api 模型名；业务服务不选择供应商渠道；分别固定文本和多模态能力绑定。
- 验收：assistant/review 请求无供应商密钥和渠道 ID；不存在的模型能在启动或调用时明确失败。

#### [ ] S1-09 完成网关 Chat 回归测试

- 依赖：S1-07、S1-08。
- 工作：更新 AiChatService、Controller、审计和配置测试，覆盖成功、失败、usage 缺失、长超时与关联 ID。
- 验收：ai-gateway-service 与 common-ai 测试通过；旧适配器被保留或替换的状态明确。

#### [ ] S1-10 完成真实客服文本冒烟

- 依赖：S1-09。
- 工作：用 ai-assistant-service 走 common-ai → ai-gateway → new-api 完成一条最小调用链。
- 验收：记录 callId、new-api ID、模型和 usage；客服失败回复保持原行为；日志无正文。

#### [ ] S1-11 完成真实论文评审多模态冒烟

- 依赖：S1-10。
- 工作：使用项目允许的最小测试夹具验证 review 调用，不先执行完整大 PDF 批次。
- 验收：多模态只经 new-api；调用成功或失败均可关联审计；无论文内容日志。

#### [ ] S1-12 完成切换、回退和旧直连收口

- 依赖：S1-11。
- 工作：短期显式回退或删除旧 DeepSeek/Kimi 直连二选一；禁止双写和同请求双调用；同步启动/部署文档。
- 验收：唯一默认链路为 new-api；回退可审计且不造成重复费用；不存在隐式直连。

### S2：调用上下文、用量和费用任务

#### [ ] S2-01 分离业务来源与输入模态

- 依赖：D-05、S1-12。
- 工作：设计 featureCode、operationCode、modality；首批覆盖客服、正式评审、实验评审、建议、RAG 索引和 RAG 查询。
- 验收：GENERAL_TEXT/MULTIMODAL 有兼容迁移；路由能力和统计来源可以分别查询。

#### [ ] S2-02 定义统一 AiCallContext

- 依赖：S2-01。
- 工作：定义 callerService、featureCode、operationCode、businessTaskId、workflowVersion、promptVersion、modelConfigVersion、evaluationTaskId、priority、idempotencyKey、deadline。
- 验收：必填、长度、脱敏和传递边界明确；网关不解释业务 ID 的实体语义。

#### [ ] S2-03 扩展 common-ai Chat 契约和消费者

- 依赖：S2-02。
- 工作：更新请求、客户端和序列化测试，逐个修改 assistant、review、suggestion 调用入口。
- 验收：common-ai 与三个消费者编译通过；不存在所有调用继续归 GENERAL_TEXT 的兼容偷懒。

#### [ ] S2-04 扩展 Token 与费用值对象

- 依赖：S1-04。
- 工作：表达输入、输出、推理、缓存命中/创建、总 Token，以及币种、Decimal 金额、费用来源、价格快照和完整性。
- 验收：未知值不是零；单位、精度、完整/部分/未知都有序列化测试。

#### [ ] S2-05 迁移 AI 调用审计表

- 依赖：S2-03、S2-04。
- 工作：新增 Flyway 和索引，保存 newApiRequestId、业务上下文、queueMs、executionMs、totalMs、费用及来源；保留历史行兼容。
- 验收：旧数据可读；支持按 callId、业务任务、评价任务、时间和状态查询；无正文列。

#### [ ] S2-06 实现同步 usage 归一化

- 依赖：S2-05。
- 工作：按 S1-04 的真实响应映射完整、部分和缺失 usage；审计写入失败不改变业务成功结果。
- 验收：三类 usage 测试通过；不臆造 new-api 未提供的字段。

#### [ ] S2-07 设计并实现费用补全

- 依赖：S1-04、S2-05。
- 工作：在日志查询、回调或本地价格快照中选择实际可行方案；实现幂等补全、重试上限、最终未知和费用来源。
- 验收：不会重复累计；new-api 日志不可用不影响业务；实际与估算费用可区分。

#### [ ] S2-08 扩展现有调用查询和 admin 代理

- 依赖：S2-07。
- 工作：在已有 calls、stats 接口上增加来源、任务、版本、费用、用量、耗时和 ID 过滤/聚合。
- 验收：可从 evaluationTaskId 追踪 callId；保持管理员鉴权；不建立重复查询接口。

#### [ ] S2-09 完成计量集成测试

- 依赖：S2-08。
- 工作：使用 Mock new-api 覆盖成功、失败、usage 缺失、费用延迟、重复补全和查询聚合。
- 验收：ai-gateway、common-ai、admin 相关测试通过，形成评价平台可依赖的稳定契约。

### S3：Embedding 统一调用任务

#### [ ] S3-01 定义 common-ai Embedding 契约

- 依赖：S0-04、S2-02。
- 工作：定义单条/批量输入、逻辑模型、AiCallContext、向量、维度、usage、callId 及输入限制。
- 验收：校验和序列化测试通过；不暴露渠道和密钥。

#### [ ] S3-02 实现 common-ai Embedding HTTP 客户端

- 依赖：S3-01。
- 工作：调用 /internal/ai/embeddings；选择扩展 AiClient 或独立 EmbeddingClient，并保持 Chat 兼容。
- 验收：成功、统一错误、空响应和超时测试通过。

#### [ ] S3-03 实现网关 Embedding 接口和能力校验

- 依赖：S1-08、S3-02。
- 工作：新增 Controller、Service、逻辑模型配置、批量/长度/维度校验，区分 RAG_INDEX 与 RAG_QUERY。
- 验收：错误模型、超批量、空输入和维度不符均明确拒绝。

#### [ ] S3-04 实现 new-api /v1/embeddings 适配

- 依赖：S3-03。
- 工作：映射数组输入、模型名、encoding_format、usage 和错误。
- 验收：Mock new-api 覆盖单条、批量、限流、额度、畸形向量和维度变化。

#### [ ] S3-05 实现 LangChain4j 项目 EmbeddingModel 适配器

- 依赖：S0-04、S3-04。
- 工作：通过 common-ai 调用，处理批量、TokenCount、响应 metadata 和异常；不得直接连接 new-api。
- 验收：确定性向量单测通过；assistant 配置中不存在 new-api 地址或 Token。

#### [ ] S3-06 接入审计并完成中文真实冒烟

- 依赖：S2-06、S3-05。
- 工作：记录模型、维度、条数、Token、费用、耗时和失败，不记录原文与向量；执行一次小批量中文调用。
- 验收：维度稳定、callId 可查、敏感输入不进日志。

### S4：Elasticsearch 与客服 RAG V1 任务

#### [ ] S4-01 增加 Elasticsearch 本地基础设施

- 依赖：S0-04。
- 工作：选择兼容的 Elasticsearch 8.x 固定版本，加入 Compose、健康检查、持久卷和开发资源限制。
- 验收：可独立启动并健康；README 写明端口、版本和启动方式；默认命令不删除数据。

#### [ ] S4-02 增加 assistant RAG 配置

- 依赖：S4-01。
- 工作：配置开关、知识库路径、索引别名、Top K、阈值、Token 预算、批量和超时；测试允许内存 Store。
- 验收：非法配置测试通过；RAG 关闭时完全保持现有行为。

#### [ ] S4-03 固化知识源纳入与排除规则

- 依赖：D-03。
- 工作：只纳入 rag_kb/数学建模 的内容 Markdown；排除各级 README、.kb、.claude、CONTEXT、data、docs、scripts、.git、数模评审参考资料和 PDF。
- 验收：从实际目录生成确定性文件清单；新增未声明目录不会自动进入索引。

#### [ ] S4-04 将 rag_kb 行为说明迁移为 CONTEXT.md

- 依赖：D-03、S4-03。
- 工作：迁移 CLAUDE.md，保留结构、笔记和维护规范；明确向量 V1 与 AI 导航 V2；移除 Claude Code 专属表述。
- 验收：CONTEXT.md 可独立指导 Agent；内部链接有效；旧 CLAUDE.md 删除或只保留无冲突迁移提示。

#### [ ] S4-05 实现 Markdown 加载与元数据提取

- 依赖：S4-03、S4-04。
- 工作：提取相对路径、标题、YAML tags/summary、目录层级、内容哈希和修改信息；安全处理缺字段和坏文件。
- 验收：README 被排除；内容文档计数稳定；单个坏文件不会静默污染索引。

#### [ ] S4-06 实现 Markdown 结构化清洗

- 依赖：S4-05。
- 工作：处理 YAML、标题、列表、代码块、表格和链接，保留语义与来源，不把绝对路径写入展示上下文。
- 验收：模型方法、比赛经验和评审文档都有快照测试。

#### [ ] S4-07 确定并实现中文切分策略

- 依赖：S0-04、S4-06。
- 工作：先按标题/段落，再按长度带重叠切分；固定最小、目标、最大 Token 和重叠量。
- 验收：长短文、表格、代码块和中文标点测试通过；片段不超过 Embedding 上限。

#### [ ] S4-08 定义稳定文档 ID、片段 ID 与索引版本

- 依赖：S4-07。
- 工作：基于相对路径、内容哈希和片段序号生成 ID；定义 contentVersion、embeddingModelVersion、chunkPolicyVersion、ragIndexVersion。
- 验收：重复索引不产生重复片段；改单文档只影响该文档；版本字段可查。

#### [ ] S4-09 创建 Elasticsearch 向量索引与别名

- 依赖：S3-06、S4-08。
- 工作：定义 dense_vector、相似度、元数据 mapping、物理索引命名和读别名；启动时校验向量维度。
- 验收：维度错误拒绝写入；旧索引不自动覆盖；测试使用独立索引。

#### [ ] S4-10 实现全量索引命令

- 依赖：S4-09。
- 工作：加载、切分、批量 Embedding、批量写入、失败汇总；全部成功后原子切换读别名。
- 验收：失败不切别名；重复执行一致；只输出文档数、片段数、失败数和版本。

#### [ ] S4-11 实现增量更新与删除同步

- 依赖：S4-10。
- 工作：比较 manifest/内容哈希，分别处理新增、修改、删除；全部操作幂等可重试。
- 验收：三个场景测试通过；中断恢复不遗留已删除片段。

#### [ ] S4-12 实现基础向量检索

- 依赖：S4-10。
- 工作：Query Embedding → ES Top K → 阈值过滤 → 去重 → Token 预算裁剪，返回文本、分数、路径、标题和版本。
- 验收：空结果、低相关、ES 超时、Embedding 失败和维度错误均有测试；不做改写和重排。

#### [ ] S4-13 接入 AssistantWorkflow

- 依赖：S4-12。
- 工作：在受控位置注入带来源边界的知识上下文，防止知识内容覆盖系统行为；保留历史消息和题目工具。
- 验收：命中、无命中和工具调用都通过；Chat 审计能关联 ragIndexVersion。

#### [ ] S4-14 实现 RAG 降级和可观测日志

- 依赖：S4-13。
- 工作：捕获可降级的 Embedding、ES、解析和超时异常；记录类型、耗时、索引版本和召回数量，不记录问题或知识正文。
- 验收：检索失败继续现有 Chat；Chat 失败仍返回现有失败回复；两类失败可区分。

#### [ ] S4-15 完成自动化与真实验收

- 依赖：S4-11、S4-14。
- 工作：单测用确定性 Embedding/内存 Store，集成测试用独立 ES；覆盖命中、无命中、错误阈值、ES/Embedding 故障、重复索引和删除。
- 验收：全链路通过并记录延迟、Token 增量与召回基线。

#### [ ] S4-16 同步 RAG 运维与模块文档

- 依赖：S4-15。
- 工作：更新 assistant README、RAG 架构、Docker、配置、索引、回滚、测试索引清理和故障说明。
- 验收：按文档可从空 ES 完成一次索引和客服问答，正式索引无破坏性清理命令。

### S5：AI 网关优先级调度任务

#### [ ] S5-01 采集无队列并发基线

- 依赖：S2-09。
- 工作：分别测客服文本、论文多模态、Embedding 索引和后台实验的单并发与小并发。
- 验收：记录 p50/p95、失败率、超时与 429 样本，不用生产密钥做破坏性压测。

#### [ ] S5-02 定义调用任务、执行尝试与状态机

- 依赖：S2-02、S5-01。
- 工作：区分业务任务、逻辑 AI 调用和上游 attempt；定义 QUEUED、LEASED、RUNNING、SUCCEEDED、FAILED、CANCELLED、EXPIRED 及合法转换。
- 验收：同步 Chat、异步评价和 Embedding 批次均能映射，网关不复制完整业务流程。

#### [ ] S5-03 固化优先级分类

- 依赖：S2-01、S5-02。
- 工作：P0 客服，P1 正式评审/建议，P2 管理员单次测试，P3 批量评价，P4 RAG 批量索引。
- 验收：调用方不能任意提权；未知来源使用安全默认级别并记录。

#### [ ] S5-04 选定公平与防饥饿算法

- 依赖：S5-03。
- 工作：用具体参数组合加权公平、等待老化和高优先级保留容量。
- 验收：确定性样例证明 P0 延迟受控且 P3/P4 最终执行，不能只写原则。

#### [ ] S5-05 设计容量、背压和同步等待

- 依赖：S5-01、S5-04。
- 工作：固定并发、最大队列、最大等待、deadline、拒绝错误、断连行为，以及 Chat/Embedding 是否分池。
- 验收：不无限占用 Web 线程；调用方超时不会隐式产生第二次费用。

#### [ ] S5-06 设计持久化、租约与崩溃恢复

- 依赖：S5-02、S5-05。
- 工作：定义任务表、attempt 表、owner/leaseExpiry、心跳、重入、死信、取消和幂等键。
- 验收：对“未知是否已被上游执行”的情况有明确策略，禁止盲目重试。

#### [ ] S5-07 实现队列表与并发安全仓储

- 依赖：S5-06。
- 工作：新增 Flyway、实体、Mapper、索引和条件状态更新；ai_call_log 继续保存调用事实。
- 验收：并发领取仅一个成功者；状态跳转受条件限制；数据库测试通过。

#### [ ] S5-08 实现单实例公平调度器

- 依赖：S5-07。
- 工作：实现入队、选择、租约、并发许可、老化、deadline、取消检查和执行结果持久化。
- 验收：测试覆盖优先级、公平、老化、队列满、并发上限和租约过期。

#### [ ] S5-09 接入 new-api 限流反馈

- 依赖：S1-07、S5-08。
- 工作：解析实际 Retry-After/错误信号，实现有上限的调度退避，不复制 new-api 渠道重试。
- 验收：持续 429 时降低派发，恢复后逐步放量，无无限重试。

#### [ ] S5-10 改造同步 Chat 和 Embedding 调用

- 依赖：S5-09。
- 工作：Chat 入队后有界等待；RAG_QUERY 使用实时优先级，RAG_INDEX 使用后台最低优先级；保留 callId。
- 验收：assistant/review/suggestion 语义不变，大量索引不能阻断客服，超时不重复调用。

#### [ ] S5-11 完成恢复、取消和混合负载验收

- 依赖：S5-10。
- 工作：覆盖进程重启、租约过期、数据库短故障、429、断连、取消、P0 洪峰和 P3/P4 积压。
- 验收：客服获得执行机会、后台不永久饥饿、未知上游结果不重复扣费。

#### [ ] S5-12 增加只读队列监控与运维文档

- 依赖：S5-11。
- 工作：按状态、优先级、来源和等待时间查询积压；说明告警、取消入口和恢复边界。
- 验收：查询无 Prompt/回答正文，管理员无需直接改数据库。

### S6：统一 AI 功能与版本契约任务

#### [ ] S6-01 盘点 REVIEW、ASSISTANT、SUGGESTION 的真实状态

- 依赖：D-05。
- 工作：核对 POM、Controller、Service、Feign、数据库和前端，记录 owner、正式入口、实验入口、版本来源与缺口。
- 验收：修正 suggestion 旧文档；未实现能力不得出现在“可评价版本”列表。

#### [ ] S6-02 定义 AI 功能描述与版本查询契约

- 依赖：S6-01。
- 工作：定义 featureCode、支持的数据集/指标、owner，以及 workflowVersion 的编码、名称、状态、输入、输出 schema 和兼容说明。
- 验收：管理端无需手输任意版本；禁用版本不可新建实验，历史快照仍可读。

#### [ ] S6-03 定义不可变模型执行配置

- 依赖：S2-02、S6-02。
- 工作：modelExecutionConfigVersion 固定逻辑模型、参数、Prompt、thinking、输出格式和适用工作流，运行时保存快照。
- 验收：同一槽位重试配置一致；修改模型别名不改变历史含义。

#### [ ] S6-04 定义隔离实验执行契约

- 依赖：S6-03。
- 工作：定义 experimentRunId、sample、workflowVersion、modelConfigVersion、ragIndexVersion、priority 和标准结果摘要。
- 验收：能表达 REVIEW 与 ASSISTANT；不覆盖正式评审、客服会话或建议结果。

#### [ ] S6-05 适配现有论文评审实验

- 依赖：S6-04。
- 工作：用通用契约包装已有 ReviewVersion 和实验接口，补充模型配置快照并保留兼容 DTO。
- 验收：现有评价测试通过，旧接口的迁移终点明确。

#### [ ] S6-06 增加客服可评价版本

- 依赖：S4-16、S6-04。
- 工作：提供无 RAG、RAG V1 的版本列表和隔离单轮实验入口，锁定索引和模型配置，不创建正式会话。
- 验收：同一问题可重复执行；结果包含 callId 与 ragIndexVersion。

### S7：通用 ai-evaluation-service 任务

#### [ ] S7-01 设计通用领域模型与旧数据迁移

- 依赖：D-04、S6-04。
- 工作：把现有 dataset、sample、task、run_attempt 映射为 feature + version + dataset，选择兼容扩列或新表。
- 验收：历史论文评审任务不丢失且仍可查询，字段所有权和状态机明确。

#### [ ] S7-02 定义版本化数据集与样本 Payload

- 依赖：S7-01。
- 工作：REVIEW 引用 submissionId；ASSISTANT 保存问题、标签和可选标准要点；SUGGESTION 在真实实验入口完成后再接入。
- 验收：schema 版本化，不把本地绝对路径或大 PDF 写入评价库。

#### [ ] S7-03 建立指标注册表

- 依赖：D-04、S7-02。
- 工作：为网关资源、业务确定性、人工/真值质量指标定义单位、方向、来源、适用功能、missingPolicy 和 metricVersion。
- 验收：Token、费用、延迟和稳定性不能冒充正确性；未知指标不可参与计算。

#### [ ] S7-04 新增通用评价 Flyway

- 依赖：S7-01、S7-03。
- 工作：保存 feature、候选版本、执行配置、指标口径和快照，为旧任务回填兼容值，建立 task/slot/attempt/callId 索引。
- 验收：空库和已有库都可升级，无破坏性 SQL，历史读取回归通过。

#### [ ] S7-05 实现通用实验 Runner SPI

- 依赖：S6-04、S7-04。
- 工作：定义版本发现、样本校验、执行、结果解析和指标提取接口，避免评价核心堆叠 feature if/else。
- 验收：可注册假 Runner；未知功能明确失败；核心不依赖 review 专有 DTO。

#### [ ] S7-06 迁移论文评审 Runner

- 依赖：S6-05、S7-05。
- 工作：适配 submission、review 结果、score 与结构有效性，保留恢复和环境失败重试。
- 验收：旧数据集和比较可用；成功、业务失败、环境失败测试通过。

#### [ ] S7-07 实现客服 Runner

- 依赖：S6-06、S7-05。
- 工作：调用隔离实验接口，保存必要结果摘要、callId 和索引版本，不创建正式会话。
- 验收：能比较无 RAG 与 RAG V1；不保存不必要的完整敏感回答。

#### [ ] S7-08 扩展重复次数与规模预估

- 依赖：S5-12、S7-05。
- 工作：替换固定 1 至 3 限制；增加最大样本、版本、总槽位、预计调用量/费用和权限校验。
- 验收：创建前可预估；超限拒绝；所有批次固定为 P3。

#### [ ] S7-09 接入低优先级、幂等和调用关联

- 依赖：S7-08。
- 工作：为 task/slot/attempt 生成稳定上下文和 idempotencyKey，保存 callId，区分业务失败与环境失败。
- 验收：服务重启不重复成功槽位；客服可优先执行；未知上游结果不盲目重试。

#### [ ] S7-10 实现暂停、取消和恢复

- 依赖：S7-09。
- 工作：暂停新派发、取消可取消的排队调用、保留运行中结果、恢复剩余槽位，并记录操作者。
- 验收：暂停后无新调用；取消不删历史；重启后可恢复。

#### [ ] S7-11 实现可信原始指标

- 依赖：S7-03、S7-09。
- 工作：聚合成功率、失败分类、Token、费用、排队/执行/总耗时；review 计算均值、方差、标准差、极差和结构有效率。
- 验收：公式有确定性测试；样本不足和缺失 usage 显式处理。

#### [ ] S7-12 实现客服/RAG 可验证指标

- 依赖：S7-07、S7-11。
- 工作：先支持检索命中、来源覆盖、格式规则和人工标注占位；语义正确性只在有标准要点或人工评分时计算。
- 验收：无真值时显示未评价，不从成本或延迟推导质量。

#### [ ] S7-13 实现口径快照和同口径比较

- 依赖：S7-11、S7-12。
- 工作：保存 metricVersion、参数和数据集版本；比较时校验 feature、dataset、metricVersion 和执行配置。
- 验收：不同口径默认拒绝排名，只允许带不可比标记并排展示。

#### [ ] S7-14 完成通用评价迁移回归

- 依赖：S7-06 至 S7-13。
- 工作：覆盖历史读取、新任务、恢复、暂停、取消、比较、限流和费用缺失。
- 验收：全量测试通过，旧接口兼容策略有明确结束条件。

### S8：权重与管理端闭环任务

#### [ ] S8-01 定义归一化配置

- 依赖：S7-03、S7-13。
- 工作：支持越高越好、越低越好、目标区间和不可归一化，参数版本化，缺失值不默认为零。
- 验收：边界、越界、常量样本和缺失值公式测试通过；不使用当前批次 min/max 随意制造排名。

#### [ ] S8-02 实现版本化权重方案

- 依赖：S8-01。
- 工作：由 evaluation 保存 feature、metricVersion、归一化参数、权重、状态、创建人和版本，权重和校验为 100%。
- 验收：停用不影响历史；不兼容指标不能加入；admin 不建权重表。

#### [ ] S8-03 计算并快照版本选择指数

- 依赖：S8-02。
- 工作：任务保存方案快照；保留每项原值、归一化值、权重和贡献值。
- 验收：结果可复算且不覆盖原始指标，名称不再使用准确率或客观质量分。

#### [ ] S8-04 实现不覆盖历史的重新计算

- 依赖：S8-03。
- 工作：另一权重方案产生新的 scoreResultVersion，引用同一原始指标并记录操作者。
- 验收：旧结果不可变，不可比数据拒绝重算。

#### [ ] S8-05 扩展 admin-service 评价代理

- 依赖：S7-14、S8-04。
- 工作：增加功能/版本查询、规模预估、权重、暂停、取消、重试、重算和调用追踪，只做鉴权、校验、转发。
- 验收：所有写操作要求管理员；admin 不直接读取评价或网关数据库。

#### [ ] S8-06 渐进改造现有 EvaluationPage

- 依赖：S8-05。
- 工作：增加功能选择、版本下拉、配置摘要、重复次数、预估调用/费用、权重、进度和控制；移除任意版本文本输入。
- 验收：旧 review 任务仍可查看；高成本任务二次确认；刷新后状态正确。

#### [ ] S8-07 增加原始指标与版本比较视图

- 依赖：S8-06。
- 工作：展示 Token、费用、排队/执行耗时、稳定性、质量依据、权重贡献、口径版本和 callId。
- 验收：缺失/不可比明显；可追踪单次失败；不把选择指数写成准确率。

#### [ ] S8-08 完成管理端端到端验收

- 依赖：S8-07。
- 工作：分别运行小型 REVIEW 和 ASSISTANT 实验，验证暂停、恢复、重试、权重重算和调用追踪。
- 验收：权限、状态、数据一致性通过，批量实验运行时客服仍获得高优先级。

### S9：RAG V2 设计任务，仅设计

#### [ ] S9-01 定义轻量知识目录 manifest

- 依赖：S4-04。
- 工作：只包含目录、文档名、tags、summary、版本和路径白名单，实测完整 manifest Token。
- 验收：不含全文，缺少 summary 有明确处理，不允许模型构造任意路径。

#### [ ] S9-02 设计两阶段 AI 选文流程

- 依赖：S9-01。
- 工作：定义选择 Prompt、最大候选数、受限文档加载、二次裁剪、超时、无候选降级和与向量召回组合点。
- 验收：流程有清晰输入输出和安全边界，不产生运行时代码。

#### [ ] S9-03 设计 V1/V2 固定对比实验

- 依赖：S9-02。
- 工作：定义问题集、召回相关性、答案支持率、延迟、Token、费用和人工标注需求。
- 验收：无人工/真值不宣称准确率，实验能锁定索引版本与目录版本。

#### [ ] S9-04 记录 V2 实施门槛

- 依赖：S9-03。
- 工作：只有 V1 基线完成且对比实验证明增益后才评估实现；在线文件管理和独立 RAG 服务仍排除。
- 验收：正式 RAG 文档有独立 V2 章节，当前 TODO 不含未授权的 V2 代码实现。

### S10：生产工作流动态切换与 REST API 版本任务，远期

#### [ ] S10-01 设计生产版本配置所有权

- 依赖：S6-06、S8-08。
- 工作：业务服务拥有版本和生效配置，admin 只下发命令；定义默认版本、配置版本、操作者、审批和审计。
- 验收：评价服务不直接改生产业务库；工作流切换与 new-api 渠道/模型切换边界清楚。

#### [ ] S10-02 设计安全激活、在途快照和回滚

- 依赖：S10-01。
- 工作：定义预检查、原子激活、缓存失效、旧版本保留、健康观察和一键回滚。
- 验收：在途业务任务使用启动时快照，不会在一次任务中混用两个版本；失败无半生效状态。

#### [ ] S10-03 实现业务服务版本配置与审计

- 依赖：S10-02。
- 工作：先选择一个真实 AI 功能实现版本列表、当前生效项、条件更新和历史审计，再抽取稳定共性。
- 验收：并发切换只有一个成功；旧版本可回滚；没有为了未来场景提前建立中央版本数据库。

#### [ ] S10-04 实现管理端生产版本切换

- 依赖：S10-03。
- 工作：增加强鉴权、版本差异摘要、影响范围、二次确认、切换原因、结果和回滚入口。
- 验收：普通用户不可操作；全部变更可追溯；页面明确区分实验候选版本与生产生效版本。

#### [ ] S10-05 独立评估 REST /v1、/v2 版本化

- 依赖：实际出现外部 API 不兼容变更。
- 工作：盘点 Gateway 路由、Controller、Feign、OpenAPI 和前端迁移，定义并行支持、弃用和下线周期。
- 验收：不使用 REST 版本代替 workflowVersion；没有真实不兼容需求时保持未开始。

## 远期触发条件

RAG V2、生产版本切换和 REST API 版本化已经分别拆为 S9、S10，但只有满足对应依赖和真实需求后才可领取。以下能力暂不拆实现任务：

- 出现第二个真实知识库消费者、在线知识管理、独立扩缩容或权限隔离需求后，评估独立 RAG 服务。
- 单调度器成为可测瓶颈或必须部署多个调度实例后，评估分布式 AI 调度。
- new-api 无法满足渠道治理且出现明确额外业务需求后，评估 LeetModel 多账号资源池。
- 指标具备可信真值且版本选择指数稳定运行后，才讨论自动推荐生产版本；不得自动激活。

## 执行规则

1. 每次只选择一个 D 或 S 编号的任务卡，不直接领取整个阶段。
2. 默认由用户确认任务范围；托管模式下 Agent 按依赖自动领取并在完成后报告代码、测试、文档和未解决风险。
3. 任务依赖未满足时不得用临时硬编码绕过，应明确标记阻塞。
4. 新发现的问题若不阻断当前闭环，只记录到后续任务，不扩大当前任务。
5. 当前 D-01 至 D-04 已完成；继续完成 D-05 后，优先顺序为 S0、S1、S2-01 至 S2-06、S3、S4。
