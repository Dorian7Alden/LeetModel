# LeetModel TODO

> 本文件只管理当前一轮开发任务。这里不是完整功能清单、长期路线图或 idea 池。

## 管理原则

- 同一时间只维护一轮任务，只允许一个明确目标。
- 当前任务必须在独立分支完成，不直接在 `dev` 分支开发。
- TODO 只记录完成当前目标真正需要执行和验收的事项，不罗列未来功能。
- AI 不得主动补全远期规划、潜在需求、优化方向或可能有价值的功能。
- 新想法只有在阻断当前目标时才能加入本轮，其余内容不记录。未来需要时可以重新分析和生成。
- 所有条目完成后，由用户判断本轮是否真正完成。用户确认后才能合并分支并思考下一轮 TODO。
- 新一轮开始时重建本文件，不在上一轮清单后持续追加任务。

AI 很容易快速生成全面的任务清单，但人的注意力、判断力和实践时间有限。这个项目不担心以后不知道做什么，也不担心暂时放弃的 idea 消失。当前更重要的是限制目标，避免因为想得太多、范围太大而拖慢从 0 到 1 的进度。

## 当前任务

> 状态：已完成。公开题库查询、筛选、排序、分页和 Markdown 题面查看闭环已通过验收，用户已确认本轮任务完成。

### 目标

以预置赛事为题库分类核心，完善并验收公开题库查询闭环，使前端能够获取筛选条件、分页查询已发布题目，并查看可直接渲染的 Markdown 题面与可选附件。

### 分支

`feat/problem-public-query`

### 入口

调用方通过网关访问公开题库接口：

- 获取可用赛事
- 获取可用标签
- 分页查询已发布题目

### 主流程

1. 调用方获取预置赛事和可用于筛选的标签。
2. 调用方请求公开题库分页接口。
3. problem-service 强制限定题目状态为已发布。
4. 调用方可以按赛事、年份、题面语言、难度、背景领域、题目类型、模型与算法、历史评审分数区间和标题关键词筛选题目。
5. 背景领域和题目类型分别只能选择一个标签；模型算法允许多选，全部标签与其他筛选条件按 AND 关系组合。
6. 服务返回分页信息、题目摘要和标签名称。
7. 调用方进入题目详情，获取标题、可为空的 Markdown 题面和零个或多个附件。
8. 无匹配题目时返回成功响应和空列表。

### 完成标准

- [x] 明确公开题库列表的请求参数、响应字段和筛选语义
- [x] 国赛、美赛和力模作为基础数据预置，不提供赛事创建、删除和真实比赛生命周期状态
- [x] 题目表使用可为空的 `content_markdown` 保存 Markdown 题面，不再使用 `content_file_id`
- [x] 使用 `problem_attachment` 维护一道题目的零个或多个附件，不再使用 `problem_link`
- [x] 题目管理接口能够维护 Markdown 题面，并完成附件的上传、查询和删除
- [x] 公开题目详情返回 Markdown 题面和附件说明与下载信息
- [x] 公开接口始终只返回已发布题目，客户端传入其他状态不能绕过限制
- [x] 支持按赛事、年份、题面语言、难度、背景领域、题目类型、模型与算法、历史评审分数区间和标题关键词筛选
- [x] 多个筛选条件能够组合使用
- [x] 标签按背景领域、题目类型、模型与算法分类；前两类同类互斥，模型算法允许多选，组合筛选时按 AND 匹配
- [x] 标题关键词为空白时不参与筛选
- [x] 提供公开可访问的预置赛事列表
- [x] 提供公开可访问的标签列表
- [x] 分页结果包含正确的 `total`、`page`、`size` 和 `rows`
- [x] 支持按年份、难度和平均分升序或降序排列
- [x] 列表中的题目赛事和标签采用批量查询，不产生逐题查询
- [x] 补充自动化测试，验证每类筛选条件、组合筛选、空结果和未发布题目隔离
- [x] problem-service 能以项目真实方式启动并完成 Flyway 迁移
- [x] 通过网关发送真实 HTTP 请求，验收无条件查询、组合筛选、空结果和非法参数
- [x] 更新题目服务设计文档，使接口和筛选规则与当前代码一致
- [x] 前端题库列表接入公开筛选项与分页查询接口，支持全部已确认筛选条件
- [x] 前端题目详情渲染经过清理的 Markdown，并展示可选附件
- [x] 通过浏览器完成前端、网关和 problem-service 联调

### 修改范围

- `LeetModel-backend/problem-service/`
- 必要的 `LeetModel-backend/common/common-core/` 对象存储能力调用
- 必要的 `LeetModel-backend/gateway-service/` 公开路径配置
- `LeetModel-frontend/` 题库页面与 API 对接
- 题目服务相关设计文档
- `TODO.md`

### 非目标

- 不实现 Elasticsearch 全文搜索
- 不实现 AI 题目推荐
- 不实现 PDF 到 Markdown 的自动转换
- 不支持 PDF、HTML 或富文本作为并行题面渲染格式
- 不建设通用文件中心、附件预览和附件版本管理
- 不扩展随机选题功能
- 不进行与公开题库查询无关的重构
