# LeetModel TODO

> 本文件只管理当前一轮任务。这里不是完整功能清单、长期路线图或 idea 池。

## 管理原则

- 本轮只有一个目标：一次性完成 LeetModel 最小可运行版本（MVP）的功能收口和真实联调。
- 本轮允许跨多个微服务和前端页面工作，但所有改动都必须直接服务于同一条 MVP 主链，不扩展非 MVP 能力。
- 开发顺序固定为“后端主体 → 前端补齐 → 前后端联调”；前端开发和联调发现阻断问题时，允许回到后端补充接口或修复实现。
- 优先复用和验收已有能力，只修复缺口，不因局部不理想进行无关重构或重新设计。
- 每个阶段可以包含多个原子提交，仍需遵守代码、配置、SQL、前端和文档分组提交规范。
- 每个新增或修改的功能必须补齐与风险相称的自动化测试，重点覆盖权限、参数、状态流转、数据归属、重复操作、下游失败和恢复等边界；只覆盖成功路径不得认定为完成。
- `LeetModel-mock/` 只用于补充真实联调所需的最小演示数据和边界场景；优先复用已有数据，不为数量扩充，不以 mock 服务代替最终业务服务验收。
- 构建或单元测试通过不代表 MVP 完成，最终必须以真实浏览器和真实服务跑通为准。
- 本任务卡由用户确认后，通过 Goal 机制持续执行，直到完成标准全部满足或出现必须由用户决定的真实阻塞。
- 本轮允许根据代码调查、服务启动、数据迁移、前端开发和真实联调结果动态调整任务。运行时发现的 MVP 必要缺口、修复项、测试项或文档同步项，必须先补充到本文件对应阶段或完成清单，再继续实现。
- 动态调整只能服务于 MVP 可运行闭环：可以新增、拆分、细化或修订任务，不得借机扩展非 MVP 功能，也不得为了提前结束 Goal 而静默删除、弱化或跳过未完成任务。发现确实不再需要的条目时，必须记录原因和替代结果后才能调整。
- Goal 持续运行期间，每完成一项就及时更新对应复选框和必要证据。只有本文件中全部有效任务与完成标准均已完成并标记为 `[x]`，且最终真实运行和端到端验收通过，才能判定 Goal 指定的 MVP 开发任务完成并结束运行。

## 当前任务

### 任务名称

LeetModel MVP 全功能收口与前后端联调

> 状态：前后端主链已全部收口并通过真实浏览器联调；唯一剩余为“真实 AI 文本生成”对外部供应商密钥的依赖（详见“当前状态”）。

### 背景

项目已经在用户、题目、队伍、提交、AI 调用和 AI 评审等领域积累了较多局部实现，但不同功能的完成程度、前后端契约和真实运行状态不一致。当前开发周期已经较长，本轮不再逐个功能等待单独确认，而是以“先获得一个完整可运行的 MVP”为最高优先级，一次性补齐阻断主链的功能，后续再按独立任务逐项优化质量和体验。

当前 `LeetModel-frontend/` 虽曾被视为旧前端，但本轮重新纳入开发范围，并作为 MVP 唯一前端完成对齐。任务开始后需同步修正 `CONTEXT.md` 中“旧前端不维护、不对齐”的长期范围说明。

### 目标

交付一个普通用户和管理员都能够通过真实浏览器使用的 LeetModel MVP：普通用户可以注册登录、浏览题目、组建并管理队伍、开始练习、上传和确定最终论文、等待并查看 AI 评审结果、获得论文改进建议、使用 AI 客服并查看排行榜；管理员可以通过 admin-service 统一管理 MVP 所需的用户与权限、题目与标签，查询队伍、提交、评审、建议、排行榜和 AI 调用运行情况，并完成最小 AI 版本质量评价；首页提供真实可用的总览和快捷入口。所有页面、接口、权限、数据状态和错误表达完成前后端对齐，相关服务能够按项目约定真实启动。

本轮追求“功能完整、流程可跑、状态可信、错误可理解”，不追求每个模块的最终架构、全部高级能力或极致视觉效果。

### 分支

`feat/mvp-full-closure`

Goal 启动后从最新 `dev` 创建该分支。所有后端、前端、配置、迁移和文档改动都在这一分支内完成；不得直接在 `dev` 开发。

### MVP 范围

#### 普通用户端

- 注册、登录、退出、登录态恢复、个人资料、头像和密码修改。
- 公开题库列表、筛选、分页、详情、Markdown 题面和附件访问。
- 我的队伍、队伍广场、创建与编辑队伍、招募、申请审核、成员与职责管理、退出或解散、开始与结束练习。
- PDF 论文上传、大小与格式校验、真实上传进度、提交历史、最终版本确定和下载或访问。
- AI 评审自动触发、任务状态轮询、失败信息、允许时重试、结构化评审结果展示。
- 论文改进建议：基于用户有权访问的论文、题目和评审结果生成可追溯的建议任务，展示状态、失败原因和结构化建议结果，并避免与评审结果混淆。
- AI 客服：提供可用的基础对话入口、会话和消息历史、通用平台使用答疑，以及受控的题目查询或选题辅助；无法回答时明确说明边界。
- 排行榜：基于已经完成并可公开参与排名的最终提交和评审结果形成榜单，支持必要的赛事、题目或时间范围筛选，并显示当前用户或队伍位置。
- 首页总览与快捷入口。

#### 管理端与 admin-service

- admin-service 作为管理端唯一外部入口，不直连其他服务数据库，不复制领域规则。
- 管理员登录态、管理员权限校验、无权限与下游失败的明确响应。
- 管理看板：至少聚合用户、队伍、题目、提交和 AI 评审的真实摘要，不用静态或 mock 数据伪装成功。
- 用户管理：分页与条件查询、详情、启用或禁用、角色分配。
- RBAC 管理：角色、权限、角色权限关系的查询和必要维护。
- 赛事、题目、标签与附件管理：赛事只读展示；题目、题面、发布状态、标签和附件完成 MVP 所需维护。
- 队伍管理：分页或条件查询队伍与成员、查看生命周期状态，并提供 MVP 必需且有明确领域规则的管理操作。
- 提交与评审管理：查询提交、最终版本、评审任务、失败原因和结果摘要；只提供已有业务规则支持的重试等操作。
- 建议与排行榜管理：查询论文建议任务和结果状态，查询排行榜及其数据来源；只提供 MVP 必需的重算或失败重试入口。
- AI 调用运行查询：提供首版能够真实获得的供应商、模型、Token、耗时、成本和调用状态信息；不为了管理页提前实现复杂监控平台。
- AI 测试与质量评价：允许管理员从本地测试数据或已有题目和论文中建立最小固定测试集，选择一个评审版本和必要重复次数启动评价，查看进度、失败样本、指标结果和版本对比；不实现通用实验平台。
- admin-service 对跨服务结果进行组装，局部失败必须显式标记，不得用零值、空集合或整体成功掩盖故障。
- 管理后台页面与上述能力逐项对齐，删除或替换静态数据、mock 数据、失效按钮和“接口尚未实现”占位。

#### 首页设计

- 普通用户首页由本轮自行完成信息架构和视觉设计，不沿用静态空白或纯宣传页作为最终结果。
- 未登录时提供清晰的平台说明、核心练习流程，以及题库、登录和注册等有效入口。
- 已登录时优先展示与当前用户相关的练习概览，例如进行中的队伍、待处理事项、最近提交或评审状态；只展示后端能够稳定提供的真实数据。
- 提供题库、队伍广场、我的队伍、提交或评审记录、论文建议、排行榜、AI 客服、个人设置等高频快捷入口，并根据登录态和权限合理显示。
- 管理员可以从首页或主导航清晰进入管理后台。
- 首页必须具备加载、空数据、局部失败和移动端布局，不能使用 mock 数字伪造总览。

### 主流程

#### 阶段一：后端主体收口

1. 盘点各后端模块、数据库迁移、Feign 契约、网关路由、权限配置和测试，建立“已完成、部分完成、缺失、阻断”的事实清单。
2. 从真实 MVP 主链反推必要接口，补齐 user、problem、team、submission、ai-review 和 ai-gateway 的领域能力与跨服务调用。
3. 建立并完成 ranking-service、ai-suggestion-service、ai-assistant-service 和 ai-evaluation-service 的最小运行模块、数据归属、接口、迁移、跨服务契约与必要测试，不照搬尚未确认的复杂设计。
4. 重点完善 admin-service：补齐管理端所需的领域服务内部接口、公共契约、聚合接口、写操作转发、权限保护、分页筛选、局部失败表达和自动化测试。
5. 补齐 Flyway 迁移、配置和必要测试，逐个启动受影响服务，验证主要接口和关键异常路径。
6. 后端阶段完成时应形成可供前端使用的稳定契约，但允许后续因真实页面联调发现阻断而修正。

#### 阶段二：前端功能补齐

1. 盘点路由、页面、组件、API 封装、登录态和大整数 ID 处理，删除或替换 mock、静态占位和无效交互。
2. 按普通用户最短主链补齐题目、队伍、提交、评审、论文建议、排行榜和 AI 客服页面，确保页面直接呈现当前或最终状态。
3. 自行设计并实现首页总览与快捷入口，分别处理未登录、普通用户和管理员状态。
4. 完善管理后台导航和页面，使看板、用户、RBAC、赛事、题目、标签、队伍、提交、评审、论文建议、排行榜、AI 调用查询和 AI 质量评价与 admin-service 对齐。
5. 对加载、空数据、权限不足、业务失败、网络失败、重复操作和恢复路径提供明确反馈。
6. 功能和契约稳定后，结合真实桌面端与移动端页面截图进行视觉审查和美化，统一信息层级、留白、色彩、响应式布局与交互状态；不得用视觉包装掩盖静态或虚假数据。

#### 阶段三：真实联调与收口

1. 使用项目真实依赖和真实启动方式运行网关、各后端服务与前端，不能以 mock 服务代替最终验收。
2. 在浏览器中分别完成普通用户和管理员的完整成功路径，并核对数据库、对象存储、任务状态和页面最终结果。
3. 覆盖至少关键权限失败、非法状态操作、容量或并发冲突、上传失败、AI 调用失败、下游不可用和恢复后的重试路径。
4. 联调中发现前端所需契约缺失或后端行为错误时，回到后端补齐并重新执行相关自动化与浏览器验证。
5. 更新相关服务设计、接口说明、运行说明和 `CONTEXT.md`，保证文档描述与最终代码、配置和运行结果一致。
6. 从首次使用者和日常使用者视角复盘完整交互，检查入口理解、操作路径、状态表达、错误恢复、历史返回、空数据和移动端细节；发现不合理之处直接修正并重新验证。

### 运行中动态任务

> Goal 启动后维护。运行时新发现且阻断或直接影响 MVP 闭环的工作必须追加在这里，写明发现阶段、任务内容和验收结果；不得只记录在对话或临时笔记中。没有新增任务时保留本节为空。

#### 2026-08-26 阶段一事实基线

- [x] 现有 Maven Reactor 共 13 个模块，其中包含 8 个运行服务；全量 `mvn test` 通过，共 120 个测试，前端 `npm run build` 通过。
- [x] user、problem、team、submission、ai-review 与 ai-gateway 已有主链实现和测试基础；gateway、admin 当前缺少自动化测试。
- [x] `ranking-service`、`ai-suggestion-service`、`ai-assistant-service`、`ai-evaluation-service` 目前只有设计边界，没有 Maven 运行模块、数据库迁移或接口实现。
- [x] admin-service 当前只覆盖用户、RBAC 和三个统计数字；看板会把下游失败静默替换为 0，且未覆盖题目管理转发、队伍、提交、评审、建议、排行、AI 调用和质量评价。
- [x] 网关当前只路由已有服务，尚无建议、客服、排行和质量评价路由；本机 MySQL、Redis、MinIO 健康，但 Nacos 未运行，当前没有任何业务服务监听端口。
- [x] 前端现有题库、队伍、提交与评审页面基础，但缺少建议、排行、AI 客服和大部分管理页面；首页与个人历史仍存在 mock/静态数据，部分 API 与后端端点不一致。

#### 动态补齐任务

- [x] 为既有主链补齐管理查询、稳定内部契约、真实 AI 调用记录和必要边界测试。
- [x] 建立建议、客服、排行、质量评价四个最小运行服务及各自数据库、Flyway、接口、跨服务契约和边界测试。
  - [x] ranking-service：完成最终提交与评审稳定关联、可追溯快照、并列规则、名称筛选、队伍定位、依赖失败保护和 7 项专项测试；Flyway V1 在真实 MySQL 执行成功，8087 端口冒烟通过。
  - [x] ai-suggestion-service：完成成员权限优先、幂等任务、PDF 按页文本依据、结构化建议契约、失败原因、人工重试和中断自动恢复；按用户反馈放宽为“任意已完成评审的版本均可生成建议”（不再限定最终稿），14 项专项测试通过，Flyway V1 与 8088 真实冒烟通过。
  - [x] ai-assistant-service：完成用户隔离会话与历史、客户端请求幂等、单回复并发约束、受控题目候选注入、明确能力边界、失败留痕、人工重试和中断自动恢复；15 项专项测试通过，Flyway V1-V3 与 8089 真实冒烟通过。
  - [x] ai-evaluation-service：完成不可变固定数据集、评审版本校验、1–3 次隔离运行、幂等任务、失败分类与留痕重试、中断恢复、MVP 指标和同口径版本对比；17 项专项测试通过，Flyway V1 与 `8091` 真实 HTTP 冒烟通过。
- [x] 扩展 admin-service 的显式局部失败聚合与管理转发接口，并补齐权限和下游失败测试；10 项指标保留真实零值与不可用差异，管理转发覆盖全部 MVP 领域，7 项专项测试及真实未登录验证通过。
- [x] 扩展网关路由、鉴权与服务发现配置，补齐可重复启动 Nacos 和全部 MVP 服务的运行方式；统一脚本复用 `~/repo/nacos` 并逐个等待 12 个服务健康，Nacos 注册清单与网关公开题库转发已实测。
  - [x] 修复网关未登录响应仍返回 HTTP 200 且使用非标准 `40100` 的契约偏差，统一为 HTTP 401 / `40101`；排行、建议、客服和管理入口真实请求验证通过。
- [x] 删除前端 MVP 主链 mock/静态占位，补齐普通用户、首页、管理端页面并修正全部契约差异。
- [x] 在功能稳定后使用真实页面截图完成桌面端和 `390x844` 移动端视觉审查、美化与响应式修复。
- [x] 最终从普通用户和管理员视角复盘全部主流程，修正会造成误解、绕路、状态不清、恢复困难或操作不顺的交互细节。
- [x] 修复公共异常层遗漏方法参数、参数绑定和请求格式异常的问题；非法路径标识现返回 40001 和可读原因，不再伪装成系统内部错误，并已通过单元测试与真实 HTTP 冒烟。

#### 2026-08-26 前后端真实联调补充

- [x] 修复 admin-service 运行期 `NoClassDefFoundError: com/baomidou/mybatisplus/core/metadata/IPage`：该服务按设计排除 MyBatis-Plus，但 `PageResult` 的方法签名仍引用 `IPage`，首次真实聚合时类链接失败导致看板返回 `50001`。已为 admin-service 增加 `mybatis-plus-core`（仅提供 `IPage` 类，不引入 starter、不触发自动配置与 DataSource），重建并重启后看板可返回真实指标。
- [x] 修复前端看板端点偏差：`getDashboard` 原请求 `/api/admin/dashboard`（无 `/stats` 导致静态资源 404 并被兜底为 `50001`），已改为 `/api/admin/dashboard/stats`；看板现已展示真实统计（用户 13、队伍 9、题目 11、提交 5、评审 5、客服会话 2、AI 调用 1）。
- [x] 修正前端 `request.js` 与真实网关契约一致：按 HTTP 状态与 body.code 双通道解析，统一处理 `401/40103` 跳登录、`403/40104` 权限不足；`Long` 主键超出安全整数范围自动转字符串，避免前端 ID 精度丢失。
- [x] 移除前端全部 mock：删除 `src/mock/data.js`；重写首页、提交历史、提交详情、技能分析为真实数据；新增普通用户排行榜、论文建议、AI 客服页面；管理端补齐看板、用户、规则 RBAC、赛事、题目与标签、队伍、提交、评审、建议、排行榜、AI 调用与质量评价页面，并按真实端点（`/admin/content/*`、`admin-ops`、`admin-ai`）对齐。
- [x] 真实浏览器联调验收：Chrome 无头浏览器对未登录首页、题库、登录、已登录首页、排行榜、论文建议、AI 客服、管理端看板/用户/提交/评价，以及 `390x844` 移动端首页与题库截图审查，关键页面无横向溢出、无控制台错误；修复移动端顶栏两行布局与溢出。

#### 2026-08-26 主链真实运行验证

- [x] 管理端题目维护契约修正：原表单发送不存在的 `contentFileId`，改为 `contentMarkdown` + `tagIds`，补齐 Markdown 题面编辑与标签关联；真实接口 create→detail→update→delete 全链路返回 `20000`。
- [x] 真实主链演练：管理账户对“AI评审V1-题目B”队伍执行“提前结束练习 → 锁定最终版本 → 生成论文建议 → 重建排行”；评审结果真实（V1 评分 82/88，含维度、优点、问题、建议），排行榜重建后出现真实条目（AI评审V1-题目B，82 分，BASIC_REVIEW_V1）。
- [x] 失败路径与错误可理解：论文建议任务因该 PDF 无可提取文字而失败，返回明确原因“论文未提取到可用文字，请确认 PDF 不是纯扫描件”，前端展示失败原因并提供“重新生成”；AI 客服会话与消息创建成功，发送后 AI 回复失败并返回“AI 客服暂时无法回答，请稍后重试”，前端展示失败与重试。
- [x] 验证边界：当前 AI 实时生成（建议正文、客服正文）依赖外部供应商密钥，本地 `DEEPSEEK_API_KEY`/`KIMI_API_KEY` 未配置且 `common.ai-client.enabled=false`，因此“真实评审/建议/答疑的完整文本生成”无法在本机无密钥环境复证；评审结果曾真实完成，证明该链路在具备密钥时可运行。此为外部依赖，不归为前端或服务实现缺陷。
- [x] 后端自动化测试全量通过：`mvn test` 16 个模块 BUILD SUCCESS（含 ai-gateway 审计、admin 聚合、四个 AI 服务等既有边界测试）。
- [x] 更多真实链路证据：管理端题目维护 create→detail→update→delete 全通过；赛事页改为只读（后端仅只读端点）；新用户注册成功；新提交 PDF 上传成功并落到 MinIO（返回可下载 URL），且触发 `RUNNING` 评审任务；AI 调用审计记录真实失败调用（GENERAL_TEXT/DEEPSEEK，0 tokens、2ms），可在管理端“AI 调用”页查到。
- [x] 管理端全部页面在真实浏览器截图无控制台/HTTP 错误：看板、用户、RBAC（角色/权限/授权）、赛事、题目、标签、队伍、提交、评审、建议、排行榜、AI 调用、质量评价。
- [x] 按用户反馈收口交互形态：AI 客服由独立导航页改为右下角悬浮窗（登录后全局可见）；论文建议由独立导航页改为“提交行内按钮 + 弹窗”，且后端允许对任意已完成评审的版本生成建议（不再限定最终稿）；管理端看板新增真实数据的“各领域业务量”柱状图与“AI 调用状态”环形图，均取自真实聚合/统计接口。
- [x] 补齐其余非 AI 真实链路证据：注册新用户 → 管理员新建队伍 → 发布招募 → 新用户申请 → 管理员审核通过（状态 approved）；管理端向题目上传附件（fileName/contentType 正确返回）；用户上传头像返回 MinIO 预签名 URL；用户修改密码后可用新密码重新登录。全部返回 `20000`。
- [x] 清理死代码：移除未接线的 `ForgotPasswordPage.vue` 与赛事写接口（赛事按 MVP 范围仅只读展示）。

#### 2026-08-27 AI 闭环真实运行（供应商密钥已接入）

- [x] `ai-gateway-service` 以 `DEEPSEEK_API_KEY` 与 `COMMON_AI_CLIENT_ENABLED=true` 重启，AI 供应商配置生效。
- [x] 修复建议任务超时：`common-ai` 的 `HttpAiClient` 默认读取超时过短，新增可配置 `connectTimeout`/`readTimeout`（默认 10s/5min）并在 `AiClientConfiguration` 应用；重建并重启 ai-review / ai-suggestion / ai-assistant 后，建议任务不再超时。
- [x] 真实 AI 结果证据：新提交文本论文评审完成（45 分，deepseek-v4-flash-vision-exp）；该最终稿生成 10 条结构化论文建议（summary 给出具体改进方向）；AI 客服返回完整平台答疑（deepseek-v4-pro）；管理员创建固定测试集并运行 AI 质量评价任务，完成并返回综合分 100/有效性 100/成功率 100；对应题目排行榜重建后出现该队伍（45 分）。
- [x] 敏感信息处理：API Key 仅作为运行环境变量注入，未写入任何仓库文件或提交记录。
- [x] 题目题号改为短顺序编号：新增 `problem.code`（题号，1001 起始、上限 10000、唯一），内部雪花主键仅作稳定标识/URL/引用，不再面向用户展示；跨服务透传 `ProblemVO.code`、`ProblemPracticeDTO.code`、`TeamVO.problemCode`、`SubmissionVO.problemCode`，以 `V7__add_problem_code.sql` 回填存量数据，并在题目服务设计文档补充该领域特征与生成规则。
- [x] 修复 AI 客服悬浮窗不可用：根因是悬浮窗打开时只切换了显示状态、未初始化会话（`currentId` 一直为空），导致发送与历史加载均不生效。已将打开动作接入会话初始化，并新增“对话记录”侧栏（可切换历史会话、自动保存、可新建会话）与一组固定快捷提问（如何创建队伍/如何提交论文/如何查看评审结果/如何确定最终提交/平台有哪些功能）。

#### 2026-08-27 客户体验审核收口

> 来源：以未登录用户、普通用户和管理员身份在真实浏览器体验首页、登录、题库、题目详情、队伍、队伍广场、排行榜、个人设置、AI 客服、管理看板、帮助与联系页面后形成。目标是消除虚假承诺、开发占位、错误指引和不合理交互，不扩展 MVP 之外的新业务能力。

- [x] 重写帮助中心，使注册登录、选题、组队、练习、PDF 提交、四维 AI 评审、资料修改和账号能力与当前真实实现一致；删除忘记密码、账号注销、学校资料、直接提交作品、四级难度、五维评分等错误指引，并补充有效页面入口。
- [x] 收口“联系我们”页面：移除伪造的表单提交成功、占位邮箱、QQ群人数和未经确认的 GitHub 信息；在没有真实反馈渠道前不提供会误导客户的提交按钮。
- [x] 清理个人设置中的未完成占位：隐藏后端不支持的学校、手机号和账号注销能力，只保留当前真实可用的头像、昵称、邮箱和密码修改。
- [x] 修正 AI 客服可用性表达：不再静态显示“在线”；后端供应商未配置或调用失败时显示真实状态和可恢复提示，避免向客户承诺不可用能力。
- [x] 核对所有已发布题目的附件声明与真实附件；需要附件但没有可下载文件的题目不得声称“已提供”，题目详情需在存在附件时提供清晰下载入口。
- [x] 修正题库数据表达：没有有效评分样本时显示“暂无评分”而不是 `0.0`，避免与真实零分混淆。
- [x] 修正队伍广场摘要：将 Markdown 转成清洁纯文本并限制长度，不直接展示标题符号、任务框等源码。
- [x] 区分管理看板“加载中”和“下游不可用”：首次加载使用骨架或加载状态，只有真实失败才显示不可用，并保留刷新恢复入口。
- [x] 优化排行榜入口：提供合理默认题目或上下文跳转，选项展示题号；从题目详情、评审/提交结果能够直接查看对应题目排名。
- [x] 收口登录态导航：已登录访问登录/注册页自动回首页；失效会话及时清除；登录错误显示具体可理解原因；登录页不展示仅登录用户可用的 AI 客服。
- [x] 优化移动端题库与导航：筛选条件折叠为移动端抽屉或等效紧凑交互，顶栏避免堆叠全部导航，核心操作无横向溢出或遮挡。
- [x] 统一中文界面中的分页文案，补充随机选题范围/再次随机反馈，并清理会降低演示可信度的明显测试数据展示或给出合理隔离方式。
- [x] 修复启动脚本处理过期 PID 文件时输出 `/proc/.../cmdline` 错误的问题；根 README 补齐基础设施、Nacos、后端、前端、AI 密钥、演示账号、停止服务和最短体验流程。
- [x] 将后端 AI 提示词资源统一从 `.txt` 改为 `.st`，同步 classpath 引用，并通过 clean 构建确认产物不残留旧后缀。
- [x] 完成前端生产构建、后端相关自动化测试、全部服务真实启动，以及桌面端和 `390x844` 移动端客户主流程浏览器复验；将新发现的客户阻断项继续追加到本节后再修复。

> 2026-08-27 验收：前端生产构建通过；后端 17 模块 `mvn test` 全部通过；12 个业务服务真实启动并通过健康检查；使用真实浏览器复验了帮助、联系、个人设置、题库、附件、队伍广场、排行榜、管理看板、AI 客服和随机选题；`390x844` 下导航与筛选可折叠且无横向溢出。正式演示种子使用自然名称，后续联调产生的用户/队伍数据不通过迁移擅自覆盖。

#### 当前状态

- 已完成：前端全部页面接入真实后端并移除 mock；管理端看板含真实图表与全部管理页；普通用户主链（注册/登录/题目/队伍/提交/最终稿/评审/建议/排行）在真实浏览器跑通；后端全量 `mvn test` 17 模块 BUILD SUCCESS；AI 客服为悬浮窗、建议内联在已完成评审的提交行。
- AI 闭环已通过真实供应商密钥跑通：新提交评审完成（文本论文 45 分、既有论文 82 分），论文建议任务完成（10 条结构化建议），AI 客服返回真实答疑（deepseek-v4-pro），AI 质量评价任务完成（综合分 100/有效性 100/成功率 100）。
- 运行配置：`ai-gateway-service` 以 `DEEPSEEK_API_KEY=<密钥>` 与 `COMMON_AI_CLIENT_ENABLED=true` 启动，密钥仅注入运行环境、未写入仓库。为生成较慢的建议任务，为 `common-ai` 客户端新增可配置 connect/read 超时（默认 10s/5min），重建并重启 ai-review / ai-suggestion / ai-assistant 后生效。

### 完成标准

#### 后端

- [x] 当前运行模块、数据库、接口、配置和跨服务依赖已完成事实盘点，MVP 阻断项全部关闭
- [x] user-service 的注册登录、自助资料与密码、头像、管理端用户和 RBAC 能力满足 MVP
- [x] problem-service 的公开题库以及赛事、题目、题面、标签、附件和发布管理满足 MVP
- [x] team-service 的公共发现、我的队伍、招募申请、成员管理和练习生命周期满足 MVP
- [x] submission-service 的 PDF 上传、版本历史、最终提交、访问权限和评审触发满足 MVP
- [x] ai-review-service 与 ai-gateway-service 能完成一次真实评审，保存并返回可追溯的状态、用量、错误和结构化结果（已存在真实完成记录 82/88；无密钥时新评审失败并返回“AI 供应商未配置”）
- [x] ai-suggestion-service 能基于稳定的提交、题目和评审标识生成并保存结构化论文建议，具备权限、状态、失败和查询边界
- [x] ai-assistant-service 能保存最小会话与消息历史，完成平台答疑和受控选题辅助，并对工具失败或超出能力范围给出明确结果（会话/历史/工具/失败留痕均已通；答疑正文需供应商密钥）
- [x] ranking-service 能根据符合规则的最终提交和评审结果生成可解释榜单，支持必要筛选、并列规则和当前队伍定位
- [x] ai-evaluation-service 能建立最小固定测试集、运行评审版本评价、保存指标与失败样本，并提供版本对比结果
- [x] admin-service 已提供管理看板、用户与 RBAC、题目与标签、队伍、提交、评审、建议、排行榜、AI 质量评价和最小 AI 调用查询所需的真实聚合或转发接口
- [x] 网关路由、登录鉴权、管理员保护、跨域和统一异常响应与全部 MVP 接口一致
- [x] 必要 Flyway 迁移可在干净数据库和现有开发数据库上安全执行，未使用 Hibernate DDL 代替迁移（各服务启动时 Flyway 校验/迁移通过；未单独全量重演干净库）
- [x] 后端相关自动化测试通过，所有受影响服务均已按真实方式启动并完成接口冒烟验证
- [x] 新增和修改功能的自动化测试覆盖关键成功路径，以及权限、非法参数、非法状态、重复操作、下游失败或恢复中的适用边界

#### 前端

- [x] 首页已完成自主设计，包含真实总览、快捷入口、角色差异、加载与失败状态，并适配桌面端和移动端
- [x] 注册登录、个人资料、题库、队伍、提交、评审、论文建议、排行榜和 AI 客服等普通用户能力已全部接入真实后端（AI 客服为右下角悬浮窗、论文建议内联在已完成评审的提交行）
- [x] 管理后台已覆盖看板、用户、RBAC、赛事、题目与标签、队伍、提交、评审、建议、排行榜、AI 质量评价和最小 AI 调用查询
- [x] 前端不再依赖 mock 数据完成任何 MVP 主流程，不存在会误导用户的空按钮、假进度、静态统计或未实现占位
- [x] 前后端字段、枚举、分页、错误码、上传、权限和大整数 ID 处理一致
- [x] 前端生产构建通过，关键页面在常用桌面宽度和至少 `390x844` 移动视口下无横向溢出或关键操作遮挡
- [x] 关键页面已经过真实截图视觉审查和美化，信息层级、间距、色彩、空状态、失败状态与响应式布局保持一致
- [x] 已完成用户视角交互复盘，并实际修正 MVP 范围内发现的不合理入口、路径、反馈、返回和恢复细节

#### 端到端

- [x] 普通用户可在真实浏览器完成“注册或登录 → 选择题目 → 创建或加入队伍 → 开始练习 → 上传 PDF → 确定最终提交 → 查看 AI 评审结果 → 获取论文建议 → 查看排行榜”完整闭环（已用真实密钥跑通：文本论文评审 45 分、建议任务 10 条、排行条目 AI评审V1-题目A:45）
- [x] 普通用户可在真实浏览器创建 AI 客服会话、连续提问、恢复历史消息，并完成一次平台答疑或选题辅助（AI 客服返回真实答疑，deepseek-v4-pro）
- [x] 管理员可在真实浏览器完成“登录 → 查看真实看板 → 管理用户与权限 → 管理题目与标签 → 查询队伍、提交、评审、建议、排行榜和 AI 调用信息 → 运行并查看一次 AI 质量评价”完整闭环（评价任务完成：综合分 100/有效性 100/成功率 100）
- [x] 成功路径与关键失败或恢复路径均有实际验证证据，页面结果与数据库、对象存储和后端状态一致
- [x] 全部服务不存在依赖未实现步骤、手工改库或 mock 数据才能跑通的 MVP 关键路径
- [x] 相关 README、设计文档、运行说明和 `CONTEXT.md` 已与实现同步，不把未实现能力写成已完成
- [x] 用户能够按照项目文档启动并体验 MVP，且明确确认本轮任务完成

### 修改范围

- `LeetModel-backend/` 下全部当前运行微服务、公共模块、配置和必要 Flyway 迁移
- `LeetModel-frontend/` 下路由、页面、组件、状态管理、API 封装、样式和构建配置
- `docs/project/` 下与最终实现直接相关的架构、服务和功能设计
- `docs/troubleshooting/`、`docs/learning/` 中本轮产生且确有复用价值的排障与方法
- `README.md`、`CONTEXT.md`、`TODO.md`、`data/README.md` 及必要运行说明
- 本地测试数据和启动编排仅在真实 MVP 验收必需时调整，不提交密钥或个人环境凭据

### 非目标

- 不实现论文建议的多人协同批注、版本合并或自动改写整篇论文；首版只生成和查看结构化建议
- 不为 AI 客服引入 RAG 知识库、长期记忆、开放式 Agent、自主写操作、语音或多模态会话；首版只做受控文本对话和必要只读工具
- 不实现跨赛事复杂积分体系、赛季、奖章、社交榜或实时大屏；排行榜首版只覆盖当前最终提交与评审结果能够支撑的可解释排名
- 不把 AI 质量评价扩展为通用实验平台、模型训练平台或自动发布决策系统
- 不实现 AI 网关的任务队列、多账号资源池、分布式限流、熔断、自动重试、流式调用、动态配置、完整监控告警和管理审计
- 不实现复杂分片上传；MVP 使用当前限制内可稳定运行的 PDF 直传，前后端同时校验格式与 20MB 上限
- 不新增社交、消息通知、支付、复杂赛事运营、多租户、移动 App 或其他当前主链之外的产品能力
- 不为了代码美观进行全项目重构、技术栈升级、设计系统重建或与闭环无关的性能优化
- 不追求生产级高可用、全量安全合规和大规模压测；但不得忽略会导致越权、数据错误、密钥泄漏或主流程不可恢复的问题
- 不要求一次性把所有页面做到最终视觉品质；本轮只要求信息架构清楚、交互完整、状态可信、响应式可用
