# LeetModel TODO

> 本文件只保存当前任务、候选任务和满足条件后才启动的事项。已完成任务和阶段过程不在此保留；已经确认的长期边界与后续触发条件保留在本文件，具体设计和实现结论以 `docs/` 为准。

## 使用约定

- `[ ]` 表示待开始，`[~]` 表示进行中，`[!]` 表示被外部条件阻塞。
- 每次开始前阅读根 `AGENTS.md`、`README.md`、本文件、相关规范、目标模块 README 和任务指向的设计文档。
- 修改公共契约前检查全部生产者和消费者；数据库变更只新增 Flyway 迁移。
- 任务完成至少同步受影响的正式文档并运行目标模块测试；公共模块变化时验证直接消费者。
- 不在配置、日志、测试夹具或文档中保存真实密钥、Relay Token、Prompt、回答正文、知识片段或论文内容。
- new-api Relay Token 由 `ai-gateway-service` 的运行环境提供；具体脱敏和模型复核规则见 [AI 网关测试与验收](docs/project/03-微服务设计/ai-gateway-service/22-测试与验收.md)。
- 模型选型约定：将 New-API 提供的 `gemini-3.8-flash-high` 调整为平台全局首选使用的模型（涵盖客服对话、题目推荐、论文建议、评审及功能测试联调）。
- `cli-proxy-api` 是长期运行且不属于本项目的本地 Docker 服务，固定占用宿主机 `8085`；不得停止、重启、改端口或修改其配置。LeetModel 本地 submission-service 使用 `8092`，启动与验收必须避开该容器。
- 可以自主创建本地阶段分支、执行任务卡原子 commit 和阶段 merge；未经用户明确授权，不执行 push、force push、rebase、破坏性 reset、改写历史、远端分支操作或删除用户文件。

## 当前状态

当前分支 `phase/suggestion-v3-upgrade`，推进 AI 论文建议功能演进（对标 AI 评审 V3 双阶段流水线理念，重构为“动态任务规划 -> 按需精准 RAG 切片 -> 分任务并发推演 -> 汇总 AI 排序”架构，全面重构与打磨评审与建议的专家视角提示词矩阵）。

## 当前任务

暂无待推进任务。当前阶段处于方案规划与任务卡设计阶段，待用户确认后进入任务卡实施。

---

## 阶段规划：AI 论文建议 V3 架构演进与提示词重构（Suggestion-V3-Upgrade）

> 模型选型说明：功能测试、联调验证与生产默认统一使用 New-API 提供的 `gemini-3.8-flash-high`。

### 阶段一：专家评委思维建模与双向提示词规范（顶层设计与规范基准）
- [ ] 任务 1.1：数学建模专家评委思维建模与双向提示词工程设计规范
  - 文档设计：在 `docs/project/03-微服务设计/` 下制定《数模专家评委认知框架与双向提示词设计规范》。
  - 思维建模：系统化提炼评委专家“审什么、疑什么、盼什么”的三层思维模型（合规底线审查、科学自洽性验证、升华提分点审视）；沉淀数模常见失分陷阱（套模板、无量纲、假收敛、缺基准、弱假设、伪检验等）；统一评审与建议共享的标准及反空泛引导规则。
  - 规范自测：对照 `docs/learning/提示词管理.md` 与 `rag_kb` 阅卷标准，静态走查规范的一致性与完备性，形成提示词设计自查清单。

### 阶段二：AI 评审 V3 提示词专家视角重构与验证（上游输入源打磨）
- [ ] 任务 2.1：AI 评审 V3 核心提示词矩阵专家视角重构与回归测试
  - 文档设计：修订 `docs/project/03-微服务设计/ai-review-service/AI评审V3/12-提示词工程与防御性解析规范.md` 及提示词设计文档。
  - 功能开发：在 `ai-review-service` 中重构三大核心提示词（`phase1-structural-review.st` 初筛排雷、`phase2-task-planner.st` 命题目标拆解、`phase2-subtask-evaluation.st` 深度推导自洽性审查），消除死板八股教条，注入引导思考链。
  - 功能测试：执行 `ai-review-service` 现有单测套件；基于 `gemini-3.8-flash-high` 针对 `data/` 样本论文进行评审冒烟测试，验证评分理由与缺陷发现的针对性。

### 阶段三：AI 建议 V3 架构、产物契约与按需精准 RAG 设计（系统设计与检索接入）
- [ ] 任务 3.1：AI 论文建议 V3 动态流水线与 Markdown 富文本产物契约设计
  - 文档设计：在 `docs/project/03-微服务设计/ai-suggestion-service/` 下沉淀 V3 架构设计文档，确定“建议规划算子 -> 任务级按需精准 RAG + 上下文切片装配 -> 并发子任务生成 -> 汇总 AI 全局优选排序”的端到端执行流。
  - 契约定义：定义 `GROUNDED_SUGGESTION_V3` 产物 Schema，输出结构化 Markdown 富文本建议正文，定义 `CORRECTION`（改错）与 `ADVANCEMENT`（升华）双轨机制，确立历史版本自动升级补齐策略。
  - 契约审查：完成与前端 Markdown/KaTeX 渲染契约及跨服务调用 DTO 的一致性核验。
- [ ] 任务 3.2：知识库 rag_kb 动态切片检索契约与按需精准 RAG 对接
  - 文档设计：在 `docs/project/03-微服务设计/knowledge-retrieval-service/` 中设计针对数模题型与缺陷维度的按需定向检索协议。
  - 功能开发：在 `knowledge-retrieval-service` 中实现针对建议子任务的按需检索接口（支持优化类、预测类、评价类、统计与机理类等专属知识切片召回）。
  - 功能测试：编写检索服务单测，验证各题型典型关键词下的知识切片召回命中率与格式。

### 阶段四：AI 建议 V3 提示词矩阵研发与防御解析器（Prompt 与算法研发）
- [ ] 任务 4.1：构建 AI 建议 V3 专家教练提示词模板矩阵
  - 文档设计：编写规划算子、分任务教练、汇总 AI 三套模板的设计规范与 Few-shot 样例。
  - 模板编写：编写三套专属建议提示词模板（采用 `[[ ]]` 定界符与专家教练深度引导思考链）：
    - `phase2-suggestion-planner.st`：建议任务动态规划算子（评委视角审视全文弱点与升华空间，派发结构审查、各小题建模推演、敏感性专项等子任务）
    - `phase2-subtask-suggestion.st`：分小问建模深度推演与升华教练（结合局部切片与 RAG，输出包含现状诊断、公式/代码具体修改、基准对比的 Markdown 富文本）
    - `phase3-suggestion-synthesizer.st`：全局汇总与优先级排序 AI 模板（跨小问冲突校验、全局策略综合、P0~P3 优先级统筹）
  - 功能测试：编写 Prompt 模板插值与变量校验单测，验证 `[[ ]]` 占位符在含花括号与 LaTeX 公式环境下的安全替换。
- [ ] 任务 4.2：实现鲁棒输出解析器（V3OutputParser）
  - 文档设计：制定输出解析防御性设计（BOM/控制字符清洗、Markdown 代码围栏剥离、首尾闭包截取、LaTeX 反斜杠容错反序列化）。
  - 功能开发：在 `ai-suggestion-service` 中实现控制字符/BOM 清洗、Markdown 代码围栏剥离、首尾闭包贪婪截取与 LaTeX 反斜杠宽容反序列化。
  - 功能测试：编写针对性单元测试，覆盖纯 JSON、带 ` ```json ` 围栏、前后闲聊文字、含 `\frac` 反斜杠及包含非法控制字符的极端返回用例。

### 阶段五：建议服务工作流重构与核心落地（后端工程落地）
- [ ] 任务 5.1：数据库 Flyway 迁移与工作流版本注册
  - 文档设计：编写数据库升级设计与建议 V3 数据结构映射说明。
  - 功能开发：编写并执行 Flyway 迁移脚本，注册 `GROUNDED_SUGGESTION_V3` 工作流版本与产物 Schema。
  - 功能测试：执行 Flyway 迁移验证测试，校验历史版本与新表约束的向前兼容性。
- [ ] 任务 5.2：历史版本自动晋级补齐机制实现
  - 文档设计：制定历史提交（V1/V2）在准备阶段向 `PAPER_PARSE_V2` + `DEEP_EVIDENCE_REVIEW_V3` 调度的状态机与幂等等待设计。
  - 功能开发：在 `SuggestionService` 准备阶段实现对历史解析与评审的自动补齐与状态轮询/异步等待。
  - 功能测试：编写单测模拟无 V3 资产的历史论文，验证触发补齐、重试等待与超时熔断链路。
- [ ] 任务 5.3：实现 `GroundedSuggestionV3Workflow` 动态编排流水线
  - 文档设计：编写规划算子调度、切片上下文组装、线程池隔离与汇总 AI 编排时序文档。
  - 功能开发：实现任务规划、动态切片构建、子任务线程池并发消费与汇总 AI 优先级统筹。
  - 功能测试：编写完整工作流单元测试，Mock 依赖验证从规划到汇总的端到端 DAG 推进。
- [ ] 任务 5.4：依据链聚合与强校验升级
  - 文档设计：制定针对 Markdown 富文本方案的真实页码、blockId 引用、改错与升华合法性校验规则。
  - 功能开发：实现建议条目聚合器与服务端确定性校验逻辑（去重、排序、依据链有效性验证）。
  - 功能测试：编写校验器边界测试（伪造 blockId、虚假页码、空内容与实质重复建议拦截）。

### 阶段六：端到端全链路验收与文档归档（交付收官）
- [ ] 任务 6.1：基于 `gemini-3.8-flash-high` 的建议端到端生成与全量回归测试
  - 测试设计：制定详细验收测试用例集（选取 `data/` 中典型优化类与预测类测试集论文）。
  - 功能测试：全量微服务启动联调，执行端到端真实验收，比对结构缺失发现、公式级修改建议、升华建议与汇总排序的准确度。
  - 前端走查：在前端浏览器中验证 Markdown 富文本格式渲染、KaTeX 公式显示与代码高亮排版。
- [ ] 任务 6.2：微服务设计与架构文档全量归档同步
  - 文档维护：更新 `docs/project/03-微服务设计/ai-suggestion-service/` 相关文档，记录 V3 架构事实。

---

## 待梳理服务清单（按推荐顺序）

全平台微服务（common、user、team、problem、submission、ranking、gateway、admin、audit、knowledge-retrieval、ai-gateway、ai-suggestion、ai-review、ai-assistant、ai-evaluation）代码注释已全量对齐项目级工程规范。

## 已确认的系统边界

- SkyWalking 是唯一 Trace/APM 实现；Prometheus 是指标与告警权威来源；运行日志不能代替操作审计。
- 业务 `traceId` 必须保留并可在 Trace 采样或遥测后端不可用时回退到结构化日志和业务事实。
- audit-service 只负责中央不可变归档与受信只读查询，不拥有用户、题目、提交、AI 任务、消息任务或生产配置等领域规则。
- 管理端通过受权限保护的服务接口执行取消、暂停、恢复、重放和回滚；Grafana、Prometheus、SkyWalking UI 只读观察。
- 遥测后端故障不得阻塞普通业务主链；审计 Outbox 保留并重试，达到严重水位时高风险治理操作 fail-closed。
- 指标、日志、Trace 和审计均遵守低基数、最小数据和脱敏边界，不保存用户/队伍/提交/任务/调用标识作为 Prometheus 标签。

## 后续触发条件

以下能力暂不拆实现任务；条件成立后先补充任务卡，再进入开发：

- knowledge-retrieval-service 已服务论文建议；客服 RAG 迁移、在线知识管理和独立扩缩容在出现真实消费者或运维需求后再拆任务。
- 单调度器成为可测瓶颈或必须部署多个调度实例后，评估分布式 AI 调度。
- new-api 无法满足渠道治理且出现明确额外业务需求后，评估 LeetModel 多账号资源池。
- 指标具备可信真值且版本选择指数稳定运行后，才讨论自动推荐生产版本；不得自动激活。
- VIP 高级模型分层属于候选能力。进入设计前必须先确认普通与 VIP 在评审、建议、客服三个功能上的模型选择权、配额与计费、降级规则、运行快照和越权防护；未形成统一模型路由契约前不在各业务前端分别硬编码模型。

## 执行规则

1. 每次只选择一个编号任务卡，不直接领取整个阶段。
2. 默认由用户确认任务范围；托管模式下 Agent 按已经确认的路线图和依赖串行推进。
3. 任务依赖未满足时不得用临时硬编码绕过，应明确标记阻塞。
4. 新发现的问题若不阻断当前闭环，只记录为候选或条件任务，不扩大当前任务。
5. 任务完成后，只把后续仍有效的决策与边界合并到所属正式文档，然后删除已完成任务卡；不保存完成历史或单独的阶段过程归档。
