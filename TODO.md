# LeetModel TODO

> 本文件只管理当前一轮开发任务。这里不是完整功能清单、长期路线图或 idea 池。

## 管理原则

- 同一时间只维护一轮任务，只允许一个明确目标。
- 当前任务必须在独立分支完成，不直接在 `dev` 分支开发。
- TODO 只记录完成当前目标真正需要执行和验收的事项，不罗列未来功能。
- AI 不得主动补全远期规划、潜在需求、优化方向或可能有价值的功能。
- 新想法只有在阻断当前目标时才能加入本轮，其余内容不记录。未来需要时可以重新分析和生成。
- 所有条目完成后，由用户判断本轮是否真正完成。用户确认后才能合并分支并思考下一轮 TODO。
- 新一轮开始时重建本文件，不在上一轮清单后持续追加任务。

AI 很容易快速生成全面的任务清单，但人的注意力、判断力和实践时间有限。这个项目不担心以后不知道做什么，也不担心暂时放弃的 idea 消失。当前更重要的是限制目标，避免因为想得太多、范围太大而拖慢从 0 到 1 的进度。

## 后续开发指导思想

当前阶段先完成面向普通用户的核心业务能力，不让管理端、监控和服务治理抢占用户主链路的开发资源。

1. 先完成能够真实运行的基础 AI 评审，使用户提交论文后能够获得并查看评审结果。
2. 基础版本稳定后，再按独立任务逐个开发后续 AI 评审版本，不在 V1 中提前建设多版本工作流。
3. 用户侧 AI 评审能力形成后完成排行榜，使“选题、组队、提交、评审、排行”主链路闭环。
4. 用户主链路完成后再完善 admin 端，用于管理和观察已经存在的真实业务能力。
5. 在基础功能得到真实使用和验收后，再根据实际问题建设 AI 质量评价、调用监控、成本治理、稳定性治理和其他服务端优化。

这段顺序只用于约束开发优先级。每一轮仍然只建立一个任务卡，不根据本节同时开发多个阶段。

## 当前任务

> 状态：V1 AI 评审基础功能、真实浏览器验收和三协议供应商适配均已完成，等待用户最终确认。

### 目标

完善 `BASIC_REVIEW_V1` 基础 AI 评审闭环。队伍成员提交一份 PDF 论文后，系统通过 AI 网关使用已验证可用的 DeepSeek 多模态模型完成一次真实评审，并让队伍成员在用户页面查看可恢复的任务状态、总分和基础评语。

### 分支

`feat/ai-review-v1`

### 入口

已进入练习阶段且具有提交权限的队伍成员，从“我的队伍”进入队伍详情并提交 PDF。提交成功后能够进入对应提交的评审详情，其他队伍成员也能够从队伍提交记录查看同一评审结果。

### 模型与输入协议

- V1 使用 DeepSeek 官方实验模型 `deepseek-v4-flash-vision-exp`。官方首页和 Vision 指南已确认该模型支持文本与图像输入。
- 模型当前接受 JPEG、PNG、GIF 和 WebP，不直接接受 PDF。review-service 必须将原始 PDF 按页渲染成图像，再通过 AI 网关发送有序页面图像和评审指令。
- V1 优先采用适合本地私有论文文件的 Base64 图像或 Files API，不把 MinIO 对象临时开放为公共 URL。最终方式根据真实 PDF 的页数、渲染体积和官方限制在实现前确定。
- 实验模型和接口能力可能变化。开发与真实验收时仍须通过当前账号确认模型可用，并使用不包含敏感信息的最小页面图像验证调用协议。
- 若当前账号无法调用该模型，或真实论文转换后超出官方请求限制，停止实现并回到任务卡确认输入方案，不擅自改用 PDF 文本抽取或其他模型。

### 主流程

1. 队伍成员在练习有效期内提交一份符合限制的 PDF 论文。
2. submission-service 保存提交记录和文件后，幂等创建 `BASIC_REVIEW_V1` 评审任务。
3. review-service 领取等待任务，锁定提交快照和评审版本，将原始 PDF 按页渲染为有序图像，并通过统一多模态协议交给 AI 网关。
4. AI 网关作为唯一外部模型出口读取 DeepSeek 密钥并发起调用，review-service 不保存或直接使用供应商密钥。
5. 模型按 V1 固定 Prompt 返回结构化结果；review-service 校验总分范围和必要字段后保存任务结果。
6. 用户页面展示等待、评审中、已完成和失败状态。页面刷新后重新查询服务端状态，不依赖前端临时状态判断结果。
7. 评审完成后展示 `[0,100]` 总分、总结和基础维度评语；评审失败时展示可理解的失败信息，并允许队伍成员重试失败任务。

### V1 输出边界

- 所有版本完成后必须输出同名、同类型和同语义的 `score`，取值为 `[0,100]` 范围内的十进制数。
- V1 的一次模型调用还必须输出总体总结以及假设合理性、建模创造性、结果正确性和表达清晰性四个维度的分数与评语。
- V1 可以输出主要优点、主要问题和改进建议，但这些属于 V1 专属评语结构，不是所有版本必须保持一致的公共字段。
- 后续版本可以拆分多个小任务、执行多轮对话并产生不同的中间 JSON，最终必须汇总为统一 `score`。
- 所有 AI 原始输出和中间输出都必须是 JSON，并由所属版本校验；前端不直接解释供应商原始响应。
- V1 是固定工作流版本。更换模型或 Prompt 导致评审语义发生实质变化时必须建立新版本，不能覆盖历史任务。

### 完成标准

- [x] 使用当前账号和最小页面图像真实调用 `deepseek-v4-flash-vision-exp`，确认实验模型可用及输入协议有效
- [x] PDF 能够按页渲染为顺序稳定、方向正确且文字可辨认的受支持图像，并在发送前校验页数、单图大小和请求总大小
- [x] DeepSeek 密钥只存在于 AI 网关运行配置，不进入 review-service、仓库、日志或测试数据
- [x] submission-service 能够为一次有效 PDF 提交幂等创建 `BASIC_REVIEW_V1` 任务
- [x] review-service 能够并发安全地领取一个等待任务，并明确维护等待、评审中、已完成和失败状态
- [x] review-service 通过 common-ai 调用 ai-gateway-service，不再直接调用供应商接口
- [x] AI 网关能够按 DeepSeek Vision 官方协议发送有序论文页面图像并返回统一响应
- [x] AI 网关供应商维护契约使用 Provider ID、显示名称、API 地址、协议、密钥引用和模型目录的通用结构，并固定支持 `openai-completions`、`openai-responses`、`anthropic-messages`
- [x] 模型目录独立维护输入/输出模态、JSON 输出、思考、工具调用及上下文和图像限制；路由不能由供应商或协议名称推断多模态能力
- [x] 建立评审版本总表、公共任务表、追加式任务日志表和 V1 独立结果表，公共字段命名与类型为后续版本保持一致
- [x] 版本记录包含不可变版本标识、版本说明、流程摘要、最终得分契约版本和创建时间，使版本差异无需阅读源码即可理解
- [x] V1 Prompt 和 V1 JSON 过程契约固定，任意模型输出不是合法 JSON、字段缺失或分数越界时任务不能显示为成功
- [x] 所有版本最终结果以统一 `score` 收敛，V1 结果表与公共查询接口按该语义落地
- [x] V1 成功结果包含统一 `score`、总体总结、V1 四个评审维度的分数与评语，以及主要优点、主要问题和改进建议
- [x] 每次领取、PDF 获取、页面渲染、模型调用、结果校验、保存和失败均产生可追踪且不包含敏感正文的任务日志
- [x] 用户能够从真实提交入口进入评审详情，并查看等待、评审中、成功和失败页面状态
- [x] 页面刷新或重新进入后能够从服务端恢复任务和结果，不把轮询超时误显示为评审失败
- [x] 队伍成员能够查看本队评审结果，非队伍成员不能越权查看
- [x] 失败任务能够由队伍成员重试，已完成任务不能重复重试或产生重复结果
- [x] 自动化测试覆盖幂等创建、任务领取、成功结果、非法模型输出、依赖失败、权限校验和失败重试
- [x] 涉及的前端、网关、submission-service、review-service、ai-gateway-service、MinIO、MySQL 和 Nacos 能够按项目真实方式启动
- [x] 使用一份真实 PDF 和真实 DeepSeek 模型完成浏览器端到端验收，至少验证一次成功评审和一个关键失败路径
- [x] 将 `data/problem-01/problem.md` 和 `data/problem-02/problem.md` 作为测试题目数据幂等导入测试环境，并通过公开题目接口验证
- [x] 使用 `data/` 中题目匹配且未超过上传限制的 PDF 完成至少两个不同题目的真实评审
- [x] 使用 `data/problem-02/submission-04.pdf` 验证当前 20 MiB 上传限制，拒绝后不得创建评审任务
- [x] 更新 V1、AI 网关调用边界和用户查询流程相关设计文档，使文档与实际代码和运行结果一致

### 当前验收记录

- DeepSeek 文本接口和 `deepseek-v4-flash-vision-exp` 图像接口均已使用当前账号真实调用成功；Kimi 密钥也已通过官方模型目录接口验证。
- 已使用 MinIO 中一份 899,651 字节、3 页的真实 PDF 跑通 `submission-service -> review-service -> ai-gateway-service -> DeepSeek` 链路。任务最终为 `COMPLETED`，结果得分为 85，固定 JSON 字段和七个执行步骤日志均成功落库。
- 已验证不支持的 BMP 媒体类型返回统一错误 `41105`，超过 1M 上下文预算的请求在调用供应商前返回 `41108`。
- 后端全量 `mvn test` 和前端 `npm run build` 已通过；测试产生的临时提交、任务、结果和日志已经清理。
- 已通过真实接口幂等导入两个 `data/` 题目，并由公开列表与详情接口核对题目 ID、标题和 Markdown 正文。
- 已从浏览器创建并开始两个题目对应的练习队伍，分别上传匹配论文完成真实评审，题目 A 得分 82、题目 B 得分 88；刷新页面后结果保持一致。
- 已在浏览器观察等待、评审中、已完成和失败状态；通过暂停 AI 网关产生真实失败任务，恢复服务后由页面重试并完成，重试结果得分 82。
- 队伍成员查询返回完整结果，非成员查询返回 `40702`；已完成任务禁止重试、依赖失败、领取竞争和失败重试均有自动化测试覆盖。
- `submission-04.pdf`（22,197,988 字节）在浏览器端按 20 MiB 限制被拒绝，提交表记录数保持为 0；同时修复了存储层默认 10MB 与页面 20MB 不一致的问题。
- 浏览器验收发现并修复了题目、队伍、提交和评审任务 Snowflake ID 的 JSON 精度丢失问题，并增加序列化回归测试。
- AI 网关已使用统一供应商配置结构维护 Provider ID、显示名称、API 地址、协议和密钥引用；模型目录继续独立维护能力与限制。`openai-completions`、`openai-responses` 和 `anthropic-messages` 均已实现请求内容块、鉴权、结构化输出、停止原因和用量响应映射，并通过协议契约测试。V1 的既有调用仍锁定 `openai-completions`，未因补齐协议而改变评审语义。

### 修改范围

- `LeetModel-backend/review-service/` 的 V1 任务执行、结果校验、查询、重试和自动化测试
- `LeetModel-backend/common/common-ai/` 的 V1 多模态调用所必需的统一契约，不提前抽象其他业务能力
- `LeetModel-backend/ai-gateway-service/` 的 DeepSeek 多模态适配和完成真实调用所必需的配置与测试
- `LeetModel-backend/submission-service/` 和 `common-api` 中完成提交到评审任务衔接所必需的契约与测试
- `LeetModel-frontend/` 中提交后的评审入口、任务状态和基础评审结果页面
- `data/` 中 V1 测试题目、论文数据说明，以及完成幂等测试数据导入所必需的脚本
- V1 AI 评审、AI 网关调用边界和论文处理链路相关设计文档
- `LeetModel-backend/review-service/` 的 Flyway 迁移，包括版本总表、公共任务与日志表、V1 独立结果表
- `TODO.md`

### 非目标

- 不开发 V2、V3 或动态评审工作流
- 不开发 AI 裁判、评审版本质量比较和自动化实验平台
- 不开发排行榜
- 不完善 admin 端，不新增 AI 调用监控、成本或稳定性看板
- 不实现用户评分、人工标注、申诉或人工修改 AI 分数
- 不实现论文改进建议、AI 助手或 RAG
- 不引入 RocketMQ、流式输出、多模型自动切换、复杂限流、熔断或重试治理
- 不迁移 `review-service` 的模块名、配置名和代码包名
- 不进行与基础 AI 评审用户闭环无关的重构
