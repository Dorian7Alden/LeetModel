# LeetModel TODO

> 本文件只保存当前任务、候选任务和满足条件后才启动的事项。已完成任务和阶段过程不在此保留；已经确认的长期边界与后续触发条件保留在本文件，具体设计和实现结论以 `docs/` 为准。

## 使用约定

- `[ ]` 表示待开始，`[~]` 表示进行中，`[!]` 表示被外部条件阻塞。
- 每次开始前阅读根 `AGENTS.md`、`README.md`、本文件、相关规范、目标模块 README 和任务指向的设计文档。
- 修改公共契约前检查全部生产者和消费者；数据库变更只新增 Flyway 迁移。
- 任务完成至少同步受影响的正式文档并运行目标模块测试；公共模块变化时验证直接消费者。
- 不在配置、日志、测试夹具或文档中保存真实密钥、Relay Token、Prompt、回答正文、知识片段或论文内容。
- new-api Relay Token 由 `ai-gateway-service` 的运行环境提供；具体脱敏和模型复核规则见 [AI 网关测试与验收](docs/project/03-微服务设计/ai-gateway-service/22-测试与验收.md)。
- 模型选型约定：将 New-API 提供的 `gemini-3.8-flash-high` 调整为平台全局首选使用的模型（涵盖客服对话、题目推荐、论文建议、评审及功能测试联调）。
- 可以自主创建本地阶段分支、执行任务卡原子 commit 和阶段 merge；未经用户明确授权，不执行 push、force push、rebase、破坏性 reset、改写历史、远端分支操作或删除用户文件。

## 当前状态

项目已进入新开发阶段「平台能力增强阶段」（阶段分支 `phase/file-search-eval`），目标是在统一文件资产、预签名直传与超大附件、题库全文检索和评测指标口径等方面补齐能力短板，提升平台技术复杂度。`v2.1.1` 保留为已发布稳定基线。

当前唯一任务卡：T7 评测综合评分口径与权重说明（进行中）。阶段内按任务卡清单顺序串行推进，上一张任务卡完成验收并形成原子提交后，才能开始下一张。完整路线图、任务卡和实现流程见文末「平台能力增强阶段」。

## 已确认的系统边界

- SkyWalking 是唯一 Trace/APM 实现；Prometheus 是指标与告警权威来源；运行日志不能代替操作审计。
- 业务 `traceId` 必须保留并可在 Trace 采样或遥测后端不可用时回退到结构化日志和业务事实。
- audit-service 只负责中央不可变归档与受信只读查询，不拥有用户、题目、提交、AI 任务、消息任务或生产配置等领域规则。
- 管理端通过受权限保护的服务接口执行取消、暂停、恢复、重放和回滚；Grafana、Prometheus、SkyWalking UI 只读观察。
- 遥测后端故障不得阻塞普通业务主链；审计 Outbox 保留并重试，达到严重水位时高风险治理操作 fail-closed。
- 指标、日志、Trace 和审计均遵守低基数、最小数据和脱敏边界，不保存用户/队伍/提交/任务/调用标识作为 Prometheus 标签。
- Sentinel 负责 HTTP 边缘防刷限流与 Feign 慢调用/异常熔断；严禁侵入长耗时 AI 调度和 RocketMQ 消费端。

## 执行规则

1. 每次只选择一个编号任务卡，不直接领取整个阶段。
2. 收到新的前端调整后，先将用户意图、范围和验收点记录到 `TODO.md`，再开始实现；完成前必须回看对应任务卡并逐项核对，未完成项继续保留为进行中。
3. 简单的前端样式微调不运行构建或浏览器检查；只有用户明确要求，或改动涉及逻辑、交互、契约和较大布局风险时才执行相应验证。
4. 默认由用户确认任务范围；托管模式下 Agent 按已经确认的路线图和依赖串行推进。
5. 任务依赖未满足时不得用临时硬编码绕过，应明确标记阻塞。
6. 新发现的问题若不阻断当前闭环，只记录为候选任务，不扩大当前任务。
7. 任务完成后，只把后续仍有效的决策与边界合并到所属正式文档，然后删除已完成任务卡；不保存完成历史或单独的阶段过程归档。

## 平台能力增强阶段（phase/file-search-eval）

> 本阶段重新开启功能开发。路线图、任务卡边界和实现流程记录在此，避免关键信息只存在于会话上下文。全部任务最终都要实现，但同一时间只推进一张任务卡。

### 阶段目标

- 建立全平台统一文件资产身份：业务服务只保存稳定 `fileId`，`file-service` 成为文件技术元数据、上传会话、访问策略和生命周期的唯一所有者。
- 支持客户端预签名直传对象存储与大文件分片断点续传，覆盖 GB 级赛题附件。
- 让公开题库支持标题与题面的分词全文检索和组合过滤，并保留 SQL 降级路径。
- 把工作流与模型组合的综合评分口径（`versionSelectionIndex`）写入正式设计文档，使评分可解释、可追溯。

### 现状与关键补充（防止理解偏差）

1. 文件资产当前是分散的。`file-service` 已独立拥有 `lm_file`，提供手动资产、历史盘点、临时访问和延迟清理；但题目附件、用户头像、正式论文仍由各业务服务保存对象路径（`object_key`/`object_name`/`avatar_path`），统一 `fileId` 尚未落地。
2. 上传当前是后端中转。`StorageService` 只有 `MultipartFile` 上传和 GET 预签名下载，没有预签名 PUT 上传链路；上传上限为 `submission-service` 20MB、`file-service`/`problem-service` 约 50MB。数模赛题附件可能包含超大 CSV 或原始数据集，达到 GB 级，属于真实缺口。
3. 题库检索已按 T6 落地。`problem-service` 关键词查询优先走题库 Elasticsearch 索引（标题 `cjk` 分词权重 2、题面权重 1，结构化条件走 `filter` 子句），索引缺失时启动自举全量重建，检索不可用、索引缺失或超出结果窗口时降级到标题 `LIKE` 与动态 SQL 过滤。设计口径见 [公开题库全文检索](docs/project/03-微服务设计/problem-service/公开题库/全文检索.md)。
4. 审计不做完整快照。`OperationAuditPayloadV1` 只保存 `beforeSummary`/`afterSummary` 关键字段摘要，并明确排除请求响应正文、Prompt、回答与敏感数据。这是有意设计，本阶段不新增完整实体快照。
5. 评测已有综合评分。`versionSelectionIndex` 综合工作流版本与模型执行配置，覆盖评分分差/稳定性、成功率、响应耗时、输入输出 Token 与成本等指标；本阶段只补齐口径文档，不新建第二套评分体系。
6. 论文临时分片保留在 `submission-service`。这是既有设计确认的过渡边界，本阶段不强行收归 `file-service`。

### 既有设计依据（先读后改）

文件方向以既有已确认设计为准，本阶段是“实现已设计目标”，不是重新设计。开工前必须阅读：

- [文件资产管理架构](docs/project/02-架构设计/文件资产管理架构.md)
- [file-service 文件资产管理](docs/project/03-微服务设计/file-service/文件资产管理/README.md)
- [上传与访问](docs/project/03-微服务设计/file-service/文件资产管理/上传与访问.md)
- [引用与删除](docs/project/03-微服务设计/file-service/文件资产管理/引用与删除.md)
- [数据与分组](docs/project/03-微服务设计/file-service/文件资产管理/数据与分组.md)
- [迁移与实施边界](docs/project/03-微服务设计/file-service/文件资产管理/迁移与实施边界.md)

评测口径暂无专门设计文档，对应任务卡开工前先补设计文档，再校对实现。

### 阶段实现流程（按 05-git-workflow-spec 执行）

1. 阶段分支固定为 `phase/file-search-eval`，只能从 `dev` 创建，不得从 `master` 或其他分支派生。
2. 同一时间只推进一张任务卡：按任务卡清单顺序与依赖串行；上一张完成验收并提交后才能开始下一张。
3. 每张任务卡的执行顺序：补/更新设计文档 → 后端代码 → Flyway 迁移（只新增）→ 配置 → 前端（如有）→ 自动化测试与真实运行验收。
4. 提交粒度遵守 [Git 分支与提交规范](docs/standards/05-git-workflow-spec.md)：后端、前端、SQL、配置、文档分别提交；一次提交只表达一个意图；不使用 `--no-verify`。
5. 阶段内不混入其他阶段或路线图外工作；发现不阻断当前闭环的问题只记录为候选任务。
6. 阶段全部任务完成并经用户验收后，使用 `git merge --no-ff phase/file-search-eval` 合入 `dev`，随后删除已确认合并的本地阶段分支；是否发布到 `master` 由用户决定。
7. 未经用户授权，不执行 push、force push、rebase、破坏性 reset 或远端分支操作。

### 任务卡清单

| 顺序 | 任务卡 | 目标摘要 | 依赖 | 状态 |
|------|--------|----------|------|------|
| 1 | T7 | 评测综合评分口径与权重说明 | 无 | 进行中 |

#### T7 评测综合评分口径与权重说明

- 背景：`ai-evaluation-service` 会为「AI 工作流版本 × 模型执行配置」的组合计算综合评分（代码中的 `versionSelectionIndex`），用于在多个候选组合之间比较并挑出执行方案。当前只有代码实现，没有一份说明指标来源、权重和归一化规则的正式文档，导致评分结论无法解释、无法追溯，也无法判断某个指标异常时应该看哪张表、哪个服务。
- 目标：在 `docs/project/03-微服务设计/ai-evaluation-service/` 下补齐评测口径设计文档，完整说明 `versionSelectionIndex` 由哪些原始指标、按什么归一化方式和权重得出，以及工作流版本与模型执行配置分别影响哪些指标。
- 入口：`ai-evaluation-service` 的评测任务、原始指标记录与综合评分结果；管理端 AI 评测页面展示的对比结果。
- 实现过程：
  1. 先读实现：定位 `versionSelectionIndex` 的计算代码、评测任务调度入口、原始指标写入位置，确认实际公式、权重常量和归一化方式。
  2. 列出指标清单：有效性、评分分差/稳定性、成功率、响应耗时、输入与输出 Token、成本等，逐项标注原始来源（哪张表、哪个字段、由哪个服务或哪次调用写入）。
  3. 写清归一化与权重：每个指标如何映射到可比区间、是否加权、如何处理越界值，说明综合评分的取值范围和方向（越大越优或越小越优）。
  4. 说明组合影响：工作流版本决定评分分差与结果稳定性，模型执行配置决定响应耗时与成本，两者共同决定综合评分；需要写清哪些指标归工作流、哪些归模型、哪些由两者共同决定。
  5. 处理缺失数据：说明样本不足、用量或价格缺失、调用失败时的取值与降级规则。
  6. 校对一致性：文档必须与当前实现一致；如果发现实现与上述口径不符，先记录差异并向用户确认，再决定是否修改代码。
- 修改范围：`docs/project/03-微服务设计/ai-evaluation-service/` 相关设计文档；必要时补充指标来源与数据字典说明；默认不改评分算法实现。
- 完成标准：
  - 文档能独立回答“这个分数怎么算出来的”，包含指标清单、权重、归一化方式和排序方向。
  - 每个指标的原始来源可追溯到具体表与字段，能据此定位异常数据。
  - 综合评分受工作流与模型两个维度影响的机制有明确说明，不依赖外部材料即可理解。
  - 成本、Token、耗时等指标在数据缺失时有明确处理说明。
  - 文档与当前实现一致，不存在过时表述；发现不一致时已向用户确认处理方式。
- 非目标：不新建第二套评分体系；不重构评测调度；不改变已确认的权重方案。

### 暂缓与不做

- 审计完整实体快照：不做，保留 `beforeSummary`/`afterSummary` 设计。
- 强制物理删除文件：仍按既有设计暂缓，需要独立高风险权限与审计后再评估。
- Markdown 外链自动本地化、静态资源动态替换、压缩包在线解压：非目标。
