# LeetModel TODO

> 本文件只保存当前任务、候选任务和满足条件后才启动的事项。已完成任务和阶段过程不在此保留；已经确认的长期边界与后续触发条件保留在本文件，具体设计和实现结论以 `docs/` 为准。

## 使用约定

- `[ ]` 表示待开始，`[~]` 表示进行中，`[!]` 表示被外部条件阻塞。
- 每次开始前阅读根 `AGENTS.md`、`README.md`、本文件、相关规范、目标模块 README 和任务指向的设计文档。
- 修改公共契约前检查全部生产者和消费者；数据库变更只新增 Flyway 迁移。
- 任务完成至少同步受影响的正式文档并运行目标模块测试；公共模块变化时验证直接消费者。
- 不在配置、日志、测试夹具或文档中保存真实密钥、Relay Token、Prompt、回答正文、知识片段或论文内容。
- new-api Relay Token 由 `ai-gateway-service` 的运行环境提供；具体脱敏和模型复核规则见 [AI 网关测试与验收](docs/project/03-微服务设计/ai-gateway-service/22-测试与验收.md)。
- 模型选型约定：将 New-API 提供的 `gemini-3.8-flash-high` 调整为平台全局首选使用的模型（涵盖客服对话、题目推荐、论文建议、评审及功能测试联调）。
- `cli-proxy-api` 是长期运行且不属于本项目的本地 Docker 服务，固定占用宿主机 `8085`；不得停止、重启、改端口或修改其配置。LeetModel 本地 submission-service 使用 `8092`，启动与验收必须避开该容器。
- 可以自主创建本地阶段分支、执行任务卡原子 commit 和阶段 merge；未经用户明确授权，不执行 push、force push、rebase、破坏性 reset、改写历史、远端分支操作或删除用户文件。

## 当前状态

当前切入阶段分支 `phase/assistant-refinement`，推进 AI 客服的前端体验修复与后端核心架构（会话管理、上下文修剪、知识库与工具调用）演进。

## 当前任务

暂无待推进任务。当前阶段所有设计与实现任务已完整交付并验收。

---

## 候选阶段规划：AI 客服完善与后端架构演进（Assistant-Refinement）

> 模型选型说明：功能测试、联调验证与生产默认统一将 New-API 提供的 `gemini-3.8-flash-high` 作为全局首选模型。

### 阶段一：全局模型路由与网关能力就绪（基础设施前置）
- [ ] 任务 1.1：`gemini-3.8-flash-high` 网关接入与路由对齐
  - 文档设计：更新 `docs/project/03-微服务设计/ai-gateway-service/` 中的模型能力目录与 TEXT/TOOLS 路由映射设计。
  - 功能开发：在 `ai-gateway-service` 的配置中注册并启用 `NEW_API/gemini-3.8-flash-high`，将其配置为 TEXT 默认首选路由。
  - 功能测试：执行网关本地协议测试与真实 New-API 连通性冒烟，验证模型 Chat 与标准工具调用契约。

### 阶段二：前端交互体验修复与品牌形象落地（立竿见影 UX 攻坚）
- [ ] 任务 2.1：会话标题自动归纳修复与提问乐观更新
  - 文档设计：编写前端交互状态机说明（乐观上屏、思考占位、异常回退与标题自动提取契约）。
  - 功能开发：[AiAssistantWidget.vue](LeetModel-frontend/src/components/common/AiAssistantWidget.vue) 创建会话对齐默认标题 `"新会话"` 触发题意自动截取；发送消息立即推入列表上屏，消除假死空白感。
  - 功能测试：浏览器端验证提问即刻上屏、标题自适应更新、网络异常时草稿与状态回滚。
- [ ] 任务 2.2：题目推荐结构化卡片渲染与交互直达
  - 文档设计：补充前端工具结果渲染协议文档（解析 `toolContextJson`，定义题目卡片 Schema 与路由映射）。
  - 功能开发：在助手回复气泡下方渲染题目富交互卡片（展示题号徽章、标题、年份、难度彩色 Tag 及直达题面的路由链接）。
  - 功能测试：触发查题与荐题场景，验证卡片正常渲染、空候选友好提示、卡片点击无缝跳转题目详情页。
- [ ] 任务 2.3：集成 KaTeX 数学公式排版与代码高亮
  - 文档设计：制定富文本渲染规范（KaTeX 语法边界、防 XSS 策略与代码块样式规范）。
  - 功能开发：升级 [renderSafeMarkdown](LeetModel-frontend/src/utils/markdown.js)，引入 KaTeX 解析与代码块语法高亮及一键复制。
  - 功能测试：验证行内公式 `$x_i$`、块级公式 `$$\min \sum...$$` 及多语言代码块渲染效果与安全性。
- [ ] 任务 2.4：AI 客服专属卡通品牌形象全量替换
  - 文档设计：整理视觉资产规范，明确悬浮气泡按钮、顶栏、欢迎页、消息头像四处点位的图片规格。
  - 功能开发：将 `src/assets/images/` 下的 `AI客服-avatar.png`、`AI客服-smile.png`、`AI客服-default.png` 应用于浮窗、顶栏、欢迎界面与回复头像，替换默认 Element 图标。
  - 功能测试：桌面端与移动端多视口检查形象清晰度、排版自适应与加载流畅度。
  *(注：推荐问题与快捷 Chip 维持前端静态配置，不引入动态接口与额外开销)*

### 阶段三：后端会话管理与上下文工程演进（核心后端保障）
- [ ] 任务 3.1：会话生命周期完善与消息分页拉取
  - 文档设计：在 `docs/project/03-微服务设计/ai-assistant-service/` 中新增会话生命周期与游标分页契约文档。
  - 功能开发：实现会话软删除 `DELETE /api/assistant/conversations/{id}`、用户自定义重命名 `PUT .../title`，详情接口支持游标分页拉取；创建会话自动复用未发问的空白会话。
  - 功能测试：编写 Controller 与 Service 单元测试，验证软删除过滤、标题重命名约束、分页拉取与空会话复用。
- [ ] 任务 3.2：多轮上下文智能修剪与工具事实折叠
  - 文档设计：编写上下文修剪算法与 Token 预算控制设计文档（历史工具折叠规则、滑动窗口与用户偏好提取机制）。
  - 功能开发：在装配历史消息时，对过往轮次的工具原始 JSON 进行摘要收缩，仅保留轻量事实标记，防止 Token 线性膨胀与注意力稀释。
  - 功能测试：多轮交互回归测试，对比长对话历史下的 Token 开销与模型注意力集中度。

### 阶段四：知识库解耦、工具链扩展与流式状态透出（架构深度进阶）
- [ ] 任务 4.1：客服 RAG 迁移至 `knowledge-retrieval-service` 与混合检索
  - 文档设计：编写客服 RAG 迁移与不可变工作流发布设计文档，制定向量 + BM25 混合检索与 RRF 融合重排契约。
  - 功能开发：发布新工作流 `ASSISTANT_TOOLS_RETRIEVAL_V1`；在统一检索服务中实现混合检索，客服服务通过 Feign 委托检索并接入生产配置版本治理。
  - 功能测试：建模专有名词（如“AHP”、“灰色预测”）精准召回对比测试，验证不可变配置切换与回滚。
- [ ] 任务 4.2：只读领域工具扩展与多意图复合编排
  - 文档设计：编写只读业务工具规范（组队状态、提交评测状态查询接口及上下文边界）与非终止型知识工具编排时序。
  - 功能开发：新增 `query_user_team` 与 `query_submission_status` 只读工具；解耦知识工具的终止型硬约束，支持单次回复中“知识讲解 + 题目推荐”复合意图组合执行。
  - 功能测试：编写工具注册、参数 Schema 校验与执行池隔离测试，验证复合意图下的多工具执行循环。
- [ ] 任务 4.3：SSE 流式协议支持与工具执行状态事件透出
  - 文档设计：定义客服 SSE 通信协议格式（`tool_start`、`tool_end`、`delta`、`message_end`）与容灾降级规范。
  - 功能开发：后端提供 SSE 端点，在工具调用与文本生成阶段分别推送结构化事件流；前端接入 EventSource 并渲染动态工具执行折叠条与打字机输出。
  - 功能测试：端到端流式通信测试，验证工具状态流畅展示、打字机流式输出、异常断连重连与失败重试。

### 阶段五：端到端全链路验收与文档归档（交付收官）
- [ ] 任务 5.1：基于 `gemini-3.8-flash-high` 的端到端冒烟与全业务链验收
  - 全量微服务真实联调：覆盖普通问答、查题、荐题、队伍查询、知识讲解、公式渲染、会话管理等全场景冒烟测试。
  - 验证 `ai-evaluation-service` 对新版本的无副作用隔离评测。
- [ ] 任务 5.2：架构设计与微服务文档全量归档同步
  - 检查并全量更新 `docs/project/03-微服务设计/ai-assistant-service/` 及前端设计文档，固化版本演进事实。


## 待梳理服务清单（按推荐顺序）

全平台微服务（common、user、team、problem、submission、ranking、gateway、admin、audit、knowledge-retrieval、ai-gateway、ai-suggestion、ai-review、ai-assistant、ai-evaluation）代码注释已全量对齐项目级工程规范。

## 已确认的系统边界

- SkyWalking 是唯一 Trace/APM 实现；Prometheus 是指标与告警权威来源；运行日志不能代替操作审计。
- 业务 `traceId` 必须保留并可在 Trace 采样或遥测后端不可用时回退到结构化日志和业务事实。
- audit-service 只负责中央不可变归档与受信只读查询，不拥有用户、题目、提交、AI 任务、消息任务或生产配置等领域规则。
- 管理端通过受权限保护的服务接口执行取消、暂停、恢复、重放和回滚；Grafana、Prometheus、SkyWalking UI 只读观察。
- 遥测后端故障不得阻塞普通业务主链；审计 Outbox 保留并重试，达到严重水位时高风险治理操作 fail-closed。
- 指标、日志、Trace 和审计均遵守低基数、最小数据和脱敏边界，不保存用户/队伍/提交/任务/调用标识作为 Prometheus 标签。

## 后续触发条件

以下能力暂不拆实现任务；条件成立后先补充任务卡，再进入开发：

- knowledge-retrieval-service 已服务论文建议；客服 RAG 迁移、在线知识管理和独立扩缩容在出现真实消费者或运维需求后再拆任务。
- 单调度器成为可测瓶颈或必须部署多个调度实例后，评估分布式 AI 调度。
- new-api 无法满足渠道治理且出现明确额外业务需求后，评估 LeetModel 多账号资源池。
- 指标具备可信真值且版本选择指数稳定运行后，才讨论自动推荐生产版本；不得自动激活。
- VIP 高级模型分层属于候选能力。进入设计前必须先确认普通与 VIP 在评审、建议、客服三个功能上的模型选择权、配额与计费、降级规则、运行快照和越权防护；未形成统一模型路由契约前不在各业务前端分别硬编码模型。

## 执行规则

1. 每次只选择一个编号任务卡，不直接领取整个阶段。
2. 默认由用户确认任务范围；托管模式下 Agent 按已经确认的路线图和依赖串行推进。
3. 任务依赖未满足时不得用临时硬编码绕过，应明确标记阻塞。
4. 新发现的问题若不阻断当前闭环，只记录为候选或条件任务，不扩大当前任务。
5. 任务完成后，只把后续仍有效的决策与边界合并到所属正式文档，然后删除已完成任务卡；不保存完成历史或单独的阶段过程归档。
