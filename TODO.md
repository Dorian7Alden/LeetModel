# LeetModel TODO

> 本文件只保存当前任务、候选任务和满足条件后才启动的事项。已完成任务和阶段过程不在此保留；已经确认的长期边界与后续触发条件保留在本文件，具体设计和实现结论以 `docs/` 为准。

## 使用约定

- `[ ]` 表示待开始，`[~]` 表示进行中，`[!]` 表示被外部条件阻塞。
- 每次开始前阅读根 `AGENTS.md`、`README.md`、本文件、相关规范、目标模块 README 和任务指向的设计文档。
- 修改公共契约前检查全部生产者和消费者；数据库变更只新增 Flyway 迁移。
- 任务完成至少同步受影响的正式文档并运行目标模块测试；公共模块变化时验证直接消费者。
- 不在配置、日志、测试夹具或文档中保存真实密钥、Relay Token、Prompt、回答正文、知识片段或论文内容。
- new-api Relay Token 由 `ai-gateway-service` 的运行环境提供；具体脱敏和模型复核规则见 [AI 网关测试与验收](docs/project/03-微服务设计/ai-gateway-service/22-测试与验收.md)。
- 模型选型约定：将 New-API 提供的 `gemini-3.8-flash-high` 调整为平台全局首选使用的模型（涵盖客服对话、题目推荐、论文建议、评审及功能测试联调）。
- 可以自主创建本地阶段分支、执行任务卡原子 commit 和阶段 merge；未经用户明确授权，不执行 push、force push、rebase、破坏性 reset、改写历史、远端分支操作或删除用户文件。

## 当前状态

项目已进入新开发阶段「简历对齐阶段」（阶段分支 `phase/resume-alignment`），目标是把简历中已写、项目未实现或口径不准的能力补齐或校正，提升技术复杂度并服务实习面试叙事。该阶段明确重新开启功能开发，不再受封版范围限制；`v2.1.1` 保留为已发布稳定基线。

当前唯一任务卡：T1 统一文件资产模型（待开始）。阶段内按 T1 → T2 → T3 → T4 串行推进，上一张任务卡完成验收并形成原子提交后，才能开始下一张。完整路线图、任务卡和实现流程见文末「简历对齐阶段」。

## 已确认的系统边界

- SkyWalking 是唯一 Trace/APM 实现；Prometheus 是指标与告警权威来源；运行日志不能代替操作审计。
- 业务 `traceId` 必须保留并可在 Trace 采样或遥测后端不可用时回退到结构化日志和业务事实。
- audit-service 只负责中央不可变归档与受信只读查询，不拥有用户、题目、提交、AI 任务、消息任务或生产配置等领域规则。
- 管理端通过受权限保护的服务接口执行取消、暂停、恢复、重放和回滚；Grafana、Prometheus、SkyWalking UI 只读观察。
- 遥测后端故障不得阻塞普通业务主链；审计 Outbox 保留并重试，达到严重水位时高风险治理操作 fail-closed。
- 指标、日志、Trace 和审计均遵守低基数、最小数据和脱敏边界，不保存用户/队伍/提交/任务/调用标识作为 Prometheus 标签。
- Sentinel 负责 HTTP 边缘防刷限流与 Feign 慢调用/异常熔断；严禁侵入长耗时 AI 调度和 RocketMQ 消费端。

## 执行规则

1. 每次只选择一个编号任务卡，不直接领取整个阶段。
2. 收到新的前端调整后，先将用户意图、范围和验收点记录到 `TODO.md`，再开始实现；完成前必须回看对应任务卡并逐项核对，未完成项继续保留为进行中。
3. 简单的前端样式微调不运行构建或浏览器检查；只有用户明确要求，或改动涉及逻辑、交互、契约和较大布局风险时才执行相应验证。
4. 默认由用户确认任务范围；托管模式下 Agent 按已经确认的路线图和依赖串行推进。
5. 任务依赖未满足时不得用临时硬编码绕过，应明确标记阻塞。
6. 新发现的问题若不阻断当前闭环，只记录为候选任务，不扩大当前任务。
7. 任务完成后，只把后续仍有效的决策与边界合并到所属正式文档，然后删除已完成任务卡；不保存完成历史或单独的阶段过程归档。

## 简历对齐阶段（phase/resume-alignment）

> 本阶段重新开启功能开发。路线图、任务卡边界和实现流程记录在此，避免关键信息只存在于会话上下文。全部任务最终都要实现，但同一时间只推进一张任务卡。

### 阶段目标

把简历中已写、但项目当前未实现或口径不准的能力补齐或校正，使简历与项目事实一致，并借此提升文件基础设施、全文检索和评测口径方面的真实技术复杂度。

### 阶段边界与关键补充（防止理解偏差）

1. 审计不做完整实体快照。当前 `OperationAuditPayloadV1` 只保存 `beforeSummary`/`afterSummary` 关键字段摘要，并明确排除请求响应正文、Prompt、回答与敏感数据。这是更合理的设计，不新增完整快照。
2. `versionSelectionIndex` 就是综合评分。它同时受工作流版本与模型执行配置影响，覆盖评分分差/稳定性、成功率、响应耗时、输入输出 Token 与成本等指标。简历写“综合评分”成立，本阶段只补齐口径说明，不新建第二套评分体系。
3. GB 级附件是业务特点而非夸大。数模赛题附件可能包含超大 CSV、原始数据集等数据文件。当前 `submission-service` 最大 20MB，`file-service`/`problem-service` 约 50MB，且上传走 `MultipartFile` 后端中转，属于真实缺口。
4. 统一 `fileId` 可以直接重构。项目未上线，无需兼容历史数据。目标是业务服务只保存稳定 `fileId`；`file-service` 成为文件技术元数据、上传会话和生命周期的唯一所有者；业务表不再保存 `objectKey`/`object_name`。
5. 题库 ES 全文检索当前确实缺失。`problem-service` 目前只有标题 `LIKE` 与动态 SQL 过滤，且 `problem-service` README 与公开题库 README 均把 ES 全文搜索列为当前非目标。
6. 预签名直传当前确实缺失。`StorageService` 只有 `getUrl`（GET 预签名下载）与 `MultipartFile` 上传，没有预签名 PUT 上传链路。
7. 论文临时分片仍保留在 `submission-service`。这是既有设计确认的过渡边界，本阶段不强行收归 `file-service`。

### 既有设计依据（先读后改）

文件方向以既有已确认设计为准，本阶段是“实现已设计目标”，不是重新设计。开工前必须阅读：

- [文件资产管理架构](docs/project/02-架构设计/文件资产管理架构.md)
- [file-service 文件资产管理](docs/project/03-微服务设计/file-service/文件资产管理/README.md)
- [上传与访问](docs/project/03-微服务设计/file-service/文件资产管理/上传与访问.md)
- [引用与删除](docs/project/03-微服务设计/file-service/文件资产管理/引用与删除.md)
- [数据与分组](docs/project/03-微服务设计/file-service/文件资产管理/数据与分组.md)
- [迁移与实施边界](docs/project/03-微服务设计/file-service/文件资产管理/迁移与实施边界.md)

题库 ES 检索与评测口径暂无专门设计文档，对应任务卡开工前先补设计文档，再写代码。

### 阶段实现流程（按 05-git-workflow-spec 执行）

1. 阶段分支固定为 `phase/resume-alignment`，只能从 `dev` 创建，不得从 `master` 或其他分支派生。
2. 同一时间只推进一张任务卡：T1 → T2 → T3 → T4 按依赖串行；上一张完成验收并提交后才能开始下一张。
3. 每张任务卡的执行顺序：补/更新设计文档 → 后端代码 → Flyway 迁移（只新增）→ 配置 → 前端（如有）→ 自动化测试与真实运行验收。
4. 提交粒度遵守 [Git 分支与提交规范](docs/standards/05-git-workflow-spec.md)：后端、前端、SQL、配置、文档分别提交；一次提交只表达一个意图；不使用 `--no-verify`。
5. 阶段内不混入其他阶段或路线图外工作；发现不阻断当前闭环的问题只记录为候选任务。
6. 阶段全部任务完成并经用户验收后，使用 `git merge --no-ff phase/resume-alignment` 合入 `dev`，随后删除已确认合并的本地阶段分支；是否发布到 `master` 由用户决定。
7. 未经用户授权，不执行 push、force push、rebase、破坏性 reset 或远端分支操作。

### 任务卡清单

| 任务卡 | 目标摘要 | 依赖 | 状态 |
|--------|----------|------|------|
| T1 | 统一文件资产模型，业务只存 `fileId` | 无 | 待开始 |
| T2 | 预签名直传与超大附件（GB 级）上传 | T1 | 待开始 |
| T3 | 题库 Elasticsearch 全文检索 | T1 | 待开始 |
| T4 | 评测综合评分口径与简历事实对齐 | T1、T2、T3 | 待开始 |

#### T1 统一文件资产模型（当前任务卡）

- 目标：`file-service` 成为文件技术元数据、上传会话和生命周期的唯一所有者；业务表只保存稳定 `fileId`，不再保存 `objectKey`/`object_name`。
- 入口：管理员文件中心、题目附件管理、用户头像、论文提交四条现有链路。
- 主流程：业务请求创建文件资产 → `file-service` 分配 `fileId` → 对象落 MinIO → 业务事务保存 `fileId` 与业务关系 → 业务经 Outbox 发布绑定/解绑事件 → `file-service` 幂等维护引用投影。
- 修改范围：`file-service` 数据模型与 API；`problem-service` 附件表改用 `file_id`；`user-service` 头像改用 `file_id`；`submission-service` 正式论文改用 `file_id`；公共契约；Flyway 迁移；相关设计文档。
- 完成标准：题目附件、头像、论文三条链路都能只凭 `fileId` 读取与访问；业务表不再出现对象路径字段；绑定/解绑事件幂等；引用投影为空才允许进入待清理。
- 非目标：不重做 MinIO 二进制存储；不引入跨库分布式事务；不接管论文临时分片（T2 处理）；不做完整审计快照。

#### T2 预签名直传与超大附件上传

- 目标：支持预签名上传与分片/断点续传，覆盖 GB 级赛题附件，二进制不经过业务服务中转。
- 入口：题目附件上传、论文提交上传、管理员手动素材上传。
- 主流程：客户端请求上传会话 → `file-service` 返回预签名上传凭证 → 客户端直传 MinIO → 完成确认时校验大小/类型/摘要 → 生成正式 `fileId` → 业务绑定。
- 修改范围：`file-service` 上传会话与预签名 PUT；`StorageService` 增加预签名上传能力；前端上传流程；分片与过期清理；`compose.yaml` 与上传大小配置。
- 完成标准：GB 级文件从客户端直传 MinIO 且不经过业务服务内存；中断可恢复；分片摘要校验；合并后生成正式 `fileId`；未完成会话按过期策略清理。
- 非目标：首期不做分片大小自适应优化；不统一接管论文临时分片以外的历史对象。
- 关键补充：GB 级与“赛题附件可能包含超大 CSV”是业务特点，当前默认上传限制是真实缺口，必须通过本任务卡解除。

#### T3 题库 Elasticsearch 全文检索

- 目标：为题库标题与题面建立 ES 索引，支持关键词全文检索与布尔过滤，并保留 SQL 降级。
- 入口：公开题库搜索框与筛选。
- 主流程：题目发布/更新/删除 → 同步索引（或增量重建）→ 查询走 ES 多字段匹配 + 赛事/年份/难度/标签过滤 → ES 不可用时降级回现有 SQL。
- 修改范围：`problem-service` 索引同步与查询；ES 索引结构；前端搜索交互；设计文档更新（现有 README 把 ES 全文列为非目标，需同步修改）。
- 完成标准：标题和题面可分词检索；关键词与标签/赛事/年份/难度组合过滤生效；索引同步与重建可用；ES 故障时仍返回 SQL 结果。
- 非目标：不替换现有动态 SQL 结构化筛选；不引入 AI 语义检索（RAG 已在 `knowledge-retrieval-service` 实现）。

#### T4 评测口径与简历事实对齐

- 目标：把 `versionSelectionIndex` 的“综合评分”口径写入正式文档，并同步简历与面试说明；对全量简历逐条标注“已实现/已校正”。
- 入口：`ai-evaluation-service` 评测结果与对应设计文档。
- 主流程：核对指标来源（有效性、评分分差/稳定性、成功率、响应耗时、Token/成本）→ 明确工作流版本与模型配置隔离 → 更新设计文档与权重说明 → 校对简历措辞。
- 完成标准：综合评分口径在设计文档可追溯；简历每条表述都能指向项目代码或文档；不存在“完整快照”等过时表述。
- 非目标：不新建第二套评分体系；不改动已有 `versionSelectionIndex` 的算法定义（除非发现与简历口径冲突）。

### 暂缓与不做

- 审计完整实体快照：不做，保留 `beforeSummary`/`afterSummary` 设计。
- 强制物理删除文件：仍按既有设计暂缓，需要独立高风险权限与审计后再评估。
- Markdown 外链自动本地化、静态资源动态替换、压缩包在线解压：非目标。
